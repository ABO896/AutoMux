---
id: 260806-l0d
type: quick
status: in-progress
date: 2026-08-06
---

# Quick Task 260806-l0d: v2.0.1 release prep + README refresh

The new app icon (260806-krh) exists only locally. The published v2.0 release
was built 2026-08-04, before the icon existed, so downloaded builds still carry
the stock Tauri mark. Ship it via a 2.0.1 patch release, and bring the README
up to date with what v2.0 actually delivers.

## Decisions (locked, confirmed with user)

- Patch bump to **2.0.1** + tag `v2.0.1`. Not a v2.0 re-cut — existing download
  links stay valid.
- README gets a **full refresh**, not just an icon drop.
- CI opens a **draft** release; the user reviews and publishes. Do not publish.

## Tasks

### 1. Version bump to 2.0.1
- files: `package.json`, `src-tauri/Cargo.toml`, `src-tauri/tauri.conf.json`,
  `src-tauri/Cargo.lock`
- action: Set version to 2.0.1 in all four. `package-lock.json` carries an
  unrelated `0.1.0` and is left alone.
- verify: `grep -rn '2\.0\.1'` hits exactly the four files; no `2.0.0` remains
  in version fields.
- done: all version fields read 2.0.1.

### 2. README full refresh
- files: `README.md`, `docs/assets/icon-256.png` (new)
- action: Add 256px icon to the header. Rewrite Features against shipped v2.0
  scope (parallel execution, macro management, theming, hotkey conflict safety,
  profiles). Fix the Tauri **v1** prerequisites link → v2. Add real
  requirements (Node 24, Rust stable, macOS 12+, Windows x64). Document the
  unsigned-build Gatekeeper/SmartScreen step — builds are unsigned
  (`signingIdentity: null`, `certificateThumbprint: null`).
- verify: every claim traceable to source; every link resolves.
- done: README reflects v2.0.1 and renders the icon.

### 3. Push + tag
- files: none (git refs)
- action: Push master, then tag `v2.0.1` and push the tag to trigger
  `release.yml`.
- verify: `gh run list` shows the Release workflow started.
- done: CI building; draft release pending user publish.
