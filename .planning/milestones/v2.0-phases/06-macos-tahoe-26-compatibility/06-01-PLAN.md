---
phase: 06-macos-tahoe-26-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/platform/macos/mod.rs
  - src-tauri/Info.plist
autonomous: true
requirements:
  - COMPAT-01
  - COMPAT-02
  - COMPAT-03

must_haves:
  truths:
    - "check_accessibility_permissions(false) consults the live TCC database, not the per-process AXIsProcessTrusted cache"
    - "A successful CGEventTap::new() probe returns true from check_accessibility_permissions(false)"
    - "A failed CGEventTap::new() probe returns false from check_accessibility_permissions(false)"
    - "The probe tap does not set TAP_INITIALIZED or interact with TAP_STARTING"
    - "The app bundle's Info.plist includes NSAccessibilityUsageDescription so it appears in System Settings Accessibility list"
    - "The project compiles clean on macOS with cargo build"
  artifacts:
    - path: "src-tauri/src/platform/macos/mod.rs"
      provides: "check_accessibility_permissions() with live CGEventTap probe replacing AXIsProcessTrusted() on prompt=false path"
      contains: "CGEventTap::new"
    - path: "src-tauri/Info.plist"
      provides: "NSAccessibilityUsageDescription key merged into app bundle at build time"
      contains: "NSAccessibilityUsageDescription"
  key_links:
    - from: "src-tauri/src/platform/macos/mod.rs"
      to: "src-tauri/src/ipc/mod.rs:check_accessibility"
      via: "check_accessibility_permissions(false) return value"
      pattern: "check_accessibility_permissions"
    - from: "src-tauri/Info.plist"
      to: "AutoMux.app/Contents/Info.plist"
      via: "Tauri CLI auto-merge at bundle time"
      pattern: "NSAccessibilityUsageDescription"
---

<objective>
Fix the macOS Tahoe 26 regression where AXIsProcessTrusted() returns false from a stale per-process cache even after the user grants Accessibility in System Settings. Replace the broken cached call with a live CGEventTap probe in check_accessibility_permissions(false). Also add NSAccessibilityUsageDescription to src-tauri/Info.plist so the app appears correctly in the Accessibility Settings list.

Purpose: COMPAT-01 and COMPAT-02 are causally linked — fixing the permission detection (probe returning true) unblocks CGEventTap arming in the IPC layer, which unblocks all macro execution. COMPAT-03 is addressed by the Info.plist addition ensuring the app is grantable in System Settings.

Output:
- src-tauri/src/platform/macos/mod.rs — check_accessibility_permissions() rewritten on the !prompt path
- src-tauri/Info.plist — new file; Tauri CLI merges it at build time; no tauri.conf.json change needed
</objective>

<execution_context>
@/Users/alvaro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alvaro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-macos-tahoe-26-compatibility/06-RESEARCH.md
@.planning/phases/06-macos-tahoe-26-compatibility/06-PATTERNS.md
</context>

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. Do not explore — use these directly. -->

From src-tauri/src/platform/macos/mod.rs (current full file — 55 lines):
- pub fn check_accessibility_permissions(prompt: bool) -> bool
- extern "C" { fn AXIsProcessTrusted() -> u8; fn AXIsProcessTrustedWithOptions(options: *const c_void) -> u8; }
- The entire !prompt branch is currently: return AXIsProcessTrusted() != 0;  (line 23 — the ONLY line changing)
- The prompt=true branch (lines 27-53) uses CFDictionary unsafe FFI and is UNCHANGED.

From src-tauri/src/ipc/mod.rs (check_accessibility handler — unchanged):
  pub async fn check_accessibility() -> Result<bool, String> {
      let granted = crate::platform::macos::check_accessibility_permissions(false);
      if granted { crate::platform::macos::observer::initialize_tap(); }
      Ok(granted)
  }

From src-tauri/src/platform/macos/observer.rs (CGEventTap::new pattern — lines 250-268):
  CGEventTap::new(
      CGEventTapLocation::HID,
      CGEventTapPlacement::HeadInsertEventTap,
      CGEventTapOptions::ListenOnly,
      vec![CGEventType::MouseMoved],   // probe uses single type; real tap uses full list
      |_, _, event| Some(event.clone()),
  )

Imports used in observer.rs (same crate; copy style):
  use core_graphics::event::{
      CGEventTap, CGEventTapLocation, CGEventTapOptions, CGEventTapPlacement, CGEventType,
  };

TAP_INITIALIZED / TAP_STARTING guards live ONLY in observer.rs::initialize_tap(). The probe in mod.rs must NEVER touch them.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace AXIsProcessTrusted() with live CGEventTap probe in check_accessibility_permissions(false)</name>
  <files>src-tauri/src/platform/macos/mod.rs</files>
  <read_first>
    - src-tauri/src/platform/macos/mod.rs — read the full file (55 lines) before touching anything
    - src-tauri/src/platform/macos/observer.rs lines 1-15 — confirm CGEventTap import style
    - src-tauri/src/platform/macos/observer.rs lines 245-285 — confirm CGEventTap::new() call signature
  </read_first>
  <behavior>
    - When Accessibility is granted and CGEventTap::new() succeeds: check_accessibility_permissions(false) returns true
    - When Accessibility is not granted and CGEventTap::new() fails: check_accessibility_permissions(false) returns false
    - check_accessibility_permissions(true) behavior is entirely unchanged (no diff on that branch)
    - The probe tap is dropped at the end of the !prompt branch (it is scoped to a local binding)
    - TAP_INITIALIZED and TAP_STARTING are never read or written by check_accessibility_permissions()
  </behavior>
  <action>
    Rewrite ONLY the !prompt branch of check_accessibility_permissions() in src-tauri/src/platform/macos/mod.rs. Do not touch the prompt=true branch.

    The change:
    1. The `extern "C"` block retains AXIsProcessTrustedWithOptions (used in prompt=true path). Remove the AXIsProcessTrusted declaration (it becomes unused after the fix) to avoid a dead-code warning.
    2. The function body restructures as follows: the `unsafe` block moves inward to wrap ONLY the prompt=true branch. The !prompt branch is safe Rust (CGEventTap::new is a safe API in core-graphics 0.24.0).
    3. The !prompt branch: add a local `use` for CGEventTap, CGEventTapLocation, CGEventTapOptions, CGEventTapPlacement, CGEventType from core_graphics::event. Call CGEventTap::new with CGEventTapLocation::HID, CGEventTapPlacement::HeadInsertEventTap, CGEventTapOptions::ListenOnly, vec![CGEventType::MouseMoved], and callback |_, _, event| Some(event.clone()). Return probe.is_ok().
    4. Add doc-comment above the !prompt return: "// @safety-officer: This probe tap must NOT set TAP_INITIALIZED or interact with TAP_STARTING. It is scoped to this function and dropped immediately. Only initialize_tap() in observer.rs sets those flags. AXIsProcessTrusted() is NOT used here — it caches its result per-process and returns stale false on macOS 15 Sequoia / 26 Tahoe after a grant."

    The resulting function signature is unchanged: pub fn check_accessibility_permissions(prompt: bool) -> bool.

    No changes to observer.rs, ipc/mod.rs, or any other file.

    Verify the compile with: cd src-tauri && cargo build 2>&1 | grep -E "^error" | head -20
    If the AXIsProcessTrusted extern removal causes a compile error (unused warning treated as error), remove only that declaration line from the extern "C" block. AXIsProcessTrustedWithOptions must remain.
  </action>
  <verify>
    <automated>cd /Users/alvaro/AutoClicker/src-tauri && cargo build 2>&1 | tail -5</automated>
    <automated>grep -n "CGEventTap::new" /Users/alvaro/AutoClicker/src-tauri/src/platform/macos/mod.rs</automated>
    <automated>grep -n "AXIsProcessTrusted() !=" /Users/alvaro/AutoClicker/src-tauri/src/platform/macos/mod.rs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - cargo build exits 0 with no errors (warnings about unused imports from other files are pre-existing and acceptable, but no NEW errors or warnings introduced by this change)
    - grep for "CGEventTap::new" in mod.rs returns at least 1 match
    - grep for "AXIsProcessTrusted() !=" in mod.rs returns 0 matches (the stale-cache call is gone)
    - grep for "TAP_INITIALIZED\|TAP_STARTING" in mod.rs returns 0 matches (not touched in this file)
  </acceptance_criteria>
  <done>check_accessibility_permissions(false) uses CGEventTap::new() as the live TCC probe. The stale AXIsProcessTrusted() call is removed from the !prompt path. The project compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create src-tauri/Info.plist with NSAccessibilityUsageDescription</name>
  <files>src-tauri/Info.plist</files>
  <read_first>
    - Run: ls /Users/alvaro/AutoClicker/src-tauri/Info.plist 2>/dev/null — confirm file does not exist before creating
    - .planning/phases/06-macos-tahoe-26-compatibility/06-RESEARCH.md Finding 8 — reason for this file
    - .planning/phases/06-macos-tahoe-26-compatibility/06-PATTERNS.md "src-tauri/Info.plist" section — exact content
  </read_first>
  <action>
    Create src-tauri/Info.plist as a new file (it does not currently exist in the repo). Place it at src-tauri/Info.plist (sibling of tauri.conf.json). Tauri CLI automatically merges developer-provided Info.plist files with the generated one at build time — no tauri.conf.json change is required.

    File content (exact):

    Plist version 1.0 XML with a single key: NSAccessibilityUsageDescription with value "AutoMux requires Accessibility access to monitor and inject keyboard and mouse events for macro automation."

    This causes macOS to display a human-readable description in System Settings → Privacy & Security → Accessibility. Without this key, the app may not appear in the Accessibility list on some Tahoe 26 configurations, preventing the user from granting permission.

    Do NOT add any other keys. Do NOT add com.apple.security.* entitlement keys — those require signing and have no effect in unsigned builds. This file contains only NSAccessibilityUsageDescription.
  </action>
  <verify>
    <automated>ls -la /Users/alvaro/AutoClicker/src-tauri/Info.plist</automated>
    <automated>grep -c "NSAccessibilityUsageDescription" /Users/alvaro/AutoClicker/src-tauri/Info.plist</automated>
    <automated>grep -c "com.apple.security" /Users/alvaro/AutoClicker/src-tauri/Info.plist</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/Info.plist exists
    - grep for "NSAccessibilityUsageDescription" returns count >= 1
    - grep for "com.apple.security" returns count == 0 (no entitlement keys present)
    - The file is valid XML (no parse errors if opened with plutil -lint src-tauri/Info.plist)
  </acceptance_criteria>
  <done>src-tauri/Info.plist exists with NSAccessibilityUsageDescription and no signing-related keys. Tauri will merge it into the app bundle at build time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS TCC → AutoMux | OS permission gate; CGEventTap::new() is the trust probe |
| CGEventTap callback → StateActor | Untrusted OS events enter the callback; existing LLMHF_INJECTED filtering in observer.rs handles self-injected event loop-back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | check_accessibility_permissions() | mitigate | Replace stale AXIsProcessTrusted() cache with live CGEventTap probe — trust only the live OS verdict |
| T-06-02 | Tampering | CGEventTap probe tap | mitigate | Probe tap is ListenOnly; dropped immediately; never added to a CFRunLoop; cannot inject events |
| T-06-03 | Denial of Service | TAP_INITIALIZED / TAP_STARTING guards | mitigate | Probe tap in mod.rs must NOT touch these flags — only initialize_tap() in observer.rs sets them; re-entrancy safety preserved per Phase 5 |
| T-06-SC | Tampering | npm/cargo installs | accept | No new packages installed in this phase; package legitimacy audit confirmed N/A in RESEARCH.md |
</threat_model>

<verification>
After both tasks complete:

1. cargo build exits 0 from src-tauri/ — no new errors or warnings
2. grep confirms CGEventTap::new is present in mod.rs
3. grep confirms AXIsProcessTrusted() stale-cache call is absent from mod.rs !prompt path
4. grep confirms TAP_INITIALIZED / TAP_STARTING are not referenced in mod.rs
5. src-tauri/Info.plist exists, contains NSAccessibilityUsageDescription, contains no entitlement keys
6. plutil -lint src-tauri/Info.plist exits 0 (valid plist XML)
</verification>

<success_criteria>
- check_accessibility_permissions(false) uses CGEventTap::new() as the live TCC probe on macOS 15 Sequoia and 26 Tahoe
- The probe tap is scoped to the !prompt branch, dropped immediately, and never interacts with TAP_INITIALIZED / TAP_STARTING
- The prompt=true branch (AXIsProcessTrustedWithOptions + CFDictionary) is unchanged
- src-tauri/Info.plist exists with NSAccessibilityUsageDescription only
- cargo build exits 0
- The fix is ready for manual Tahoe 26 validation in Plan 06-02
</success_criteria>

<output>
Create .planning/phases/06-macos-tahoe-26-compatibility/06-01-SUMMARY.md when done
</output>
