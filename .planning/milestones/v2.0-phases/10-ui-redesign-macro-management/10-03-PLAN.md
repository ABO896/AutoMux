---
phase: 10-ui-redesign-macro-management
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - src/App.tsx
  - src/components/Sidebar.tsx
  - src/components/ThemeToggle.tsx
  - src-tauri/tauri.conf.json
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04]
must_haves:
  truths:
    - "A persistent 84px left sidebar rail replaces the top tab bar; the Macros and Profiles nav items are keyboard-reachable via native tab order (real <button> elements) and show icon + text label — never icon-only (D-08, UI-02)"
    - "The active nav item shows the accent pill (bg-accent/10 + text-accent); inactive items are text-text-muted with hover:text-text-main"
    - "The theme toggle sits at the sidebar footer and cycles System → Light → Dark → System, showing 🖥/☀/🌙, with an aria-label naming the action it will perform next (D-06)"
    - "The window opens at 720×680 and enforces a 560×520 minimum, remaining resizable; transparent stays false — CSS-only blur, no native window vibrancy (D-04, D-09)"
    - "The Profiles destination shows 'No saved profiles yet.' / 'Save your current configuration above.' when empty (carried forward, restyled)"
    - statement: "Idle CPU/GPU with all blur surfaces active (translucent cards + .sidebar-glass) is not measurably higher than the v1.2.0 baseline; if it rises measurably, blur is reduced to the sidebar + topmost banner only per the UI-SPEC fallback (measured EARLY here so the fallback can be applied before 10-04/10-05 build more surfaces)"
      verification: backstop
  artifacts:
    - "src/components/Sidebar.tsx — 84px nav rail (Macros/Profiles)"
    - "src/components/ThemeToggle.tsx — 3-state cycle control"
    - "src-tauri/tauri.conf.json — window 720×680 / min 560×520, transparent false"
  key_links:
    - "Sidebar receives activeTab accessor + onSelectTab callback from App(); the activeTab signal stays owned by App() (signals-down, no per-child local nav state)"
    - "ThemeToggle receives themePreference accessor + onCycle callback; applyTheme (theme.ts) is the single source of truth for the data-theme swap"
---

<objective>
Restructure navigation from the top tab bar to a persistent left sidebar rail (D-08) with the theme toggle anchored at its foot, widen the window to Raycast-leaning proportions (D-09), and run the EARLY UI-04 perf gate now that all blur surfaces (cards + sidebar) exist — so the documented fallback (reduce blur layer count) can be applied before plans 10-04/10-05 build the edit/delete surfaces on top.

Purpose: deliver UI-02's "Raycast-inspired, keyboard-navigable, efficient use of space" hierarchy and the D-06/D-07 theme control surface, while pinning UI-04 with a real on-device measurement at the point of maximum blur-surface count.

Output: `Sidebar.tsx` + `ThemeToggle.tsx` components wired into `App.tsx` (signals stay in `App()`), the resized window, and a human-verified perf/visual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-redesign-macro-management/10-CONTEXT.md
@.planning/phases/10-ui-redesign-macro-management/10-RESEARCH.md
@.planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md
@.planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md
</context>

## Artifacts this phase produces (introduced by this plan)

- `src/components/Sidebar.tsx` — new `Component<SidebarProps>` (props: `activeTab: () => Tab`, `onSelectTab: (tab: Tab) => void`, plus the theme-toggle props threaded through or a `children`/slot for ThemeToggle). 84px rail, Macros/Profiles nav items.
- `src/components/ThemeToggle.tsx` — new `Component<ThemeToggleProps>` (props: `preference: () => ThemePreference`, `onCycle: () => void`). 3-state cycle glyph + `aria-label`.
- `src-tauri/tauri.conf.json` — window dimensions updated to 720×680 / min 560×520 (transparent stays false).
- App.tsx: `cycleThemePreference` handler; the top tab-bar markup replaced by `<Sidebar .../>` in a `flex` row layout; `setThemePreference` now consumed by the toggle.

<tasks>

<task type="auto">
  <name>Task 1: Extract Sidebar.tsx and replace the top tab bar</name>
  <files>src/components/Sidebar.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (READ the current tab-bar block 733-755 — the `Dashboard`/`Profiles` buttons wired to `activeTab()`/`setActiveTab`; the `Show when={activeTab() === ...}` content blocks at 756+; the `Tab` type alias; import lines 1-6 for the relative-import convention)
    - .planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md (Window & Chrome → Navigation restructure; Component Inventory C-SB1 with EXACT container classes `w-[84px] shrink-0 flex flex-col items-center py-4 gap-1 border-r border-border` + `.sidebar-glass`, nav-item classes, active/inactive/hover states; Layout Contract diagram)
    - .planning/phases/10-ui-redesign-macro-management/10-RESEARCH.md (Pattern 1: signals-down, accessor-props, NO destructuring)
    - .planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md (Sidebar.tsx section: analog App.tsx:733-755, SidebarProps shape)
  </read_first>
  <action>
Create `src/components/Sidebar.tsx` as `Component<SidebarProps>` where `SidebarProps` = `{ activeTab: () => Tab; onSelectTab: (tab: Tab) => void }` plus whatever prop shape you choose to mount the ThemeToggle at the foot (either pass `themePreference`/`onCycleTheme` through, or accept ThemeToggle as `children` — pick one and keep it consistent). Read props via `props.activeTab()` inline — NEVER destructure props (breaks SolidJS reactivity per RESEARCH Pitfall 5). Container uses the exact C-SB1 classes plus the `.sidebar-glass` class from 10-02. Two nav items as real `<button>` elements (native tab order): `⚡ Macros` (maps to the current `dashboard` tab) and `📁 Profiles`, each `flex flex-col items-center gap-1 px-2 py-3 rounded-lg cursor-pointer transition-colors w-16`, icon `text-base` (16px), label at Label role (11px / font-semibold). Active item: `bg-accent/10 text-accent`. Inactive: `text-text-muted hover:text-text-main`. A `flex-grow` spacer sits between the nav items and the footer theme toggle.

In `App.tsx`: keep the `activeTab`/`setActiveTab` signal in `App()` (do NOT move it into Sidebar). Replace the top tab-bar markup (733-755) with a `flex` row: `<Sidebar activeTab={activeTab} onSelectTab={setActiveTab} ... />` as the first child and the existing content column (the `Show when={activeTab() === ...}` blocks, unchanged internally) as the second. The `Dashboard` tab value maps to the `Macros` nav item — keep the underlying `Tab` union value (`"dashboard"`) so the existing `Show` conditions keep working; only the visible label/icon changes. Preserve the drag-region titlebar above the flex row.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10 && grep -c 'const { ' src/components/Sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Sidebar.tsx` exists and default-exports a `Component`.
    - `grep -c 'const { ' src/components/Sidebar.tsx` returns 0 (no prop destructuring).
    - Sidebar renders two real `<button>` elements (`grep -c '<button' src/components/Sidebar.tsx` returns >= 2).
    - `grep -c 'w-\[84px\]' src/components/Sidebar.tsx` returns 1.
    - `App.tsx` no longer contains the old horizontal tab-bar row; `grep -c 'from "./components/Sidebar"' src/App.tsx` returns 1.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The top tab bar is gone; an 84px keyboard-navigable sidebar rail with Macros/Profiles nav items renders, driven by the App()-owned activeTab signal.</done>
</task>

<task type="auto">
  <name>Task 2: ThemeToggle.tsx (3-state cycle) at the sidebar footer + window resize</name>
  <files>src/components/ThemeToggle.tsx, src/App.tsx, src-tauri/tauri.conf.json</files>
  <read_first>
    - src/App.tsx (READ the themePreference signal + matchMedia effect added in 10-02; the `.toggle-track` click-handler pattern at 1293-1303 for the click→mutate→apply shape)
    - src/theme.ts (applyTheme / ThemePreference — the toggle mutates preference through here)
    - src-tauri/tauri.conf.json (READ the `app.windows[0]` block: width 420, height 640, minWidth 360, minHeight 480, transparent false)
    - .planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md (Component Inventory C-SB2 with EXACT classes; Copywriting → theme toggle tooltip; Accessibility → the aria-label-naming-next-action requirement; Window & Chrome table for the new dimensions)
    - .planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md (ThemeToggle.tsx section)
  </read_first>
  <action>
Create `src/components/ThemeToggle.tsx` as `Component<ThemeToggleProps>` = `{ preference: () => ThemePreference; onCycle: () => void }` (read via `props.preference()` inline, no destructuring). Render a single control using the exact C-SB2 classes `flex flex-col items-center gap-1 px-2 py-3 rounded-lg cursor-pointer text-text-muted hover:text-text-main transition-colors w-16` (NO accent tint — it is a utility control per the "Accent is NOT used for" rule). Show the glyph for the current preference: `🖥` System, `☀` Light, `🌙` Dark. `onClick` calls `props.onCycle()`. Add an `aria-label` that names the NEXT action (e.g. when current is System, the label describes switching to Light) — a `title` tooltip alone is insufficient for a control whose meaning changes across the 3-state cycle.

In `App.tsx`: add a `cycleThemePreference` handler that advances `system → light → dark → system`, calls `applyTheme(next)`, and `setThemePreference(next)` (this consumes the setter deferred from 10-02). Mount `<ThemeToggle preference={themePreference} onCycle={cycleThemePreference} />` at the sidebar footer (via the prop/children path chosen in Task 1).

In `src-tauri/tauri.conf.json`: set the main window `width: 720`, `height: 680`, `minWidth: 560`, `minHeight: 520`. Leave `resizable: true`, `decorations: true`, and `transparent: false` UNCHANGED (D-04 — CSS-only blur, no native vibrancy).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && node -e "const c=require('./src-tauri/tauri.conf.json');const w=c.app.windows[0];console.log(w.width,w.height,w.minWidth,w.minHeight,w.transparent)"</automated>
  </verify>
  <acceptance_criteria>
    - Window config prints `720 680 560 520 false`.
    - `src/components/ThemeToggle.tsx` exists; `grep -c 'aria-label' src/components/ThemeToggle.tsx` returns >= 1.
    - `grep -c 'const { ' src/components/ThemeToggle.tsx` returns 0 (no destructuring).
    - `App.tsx` cycle handler advances through all three states (`grep -c 'setThemePreference' src/App.tsx` returns >= 1 and the setter is consumed — tsc noUnusedLocals passes).
    - `npx tsc --noEmit` clean.
    - Manual: clicking the toggle cycles System → Light → Dark → System; data-theme updates instantly with no reload; preference survives restart.
  </acceptance_criteria>
  <done>A 3-state theme toggle with a next-action aria-label sits at the sidebar footer and drives applyTheme; the window opens at 720×680 (min 560×520), transparent still false.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Early UI-04 perf gate + both-theme visual spot-check</name>
  <what-built>The full blur-surface set now exists: translucent .glass-card on every macro card (10-01/10-02) plus the .sidebar-glass rail (this plan). This is the point of maximum backdrop-filter layer count before the edit/delete surfaces are added. UI-04 requires no measurable idle overhead vs the v1.2.0 baseline; RESEARCH mandates measuring EARLY so the documented fallback (reduce blur layer count to sidebar + topmost banner only) can still be applied before plans 10-04/10-05 build on top.</what-built>
  <how-to-verify>
1. Build/run the current app (`npm run tauri dev` or a release build) on your macOS Tahoe device. Open Activity Monitor → % CPU for the AutoMux process, app idle (no macro running, no window movement), with several macro cards visible. Note idle CPU/GPU.
2. If available, run a v1.2.0 build (or the pre-Phase-10 commit) the same way and note its idle CPU/GPU as the baseline.
3. Compare. If idle CPU/GPU is NOT measurably higher, approve.
4. If it IS measurably higher: reply with the numbers and instruct applying the UI-SPEC fallback — keep backdrop-filter only on `.sidebar-glass` and the topmost banner, and drop per-card blur (cards fall back to an opaque/near-opaque `--color-surface` background) — then re-measure.
5. Also spot-check visually: in BOTH light and dark themes, every card, banner, and form field is styled (no dark-only element in light mode); content behind a card/sidebar visibly blurs; the window respects the 560×520 minimum and stays resizable; both sidebar nav items are reachable by pressing Tab.
  </how-to-verify>
  <resume-signal>Type "approved" if idle overhead is not measurably higher and both themes render cleanly; otherwise describe the measured delta and which surfaces to de-blur.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app | Theme preference read/written by the toggle |
| tauri.conf.json → OS window manager | Declarative window dimensions consumed by Tauri at launch |

No new IPC/backend boundary is opened; the only Rust-side change is a static config value edit.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Denial of Service | excessive backdrop-filter compositing (many blur layers) degrading idle responsiveness | low | mitigate | The UI-04 human-verify checkpoint measures idle CPU/GPU and applies the documented blur-layer-reduction fallback if a measurable regression appears |
| T-10-03 | Tampering | tampered theme preference value (reused from 10-02) | low | mitigate | `getStoredPreference` validation guard (from theme.ts) still governs; the toggle only writes the 3 valid enum values |
| T-10-SC | Tampering | npm/cargo package installs | low | accept | N/A — zero new dependencies (no install tasks) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Window config asserts `720 680 560 520 false`.
- No prop destructuring in the two new components.
- Human-verify checkpoint: idle-overhead perf gate (with fallback path) + both-theme visual spot-check + keyboard-nav reachability passed.
</verification>

<success_criteria>
The sidebar-based Raycast-inspired navigation and theme control ship at the correct window size, and UI-04 is pinned early with an on-device measurement (and fallback if needed) before more blur surfaces are built.
</success_criteria>

<output>
Create `.planning/phases/10-ui-redesign-macro-management/10-03-SUMMARY.md` when done.
</output>
