---
phase: 10-ui-redesign-macro-management
plan: 04
type: execute
wave: 4
depends_on: [10-03]
files_modified:
  - src/App.tsx
  - src/components/KeyCaptureField.tsx
  - src/components/MacroForm.tsx
autonomous: true
requirements: [UX-10, UX-09, UI-02]
must_haves:
  truths:
    - "The Input selector presents exactly four labeled options — Left Click, Right Click, Middle Click, Key Press — with no unlabeled or icon-only choice (UX-10, D-11)"
    - "The Mode selector presents exactly two labeled options — Pulse (Repeat), Hold (Sustained) (UX-10, D-10)"
    - "Selecting Key Press reveals the reused Phase 8 key-capture widget (modifier chips, keydown capture, Escape-to-cancel) in the input slot — no new key-picker component is built (D-12)"
    - "Selecting Key Press with Mode = Hold (Sustained) hides the interval-ms field (a held key does not repeat, matching the existing rule for mouse buttons)"
    - "Input options render in the fixed order Left Click, Right Click, Middle Click, Key Press; Mode options in the fixed order Pulse (Repeat), Hold (Sustained) — deterministic, not data-driven"
    - "Both selectors are fixed enumerations — neither can be empty or null (no dynamic/async population)"
    - "An option's selection maps to the backend InputEvent by fixed identity (mouse-button string vs Key keycode); the label text is decorative and does not change the persisted value"
    - "MacroForm is the single shared field set used by BOTH the New Macro creation panel and (in 10-05) the inline card-edit form (D-13); it builds the same ActionSequence shape (SustainedHold vs InterleavedInterval) as the current handleCreateMacro"
    - "The target-app dropdown keeps its loading state (a disabled 'Loading…' option) and its fetch-failure state ('Failed to load apps') carried forward unchanged"
    - "Key-capture modifiers render as discrete chips (one per key), not a single concatenated string; each chip's text stays within the Label typography role"
  artifacts:
    - "src/components/KeyCaptureField.tsx — wraps startCapture + modifier-chip rendering, reused for hotkey trigger-key AND Key Press action input"
    - "src/components/MacroForm.tsx — shared create/edit field set with the two labeled selectors"
  key_links:
    - "MacroForm submit builds ActionSequence identically to handleCreateMacro (App.tsx:460-481)"
    - "KeyCaptureField preserves the single-listener invariant (_keyCaptureListener module-level ref, T-03-08)"
    - "Key Press input commits into the ActionStep Key field via a distinct onCommit, separate from the trigger-key capture instance"
---

<objective>
Extract the shared macro form into `MacroForm.tsx` and the reusable key-capture widget into `KeyCaptureField.tsx`, and in doing so deliver UX-10 (unambiguous two-selector action-type labels) and D-11/D-12 (Key Press becomes a genuinely selectable input that reuses the Phase 8 capture widget). This makes D-13's "edit reuses the same fields as create" concretely enforceable — one form component, two mount sites — rather than aspirational.

Purpose: UX-10's literal label requirement plus the net-new Key Press input, built on the truthful two-selector model (D-10: Input × Mode, because Hold is a mode that crosses any input, not a peer item). The create path is wired through MacroForm now; the edit path mounts the same component in 10-05.

Output: `KeyCaptureField.tsx` + `MacroForm.tsx`, with the New Macro creation form re-expressed through MacroForm and the Input selector exposing all four labeled options.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-redesign-macro-management/10-CONTEXT.md
@.planning/phases/10-ui-redesign-macro-management/10-RESEARCH.md
@.planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md
@.planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md
</context>

## Artifacts this phase produces (introduced by this plan)

- `src/components/KeyCaptureField.tsx` — new `Component<KeyCaptureFieldProps>` wrapping `startCapture()` + the modifier-chip + capture-chip rendering; parameterized by an `onCommit(nativeCode, modifiers)` callback and the recording/label accessors, so both the hotkey trigger-key slot and the new Key Press action slot mount it.
- `src/components/MacroForm.tsx` — new `Component<MacroFormProps>` holding the full field set: name input, Input `<select>` (4 labeled options incl. Key Press), Mode `<select>` (2 labeled options), interval-ms field (hidden when Mode = Hold), key-capture slot when Input = Key Press, target-app `<select>` (with loading + failure states), and the submit CTA.
- App.tsx: the New Macro creation panel re-expressed as `<MacroForm .../>`; the `newMacroInput` option set extended with `Key Press`; a signal/state slot for the Key Press action keycode + modifiers distinct from the trigger-key capture state.

<tasks>

<task type="auto">
  <name>Task 1: Extract KeyCaptureField.tsx (reused for hotkey + Key Press capture)</name>
  <files>src/components/KeyCaptureField.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (READ `startCapture` at 532-567; the `_keyCaptureListener` module-level ref at 198 and its single-listener invariant comment at ~528 `@architect: Single-listener invariant`; `computeModifiers` at 144; `modifierChips` at 172; the capture-chip + modifier-chip render block at 1160-1219; the trigger-key recording signals `recordingModifiers` 267 / `triggerKeyRecording`; the cancel-on-open guard at 1186-1190)
    - src/keymap.ts (READ `domKeycodeToNative` / `resolveKeyName` — used inside capture)
    - .planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md (Action-Type Model → "Selecting Key Press"; Component Inventory C-E1 note about two independent key-capture instances coexisting on one card; Typography note that chip text is 11px semibold, NOT the old 10px/font-mono)
    - .planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md (KeyCaptureField.tsx section: startCapture copy + single-listener-invariant preservation note)
  </read_first>
  <action>
Create `src/components/KeyCaptureField.tsx` wrapping the existing `startCapture` flow and the capture-chip/modifier-chip rendering, parameterized so it serves BOTH the hotkey trigger-key slot and the new Key Press action-input slot. Props (accessor-style, read inline, no destructuring): the current committed key label/code accessor, a `recording` accessor, a `recordingModifiers` accessor, and an `onStartCapture` callback that invokes the App-owned `startCapture(onCommit)` with the caller's `onCommit`. Keep `startCapture` and the `_keyCaptureListener` module-level ref in `App.tsx` (or a shared module) — do NOT give the component its own listener ref; the single-listener invariant (T-03-08) means only ONE capture may record at a time. Preserve the existing cancel-on-open behavior (opening a capture cancels any other in-progress capture). When rendering modifier chips, apply the UI-SPEC Label typography: 11px semibold chips (do NOT copy the old 10px/font-mono-only chip styling verbatim), and modifier micro-padding `px-2 py-1`.

In `App.tsx`, replace the two inline capture-chip render sites (trigger-key binding, and any place the widget appears) with `<KeyCaptureField ... />`, passing the appropriate accessors/callbacks. The trigger-key binding behavior must remain byte-for-byte equivalent (conflict surfacing, Escape-to-cancel, modifier bits).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10 && grep -c 'const { ' src/components/KeyCaptureField.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/KeyCaptureField.tsx` exists and default-exports a `Component`.
    - `grep -c 'const { ' src/components/KeyCaptureField.tsx` returns 0 (no prop destructuring).
    - The single-listener ref `_keyCaptureListener` is NOT redeclared inside KeyCaptureField (`grep -c '_keyCaptureListener' src/components/KeyCaptureField.tsx` returns 0) — it stays App/module-owned.
    - Chip text uses the 11px Label size, not `text-[10px]` (`grep -c 'text-\[10px\]' src/components/KeyCaptureField.tsx` returns 0).
    - `npx tsc --noEmit` clean; the existing hotkey trigger-key capture still works (manual: bind a hotkey, modifier chips + Escape-to-cancel behave as before).
  </acceptance_criteria>
  <done>The key-capture widget is a reusable component that preserves the single-listener invariant and the Label typography; the existing hotkey binding path is unchanged in behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Extract MacroForm.tsx with two labeled selectors + Key Press input</name>
  <files>src/components/MacroForm.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (READ the New Macro form block 1079-1233 incl. the Input `<select>` at 1106-1113 currently offering only Left/Right/Middle; the Mode selector; the interval-ms field; the target-app `<select>` with its `Loading…`/`Failed to load apps` options; `handleCreateMacro` 453-509 and the ActionSequence build at 460-481; the `disabled={!newMacroName().trim()}` guard ~1225; the `newMacroInput` 256 / `newMacroTriggerMode` 258 signals; `InputEvent`/`ActionSequence`/`TriggerMode` types 32-118)
    - src/components/KeyCaptureField.tsx (the component from Task 1 — mounted in the Key Press slot)
    - .planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md (Action-Type Model table with EXACT labels; Copywriting Contract → action-type Input/Mode labels; Interaction Contract UX-10 rows 1-3; UI Considerations rows for target-app loading/error and long-text modifier chips)
    - .planning/phases/10-ui-redesign-macro-management/10-RESEARCH.md (Code Examples → buildActionSequence; Open Question #2 resolution: client-only name guard, no new backend validation)
    - .planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md (MacroForm.tsx section: controlled-input + build-ActionSequence patterns)
  </read_first>
  <action>
Create `src/components/MacroForm.tsx` as the shared field set for both create and (in 10-05) edit. Fields, in order: name `<input>` (controlled); Input `<select>` with EXACTLY four options in fixed order — `Left Click`, `Right Click`, `Middle Click`, `Key Press` — with plain text labels (glyphs optional per the icon convention, but the text label must be present and unambiguous; no icon-only option); Mode `<select>` with EXACTLY two options in fixed order — `Pulse (Repeat)`, `Hold (Sustained)` (relabeled from the old `Pulse (Interval)` / `Hold (Latched)`); the interval-ms numeric field, HIDDEN when Mode = `Hold (Sustained)`; the target-app `<select>` carrying its existing `Loading…` (disabled) and `Failed to load apps` states unchanged. When Input = `Key Press`, render `<KeyCaptureField>` in the slot the mouse-button choice occupies, committing the captured keycode+modifiers into a Key-Press action-keycode state that is DISTINCT from the trigger-key capture state (two independent instances may coexist on a card per C-E1). The submit CTA and its `disabled={!name().trim()}` client guard (mirror the existing precedent — no new backend name validation) come from props so the create panel labels it `Create Macro` and the edit form (10-05) labels it `Save Changes`.

The form must expose a submit handler prop that receives the assembled values; MacroForm itself builds the `ActionSequence` using the SAME shape as `handleCreateMacro` (mode `Hold` → `{ steps: [{ SustainedHold: { input } }] }`, else `{ steps: [{ InterleavedInterval: { input, interval_ms } }] }`), and maps the Input choice to an `InputEvent` by fixed identity (`Left`/`Right`/`Middle` → `{ MouseButton }`, `Key Press` → `{ Key: keycode }`).

In `App.tsx`, re-express the New Macro creation panel as `<MacroForm .../>` bound to the existing `newMacro*` signals, so creation goes through the shared component. `handleCreateMacro` continues to call `add_macro` — only the form markup is now the shared component. Add `Key Press` as a genuinely selectable Input in creation (D-11).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10 && grep -c 'Key Press' src/components/MacroForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The Input selector in MacroForm renders exactly 4 options and includes `Left Click`, `Right Click`, `Middle Click`, `Key Press` (`grep -c 'Key Press' src/components/MacroForm.tsx` returns >= 1; `grep -c 'Middle Click' src/components/MacroForm.tsx` returns 1).
    - The Mode selector renders exactly 2 options `Pulse (Repeat)` and `Hold (Sustained)` (`grep -c 'Pulse (Repeat)' src/components/MacroForm.tsx` returns 1; `grep -c 'Hold (Sustained)' src/components/MacroForm.tsx` returns 1).
    - The old labels are gone: `grep -c 'Latched' src/App.tsx src/components/MacroForm.tsx` returns 0.
    - `grep -c 'const { ' src/components/MacroForm.tsx` returns 0 (no prop destructuring).
    - The interval field is conditionally hidden when Mode = Hold (a `Show`/conditional keyed on the Mode value exists).
    - `npx tsc --noEmit` clean.
    - Manual: in the create form, selecting `Key Press` shows the key-capture widget in the input slot; selecting `Hold (Sustained)` hides the interval field; creating a Key Press macro produces a working macro.
  </acceptance_criteria>
  <done>MacroForm is the shared field set with two unambiguously labeled selectors and a selectable Key Press input that reuses KeyCaptureField; the create path runs through it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView (SolidJS) → Tauri IPC | Form input becomes an `add_macro` (create) call; edit path (10-05) uses `update_macro` |
| Keyboard capture → app state | `startCapture` reads raw key events to build the action keycode + modifiers |

This plan opens no new IPC command; it reuses `add_macro` and the existing capture path.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Tampering | two coexisting key-capture instances (trigger-key + Key Press) racing the shared single listener, committing to the wrong slot | medium | mitigate | Preserve the single-listener invariant (`_keyCaptureListener`, T-03-08) and the cancel-on-open guard so only one capture records at a time; each instance passes its own `onCommit` so a commit lands in the correct slot |
| T-10-07 | Denial of Service (minor) | empty/whitespace macro name submitted via the form | low | accept | Client `disabled={!name().trim()}` guard mirrors existing precedent; identical exposure already exists via the create form today |
| T-10-SC | Tampering | npm/cargo package installs | low | accept | N/A — zero new dependencies (no install tasks) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Input selector = exactly 4 labeled options incl. Key Press; Mode selector = exactly 2 labeled options; no `Latched` label remains.
- No prop destructuring in either new component; single-listener invariant not redeclared in KeyCaptureField.
- Manual: create a Key Press macro; Hold hides interval; existing hotkey binding unaffected.
</verification>

<success_criteria>
The action-type UI is unambiguous and truthful to the backend model, Key Press is selectable and reuses the Phase 8 capture widget, and the shared MacroForm/KeyCaptureField components are ready for the inline-edit mount in 10-05.
</success_criteria>

<output>
Create `.planning/phases/10-ui-redesign-macro-management/10-04-SUMMARY.md` when done.
</output>
