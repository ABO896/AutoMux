---
phase: 10-ui-redesign-macro-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/state/mod.rs
  - src-tauri/src/ipc/mod.rs
  - src-tauri/src/lib.rs
  - src/App.tsx
  - src/App.css
autonomous: true
requirements: [UX-09, UI-01]
must_haves:
  truths:
    - "invoke('update_macro', {...}) updates a macro's name and persists it — the new name survives an app restart (auto_save_default writes default.json on the Ok path)"
    - "Editing a macro never destroys or reassigns another macro's hotkey — a trigger-key conflict is rejected atomically via resolve_trigger_key_update with NO field mutated"
    - "A macro card can be edited inline (name field) end-to-end: ✎ opens an in-card name editor, Save calls update_macro, and on success the card collapses showing the new name"
    - ".glass-card renders with genuine translucency (translucent surface background + real backdrop-filter blur) so content behind a card is visibly blurred, not a flat opaque gradient (D-03)"
  artifacts:
    - "src-tauri/src/state/mod.rs — Intent::UpdateMacro variant + handler"
    - "src-tauri/src/ipc/mod.rs — update_macro #[command]"
    - "src-tauri/src/lib.rs — update_macro registered in generate_handler!"
    - "src/App.css — translucent .glass-card (interim, finalized in 10-02)"
  key_links:
    - "update_macro IPC → Intent::UpdateMacro → StateActor mutation → reevaluate_all_macros + recompute_conflicts + auto_save_default"
    - "resolve_trigger_key_update reused (not reimplemented) for the edit-path conflict check"
---

<objective>
Deliver the phase's end-to-end tracer slice: prove the full macro-edit path (frontend inline editor → new `update_macro` IPC → new `Intent::UpdateMacro` → StateActor mutation → persistence) works for ONE field (name) on ONE re-skinned card, and stand up the translucency foundation (`D-03`) on `.glass-card`. This is the thinnest real path through every layer this phase touches (Rust state, Rust IPC, handler registration, SolidJS frontend, CSS) — production-quality, built for keeps, expanded (not replaced) by later plans.

Purpose: catch any architectural dead-end (IPC shape, Intent atomicity, persistence, translucency perf) after one commit instead of after the full redesign is built on top. UX-09's backend is net-new (`name` and `trigger_mode` have no update path today — they are `AddMacro`-only fields), so proving it first de-risks every subsequent frontend plan.

Output: `Intent::UpdateMacro` + `update_macro` IPC (registered), 3 backend unit tests, and a working inline name-edit affordance on the macro card over a genuinely translucent `.glass-card`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-redesign-macro-management/10-CONTEXT.md
@.planning/phases/10-ui-redesign-macro-management/10-RESEARCH.md
@.planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md
@.planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md
</context>

## Artifacts this phase produces (introduced by this plan)

- `Intent::UpdateMacro(Uuid, String, ActionSequence, TriggerMode, Option<String>, Option<u16>, Option<u64>, oneshot::Sender<Result<(), String>>)` — new enum variant in `src-tauri/src/state/mod.rs`.
- `update_macro` — new `#[command(rename_all = "snake_case")]` in `src-tauri/src/ipc/mod.rs`, registered in `src-tauri/src/lib.rs` `generate_handler!`.
- Backend unit tests `update_macro_applies_all_fields`, `update_macro_conflict_no_partial_mutation`, and an `update_macro` persistence round-trip test (inline `#[cfg(test)] mod tests` in `state/mod.rs`).
- Interim translucent `.glass-card` treatment in `src/App.css` (background via `color-mix`, real `backdrop-filter`) — finalized/extended by plan 10-02.
- Frontend inline name-edit affordance (`✎` button + `editingCardId`-gated name input + `update_macro` invoke) in `src/App.tsx` — generalized to the full field set by plan 10-05.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Backend Intent::UpdateMacro + update_macro IPC + handler registration</name>
  <files>src-tauri/src/state/mod.rs, src-tauri/src/ipc/mod.rs, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/state/mod.rs (READ the Intent enum at 152-200, the `Intent::SetMacroTriggerKey` handler at 594-635, `resolve_trigger_key_update` at 226-246, `check_trigger_key_conflict` at 247+, `recompute_conflicts` at 320, and the `MacroConfig` struct fields at 93-118 — note `trigger_modifiers` is `u64`, NOT `Option<u64>`)
    - src-tauri/src/ipc/mod.rs (READ `set_macro_trigger_key` at 148-160 and `bind_hotkey` at 80-93 — both `#[command(rename_all = "snake_case")]`, oneshot + double-unwrap Result pattern; READ the `use crate::state::{...}` import line at the top)
    - src-tauri/src/lib.rs (READ `generate_handler!` block at 83-110 — the exact list of `ipc::*` entries)
    - .planning/phases/10-ui-redesign-macro-management/10-RESEARCH.md (Code Examples section: the full drafted `Intent::UpdateMacro`, its handler, and the `update_macro` command — copy these, they are already conventions-correct)
    - .planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md (`src-tauri/src/state/mod.rs` and `src-tauri/src/ipc/mod.rs` pattern sections)
  </read_first>
  <action>
Add `Intent::UpdateMacro` to the `Intent` enum in `state/mod.rs`, mirroring `SetMacroTriggerKey`'s Result-carrying-oneshot shape (D-13's backend surface for UX-09). The variant carries, in order: `Uuid`, `String` (name), `ActionSequence` (sequence), `TriggerMode`, `Option<String>` (target_app), `Option<u16>` (trigger_key), `Option<u64>` (trigger_modifiers), `tokio::sync::oneshot::Sender<Result<(), String>>`.

Add the matching handler arm inside the intent `match`. It MUST call `resolve_trigger_key_update(&self.state, id, trigger_key, trigger_modifiers)` (reuse AS-IS — do NOT reimplement the conflict check; RESEARCH's Don't-Hand-Roll table flags duplication as a drift risk). On the `Err(conflicting_id)` branch: build the same conflict message string `SetMacroTriggerKey` produces and reply `Err(msg)` with NO field mutated (atomic reject). On the `Ok((new_key, new_mods))` branch: mutate `mac.name`, `mac.sequence`, `mac.trigger_mode`, `mac.target_app`, `mac.trigger_key = new_key`, `mac.trigger_modifiers = new_mods` for the macro at `id`, then call `self.reevaluate_all_macros().await`, `self.recompute_conflicts()`, `self.auto_save_default().await`, and reply `Ok(())`. Match the exact Option→field mapping of the existing `SetMacroTriggerKey` handler (the `new_mods` binding resolves to the `u64` field; the IPC param is `Option<u64>`).

Add the `update_macro` command to `ipc/mod.rs` mirroring `bind_hotkey` (`#[command(rename_all = "snake_case")]`, async, `Result<(), String>`, oneshot channel, `.map_err` on send then the inner `Result` returned). Params: `state: State<'_, StateManager>`, `id: Uuid`, `name: String`, `sequence: ActionSequence`, `trigger_mode: crate::state::TriggerMode`, `target_app: Option<String>`, `trigger_key: Option<u16>`, `trigger_modifiers: Option<u64>`. Ensure `ActionSequence` and `TriggerMode` are in the `use crate::state::{...}` import.

Register `ipc::update_macro` in `lib.rs`'s `generate_handler!` list alongside `ipc::set_macro_trigger_key`.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` (in `src-tauri`) exits 0 with no errors.
    - `grep -c 'UpdateMacro' src-tauri/src/state/mod.rs` returns >= 2 (enum variant + handler arm).
    - `grep -c 'pub async fn update_macro' src-tauri/src/ipc/mod.rs` returns 1.
    - `grep -c 'ipc::update_macro' src-tauri/src/lib.rs` returns 1.
    - The handler's Err branch contains no `mac.name =` / `mac.sequence =` mutation (atomic reject) — the only field mutations sit inside the `Ok(...)` branch.
    - `resolve_trigger_key_update` is called in the UpdateMacro handler (conflict check reused, not reimplemented).
  </acceptance_criteria>
  <done>Intent::UpdateMacro, update_macro IPC, and its handler registration compile; the conflict check reuses resolve_trigger_key_update and mutates fields only on the Ok path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend unit tests — atomic apply, conflict-no-mutation, persistence round-trip</name>
  <files>src-tauri/src/state/mod.rs</files>
  <read_first>
    - src-tauri/src/state/mod.rs (READ the inline `#[cfg(test)] mod tests` block — locate existing tests `hold_release_bypasses_gates` and `set_trigger_key_rejects_conflict_without_coercion` near 1038-1076 and the `profile_backwards_compat`-style persistence test; copy their construction/assertion style — free-function tests, no Tauri AppHandle)
    - src-tauri/src/persistence.rs (READ the `ProfileManager` save/load API used by any round-trip test, and the existing `profile_backwards_compat` test if present there)
    - .planning/phases/10-ui-redesign-macro-management/10-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map: the 3 named tests and their intent)
  </read_first>
  <behavior>
    - update_macro_applies_all_fields: given a macro added with an initial name/sequence/mode, applying an UpdateMacro-equivalent mutation updates name, sequence, trigger_mode, target_app, and trigger_key together; all five reflect the new values.
    - update_macro_conflict_no_partial_mutation: given macro A bound to key K and macro B, attempting to update B to also use key K is rejected (resolve_trigger_key_update Err) and B's name/sequence/mode/target/key are all UNCHANGED (atomicity — no partial apply).
    - update_macro persistence round-trip: a macro edited via the update path, saved through ProfileManager and reloaded, deserializes with the edited values intact (mirrors the profile_backwards_compat round-trip style).
  </behavior>
  <action>
Add three tests to the existing inline `#[cfg(test)] mod tests` module in `state/mod.rs` (same file, same style — do NOT create a separate test file; that breaks the established convention). Follow the existing free-function test approach used by `set_trigger_key_rejects_conflict_without_coercion` (call the state-mutation logic / `resolve_trigger_key_update` directly against an `AppState`, no `StateActor::new` / AppHandle needed). Name them `update_macro_applies_all_fields`, `update_macro_conflict_no_partial_mutation`, and an `update_macro`-scoped persistence round-trip test consistent with the existing `profile_backwards_compat` naming. The conflict test asserts every mutable field on the rejected macro equals its pre-attempt value.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test update_macro 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test update_macro` runs and reports the 3 new tests passing (0 failed).
    - `grep -c 'fn update_macro_applies_all_fields' src-tauri/src/state/mod.rs` returns 1.
    - `grep -c 'fn update_macro_conflict_no_partial_mutation' src-tauri/src/state/mod.rs` returns 1.
    - The conflict test asserts the rejected macro's name AND trigger_key are unchanged after the rejected update.
  </acceptance_criteria>
  <done>Three update_macro backend tests pass: atomic all-field apply, conflict-rejection-without-mutation, and a persistence round-trip.</done>
</task>

<task type="tracer">
  <name>Task 3: Frontend inline name-edit slice + translucent .glass-card</name>
  <files>src/App.tsx, src/App.css</files>
  <read_first>
    - src/App.tsx (READ the signal block 206-280 incl. `editingCardId`/`editingField` at 279-280; `handleCreateMacro` 453-509 for the invoke pattern; `handleRemoveMacro` 519-526; the macro-card `<For>` render block and its header row; the `showConflictError` helper at 241-248; the `MacroConfig`/`InputEvent`/`ActionSequence`/`TriggerMode` types near 32-118)
    - src/App.css (READ the current `.glass-card` rule at 76-88 — the opaque `linear-gradient` background + inert `backdrop-filter: blur(12px)`)
    - .planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md (Translucency section: the exact `.glass-card` replacement values; Component Inventory C-E1 for the ✎ edit-button classes and title; Interaction Contract UX-09 rows 1/3/4alt)
    - .planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md (MacroCard analog + "build the ActionSequence" pattern from handleCreateMacro)
  </read_first>
  <action>
Add ONE inline name-edit affordance to the macro card (thin tracer of D-13's inline expand-in-place — later generalized to the full field set in 10-05, do not build the full form here). Add a `✎` icon button to the card header action cluster using the C-E1 neutral-tinted classes and `title="Edit macro"`; clicking it sets `editingCardId(macro.id)`. When `editingCardId() === macro.id`, render a name `<input>` pre-filled with `macro.name` plus `Save Changes` (accent) and `Cancel` (neutral) buttons in place of the card name. `Save Changes` calls `invoke("update_macro", { ... })`, passing the macro's CURRENT sequence/trigger_mode/target_app/trigger_key/trigger_modifiers unchanged and the edited name (build the argument object from the existing `MacroConfig` fields; reuse the same snake_case arg keys the IPC expects). On resolve, clear `editingCardId`. On reject (catch), call `showConflictError(...)` and keep the editor open (mirrors handleCreateMacro's catch at 494-508). `Cancel` clears `editingCardId` with no invoke. Keep the existing `editingField` target/key inline editors working — the new full-card name edit is a sibling state gated on the same centralized `editingCardId` signal (mutual exclusion).

In `src/App.css`, replace the `.glass-card` background and blur to make translucency real (D-03): set the background to `color-mix(in srgb, var(--color-surface) 72%, transparent)` and `backdrop-filter: blur(20px) saturate(150%)`, keeping the existing border/border-radius/transition. This is the interim treatment; plan 10-02 finalizes tokens and adds `.sidebar-glass`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits clean (0 errors) — respects strict `noUnusedLocals`/`noUnusedParameters`.
    - `grep -c 'update_macro' src/App.tsx` returns >= 1 (the invoke call exists).
    - `grep -c 'color-mix' src/App.css` returns >= 1 and the `.glass-card` rule contains `backdrop-filter` with a blur value.
    - The `✎` edit button sets `editingCardId` (not a per-card local signal) — `grep -c 'setEditingCardId' src/App.tsx` returns >= 2 (open + clear).
    - Manual: launch the app, click ✎ on a macro, change the name, click Save Changes; the card shows the new name; restart the app and the new name persists.
  </acceptance_criteria>
  <done>An inline name edit on a macro card works end-to-end through update_macro and persists across restart; .glass-card shows genuine translucent blur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView (SolidJS) → Tauri IPC | Untrusted UI input crosses into the Rust process via `invoke("update_macro", ...)` |
| IPC → StateActor | IPC args become an `Intent` processed by the single-owner StateActor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | edit path could skip the trigger-key conflict check and silently shadow another macro's hotkey | medium | mitigate | Reuse `resolve_trigger_key_update` in the UpdateMacro handler; Err branch replies without mutating any field (atomic reject) — proven by `update_macro_conflict_no_partial_mutation` |
| T-10-02 | Denial of Service (minor) | malformed/oversized `name` string via `update_macro` IPC | low | accept | Identical exposure already exists via `add_macro`; local IPC only (`connect-src ipc:` CSP, no network); out of scope for a single-user desktop tool's threat model — unchanged by this phase |
| T-10-SC | Tampering | npm/cargo package installs | low | accept | N/A — this plan adds zero new npm/Cargo dependencies (no install tasks); supply-chain vector not opened |
</threat_model>

<verification>
- `cd src-tauri && cargo build` exits 0.
- `cd src-tauri && cargo test update_macro` passes the 3 new tests.
- `npx tsc --noEmit` is clean.
- Manual end-to-end: edit a macro name inline → persists across restart; a card visibly blurs content behind it.
</verification>

<success_criteria>
The macro-edit vertical slice is proven end-to-end for the name field, backed by 3 passing backend tests, over a genuinely translucent card — the architecture later plans build on is validated in one commit.
</success_criteria>

<output>
Create `.planning/phases/10-ui-redesign-macro-management/10-01-SUMMARY.md` when done.
</output>
