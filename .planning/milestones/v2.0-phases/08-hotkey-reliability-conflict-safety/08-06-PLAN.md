---
phase: 08-hotkey-reliability-conflict-safety
plan: 06
type: execute
wave: 6
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05]
files_modified:
  - .planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md
autonomous: true
requirements: [UX-14]
user_setup: []

must_haves:
  truths:
    - "`cargo test -p automux-lib` exits 0 with all new tests (4 conflict tests + 2 modifier-bit tests + previous phase's tests) passing"
    - "`cargo clippy -p automux-lib --all-targets -- -D warnings` exits 0 — no new clippy warnings"
    - "`cargo build --target x86_64-pc-windows-msvc 2>&1 | grep warning` returns zero lines (Windows cross-compile clean) — or the gate is documented as deferred if the target toolchain isn't installed locally"
    - "An existing v2.0 default profile (saved before Phase 8) loads without deserialization errors; `trigger_modifiers` defaults to 0 and `conflicts` defaults to `[]`"
    - "On macOS, after granting Accessibility and switching focus to another app, pressing a bound `Cmd+F5` toggles the macro (UX-14 macOS device verification)"
    - "On Windows, switching focus to another app, pressing a bound `Ctrl+Shift+F5` toggles the macro (UX-14 Windows device verification)"
  artifacts:
    - path: .planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md
      provides: "Phase verification report — full test suite output, clippy output, Windows cross-compile output, manual device test plan and results"
      contains: "## Verification Report"
  key_links:
    - from: src-tauri/src/state/mod.rs (Plan 08-02 test module)
      to: src-tauri/src/persistence.rs (existing test module)
      via: "combined `cargo test -p automux-lib` invocation runs both"
      pattern: "cargo test -p automux-lib"
    - from: src-tauri/src/platform/windows/mod.rs (Plan 08-03)
      to: cargo build --target x86_64-pc-windows-msvc
      via: "Windows cross-compile gate"
      pattern: "x86_64-pc-windows-msvc"
---

<objective>
Run the full verification suite — automated tests, clippy, Windows cross-compile, profile backwards-compat smoke — and document the manual macOS + Windows device test plan for UX-14. Capture the results in `08-VERIFICATION.md`.

Purpose: This plan delivers the Phase 8 gate. It does not introduce any code changes (those are plans 08-01 through 08-05). It runs every command listed in `08-VALIDATION.md` and produces a single verification report that the user can review before declaring the phase complete.

Output: `08-VERIFICATION.md` containing test results, clippy results, Windows cross-compile results, and the manual device test plan. The plan itself only writes the report; it does not modify source code.
</objective>

<execution_context>
@/Users/alvaro/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/alvaro/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hotkey-reliability-conflict-safety/08-RESEARCH.md
@.planning/phases/08-hotkey-reliability-conflict-safety/08-VALIDATION.md
@.planning/phases/08-hotkey-reliability-conflict-safety/08-UI-SPEC.md

# Direct sources for the verification gates
@src-tauri/src/state/mod.rs (the inline test module from Plan 08-02 with 4 tests; run via `cargo test -p automux-lib`)
@src-tauri/src/platform/macos/observer.rs (the `cg_event_flag_constants` test from Plan 08-01; runs only on macOS)
@src-tauri/src/platform/windows/mod.rs (the `windows_mod_constants` test from Plan 08-01; runs only on Windows; the `#[cfg(windows)]` gate means `cargo test` on macOS host skips it cleanly)
@src-tauri/src/persistence.rs (the pre-existing `large_config_memory_check` test — must still pass)
@.planning/phases/08-hotkey-reliability-conflict-safety/08-VALIDATION.md (Section "Per-Task Verification Map" — every task's expected command and expected status)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full `cargo test -p automux-lib` and `cargo clippy`; capture results in `08-VERIFICATION.md`</name>
  <files>.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/08-hotkey-reliability-conflict-safety/08-VALIDATION.md (the per-task verification map at lines 41-61 — every expected command and expected status)
    - .planning/phases/08-hotkey-reliability-conflict-safety/08-RESEARCH.md (Section 7 — sampling rates, full suite command, max feedback latency)
    - .planning/phases/08-hotkey-reliability-conflict-safety/08-01-PLAN.md Task 2 (the modifier-bit test names to expect in output: `cg_event_flag_constants`, `windows_mod_constants`)
    - .planning/phases/08-hotkey-reliability-conflict-safety/08-02-PLAN.md Task 1 (the conflict test names: `self_rebind_allowed`, `bind_conflict_rejected`, `conflict_detection_overlap`, `conflict_disappear_on_disable`)
  </read_first>
  <action>
    In `.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md`, write the report header and the first two sections. The file does not exist yet — create it with the Write tool. Structure:

    ```markdown
    ---
    phase: 8
    slug: hotkey-reliability-conflict-safety
    status: complete
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-06-19
    verified: <ISO timestamp>
    ---

    # Phase 8 — Verification Report

    ## 1. Test Suite

    Command: `cargo test -p automux-lib 2>&1 | tail -30`

    Expected results (paste the actual output in fenced code blocks):
    - `self_rebind_allowed` — ok
    - `bind_conflict_rejected` — ok
    - `conflict_detection_overlap` — ok
    - `conflict_disappear_on_disable` — ok
    - `cg_event_flag_constants` — ok (macOS host only; skipped on Windows)
    - `windows_mod_constants` — ok (Windows host only; skipped on macOS — the `#[cfg(windows)]` gate means it does not appear in macOS test output; this is the expected behavior)
    - `large_config_memory_check` — ok (pre-existing persistence test)
    - All other pre-existing scheduler tests — ok

    If any test fails, capture the failing test name, the error message, and the file:line. Do not mark the phase complete until the suite is green.

    ## 2. Clippy

    Command: `cargo clippy -p automux-lib --all-targets -- -D warnings 2>&1 | tail -20`

    Expected: empty output (clippy exits 0 with no warnings). The Phase 7 BUILD-01 fix established a zero-warning baseline; this phase must not regress it. If clippy reports any warning, capture the warning text and the file:line. Resolve before marking the phase complete.
    ```

    Run both commands and paste the literal output (or a representative tail) into the report. The commands must be run from the repo root (`/Users/alvaro/AutoClicker`). Capture the exit code as a fenced code block quote: ```` ```text\nexit 0\n``` ````.

    If a test fails due to a real defect, do NOT mark the section as "complete" — leave the report as a draft, fix the defect (in a follow-up commit), then re-run and update the report.

    Do NOT yet run the Windows cross-compile (Task 2) or the manual device tests (Task 3) or the profile backwards-compat test (Task 4). Those are separate tasks.
  </action>
  <verify>
    <automated>test -f /Users/alvaro/AutoClicker/.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md && grep -c 'self_rebind_allowed' /Users/alvaro/AutoClicker/.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md` exists
    - The file contains the literal `## 1. Test Suite` and `## 2. Clippy` sections
    - The Test Suite section names all 4 conflict tests (`self_rebind_allowed`, `bind_conflict_rejected`, `conflict_detection_overlap`, `conflict_disappear_on_disable`) and both modifier-bit tests (`cg_event_flag_constants`, `windows_mod_constants`)
    - The Clippy section contains the literal `cargo clippy -p automux-lib --all-targets -- -D warnings`
    - Each section includes the actual command output (or a representative tail) in a fenced code block
    - `cargo test -p automux-lib` exited 0 (the test suite itself was green)
    - `cargo clippy -p automux-lib --all-targets -- -D warnings` exited 0 (no new warnings)
  </acceptance_criteria>
  <done>The full test suite is green and clippy reports no new warnings. The verification report's first two sections are populated with the actual output.</done>
</task>

<task type="auto">
  <name>Task 2: Windows cross-compile gate — `cargo build --target x86_64-pc-windows-msvc` must be clean</name>
  <files>.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md</files>
  <read_first>
    - src-tauri/src/platform/windows/mod.rs (read ENTIRELY — the new `WindowsHotkeyBinding` struct, `HOTKEY_BINDINGS` static, `build_mod_mask` function, and the `hook_callback` tuple lookup from Plan 08-03)
    - src-tauri/src/state/mod.rs (the cfg-gated `build_hotkey_bindings_vec` helper from Plan 08-03 Task 1 that returns the Windows type)
    - .planning/codebase/CONCERNS.md lines 150-152 (the Windows gap this gate verifies is closed)
    - .planning/phases/07-carry-work-platform-ci-safety/07-PATTERNS.md (the Windows compile-gate pattern from Phase 7)
  </read_first>
  <action>
    In `.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md`, append a new section that documents the Windows cross-compile result. Run the gate and capture the output.

    ```markdown
    ## 3. Windows Cross-Compile Gate

    Command: `cargo build --target x86_64-pc-windows-msvc 2>&1 | tail -20`

    Expected: zero warning lines, exit 0. The Windows `#[cfg(target_os = "windows")]` blocks in `platform/windows/mod.rs` (the new `WindowsHotkeyBinding` struct, `HOTKEY_BINDINGS` static, `build_mod_mask` helper, and the tuple-keyed `hook_callback`) must compile cleanly. The cfg-gated test (`windows_mod_constants`) compiles too.

    If the `x86_64-pc-windows-msvc` target is not installed locally, document that the gate is deferred to CI and paste the output of `rustup target list --installed` to prove the target is unavailable. Do NOT mark the phase complete if the gate is silently skipped.
    ```

    The Phase 7 BUILD-01 fix (zero Windows warnings) is the baseline this gate must hold. If the cross-compile emits a new warning, capture the text and resolve before marking complete.

    Append the section to the existing `08-VERIFICATION.md` file from Task 1. Use the Edit tool — do NOT re-write the file from scratch.
  </action>
  <verify>
    <automated>grep -c '## 3. Windows Cross-Compile Gate' /Users/alvaro/AutoClicker/.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `08-VERIFICATION.md` contains the literal section header `## 3. Windows Cross-Compile Gate`
    - The section contains the command `cargo build --target x86_64-pc-windows-msvc 2>&1 | tail -20`
    - The section records the actual output (or the deferred-to-CI note with `rustup target list --installed` output)
    - If the target compiled: `cargo build --target x86_64-pc-windows-msvc 2>&1 | grep warning | wc -l` returns 0
    - If the target is not installed locally: the deferred-to-CI note is present and the section explains why
  </acceptance_criteria>
  <done>The Windows cross-compile gate is green (or deferred with a clear note). The Phase 7 zero-warning baseline holds.</done>
</task>

<task type="auto">
  <name>Task 3: Profile backwards-compat smoke (R-5) — load an existing v2.0 default profile saved before Phase 8</name>
  <files>.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md</files>
  <read_first>
    - src-tauri/src/state/mod.rs lines 101-111 (the `AppState` struct with `#[serde(default)]` on the new `conflicts` field)
    - src-tauri/src/state/mod.rs lines 79-99 (the `MacroConfig` struct with `#[serde(default)]` on the new `trigger_modifiers` field)
    - src-tauri/src/persistence.rs lines 1-100 (the `ProfileData` / `ProfileManager` save/load API and the on-disk JSON shape)
    - .planning/phases/08-hotkey-reliability-conflict-safety/08-RESEARCH.md (R-5 risk and mitigation: `#[serde(default)]` ensures old profiles load with `trigger_modifiers = 0`)
  </read_first>
  <action>
    In `.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md`, append a fourth section that documents the profile backwards-compat smoke. Run the smoke and capture the result.

    The smoke is a Rust-level test that constructs a pre-Phase-8 profile JSON (without `trigger_modifiers` or `conflicts` fields) and deserializes it into the new `ProfileData` / `MacroConfig` / `AppState` shapes. Add the test inline to `state/mod.rs` (gated on `#[cfg(test)]`) or as a small ad-hoc `cargo run` script — the test is small enough to live in the existing test module.

    Test sketch (no code blocks — describe in the action):
    - Create a JSON string that matches the pre-Phase-8 on-disk shape: a `ProfileData` with one or two `MacroConfig` entries that have NO `trigger_modifiers` field, and an `AppState` shape that is implicit in the `ProfileData.macros` field.
    - Call `serde_json::from_str::<ProfileData>(json)`.
    - Assert: the deserialization succeeded (no `Err`); the resulting `MacroConfig.trigger_modifiers == 0` (default); the resulting `MacroConfig.trigger_key` is whatever was saved (preserved); the resulting `ProfileData.macros.len()` matches the input.
    - Repeat the same for `AppState` if needed (the `AppState` shape is not directly persisted to the file, but the `conflicts: Vec<InputConflict>` field must default to `[]` when deserialized from a JSON that omits it — write a small test that round-trips an `AppState` with no `conflicts` key).

    The smoke can also be done manually: run the app before this phase (or check out a pre-Phase-8 commit), create a macro with a trigger key, save the default profile, then run this phase and load the profile. Verify the macro loads with the trigger key intact and no error in the UI. Document the manual approach as a fallback in the verification report — the inline test is the primary evidence.

    ```markdown
    ## 4. Profile Backwards-Compat (R-5)

    ### 4.1 Automated Smoke (preferred)

    Command: `cargo test -p automux-lib profile_backwards_compat 2>&1 | tail -10`

    Expected: the new test `profile_backwards_compat` passes. The test deserializes a pre-Phase-8 profile JSON (no `trigger_modifiers` or `conflicts` fields) and asserts the new fields default to `0` and `[]` respectively.

    Paste the actual output.

    ### 4.2 Manual Smoke (fallback if the automated test was not added)

    Steps:
    1. `git stash` the current changes (save the Phase 8 work).
    2. `git checkout <pre-Phase-8 commit>` (e.g., the Phase 7 last commit).
    3. `npm run tauri dev` — create a macro with a trigger key, save the default profile (which now lives at `~/Library/Application Support/com.alvaro.automux/profiles/default.json` on macOS or `%APPDATA%\com.alvaro.automux\profiles\default.json` on Windows).
    4. Quit the app.
    5. `git checkout <Phase 8 commit>` and `git stash pop`.
    6. `npm run tauri dev` again.
    7. In the app, load the default profile (the Profiles tab's Load button on the `Default` row).
    8. Verify: no error toast; the macro's trigger key is preserved; `state().conflicts` is `[]` (no spurious conflicts).
    9. Capture a screenshot if useful.

    Document the result in the report. If the manual smoke is the only evidence available, note that explicitly so future maintainers know the automated test was not added in this phase.
    ```
  </action>
  <verify>
    <automated>grep -c '## 4. Profile Backwards-Compat' /Users/alvaro/AutoClicker/.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `08-VERIFICATION.md` contains the literal section header `## 4. Profile Backwards-Compat`
    - The section contains either: (a) the `cargo test -p automux-lib profile_backwards_compat` output showing the test passing, OR (b) the manual smoke steps with a result note
    - The section explicitly addresses the `trigger_modifiers` field defaulting to 0 and the `conflicts` field defaulting to `[]`
    - If the automated test was added, `cargo test -p automux-lib profile_backwards_compat` exits 0
  </acceptance_criteria>
  <done>The profile backwards-compat smoke is documented and either the automated test passes or the manual smoke is recorded.</done>
</task>

<task type="auto">
  <name>Task 4: Write the manual macOS + Windows device test plan for UX-14 system-wide hotkey behavior; mark the report complete</name>
  <files>.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/08-hotkey-reliability-conflict-safety/08-UI-SPEC.md (Section "UX-14 — System-wide communication" — the expected behavior on both platforms)
    - .planning/phases/08-hotkey-reliability-conflict-safety/08-RESEARCH.md (Section 3.4 — mechanism is already global by definition; manual test is the proof)
    - .planning/phases/08-hotkey-reliability-conflict-safety/08-VALIDATION.md (Section "Manual-Only Verifications" — the device-test plan at the bottom)
  </read_first>
  <action>
    In `.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md`, append a fifth and sixth section with the manual device test plan and a final status section.

    ```markdown
    ## 5. Manual macOS Device Test (UX-14)

    These steps must be performed on a real macOS host. The `cg_event_flag_constants` unit test covers the bit values; the device test confirms the end-to-end behavior on a real machine.

    Pre-flight:
    - [ ] Accessibility granted to AutoMux (System Settings → Privacy & Security → Accessibility).
    - [ ] Input Monitoring granted to AutoMux (System Settings → Privacy & Security → Input Monitoring).
    - [ ] App is running via `npm run tauri dev` or a release build.

    Test 5.1 — System-wide hotkey fires when AutoMux is unfocused:
    1. Create a macro named `Test Toggle` with a left-click action (or any action), and a trigger key of `Cmd+F5` (hold `Cmd`, press `F5`).
    2. Click into another app (e.g., Safari, TextEdit, or Finder).
    3. Press `Cmd+F5`. Expected: the macro toggles on (the `Test Toggle` card's toggle flips). The macro injects a left-click into the focused app.
    4. Press `Cmd+F5` again. Expected: the macro toggles off.

    Test 5.2 — First-run banner appears once and dismisses:
    1. Clear `localStorage.automux.hotkey_global_notice_dismissed` (DevTools console: `localStorage.removeItem("automux.hotkey_global_notice_dismissed")`).
    2. Reload the app (Ctrl+R / Cmd+R).
    3. Expected: the `🌍 Binds are system-wide` banner appears.
    4. Click `Got it`. Expected: the banner disappears.
    5. Reload again. Expected: the banner does NOT reappear.

    Test 5.3 — Trigger-key conflict surfaces a toast:
    1. Create macro A with trigger key `F5`.
    2. Try to re-bind macro B to `F5`. Expected: a danger-tinted toast `Hotkey already bound — F5 is already assigned to "<macro A name>". Unbind it first or pick a different key.` appears for 8 seconds.
    3. Pick a different key for macro B. Expected: the bind succeeds, no toast.

    Test 5.4 — Same-input overlap warning appears:
    1. Enable macro A (a left-click macro) and macro B (also a left-click macro).
    2. Expected: a warning-tinted card appears: `⚠ 2 macros are injecting the same input — "Macro A" and "Macro B" both inject Left Click — clicks will fire at 2× rate.`
    3. Disable macro A. Expected: the warning disappears on the next `state-changed`.

    Test 5.5 — In-card `↗ Global` subtitle visible:
    1. With at least one macro created, scroll the dashboard.
    2. Expected: every macro card shows `↗ Global` next to the trigger-mode suffix (e.g., `(Pulse) ↗ Global`).

    ## 6. Manual Windows Device Test (UX-14)

    These steps must be performed on a real Windows host. The `windows_mod_constants` unit test covers the bit values; the device test confirms the hook fires when another app is focused.

    Pre-flight:
    - [ ] The app is running (release build or `npm run tauri dev` from a Windows host).
    - [ ] If Windows SmartScreen or UAC blocks the hook, run as Administrator OR add an exception in the antivirus.

    Test 6.1 — System-wide hotkey fires when AutoMux is unfocused:
    1. Create a macro with a left-click action and a trigger key of `Ctrl+Shift+F5` (hold `Ctrl` and `Shift`, press `F5`).
    2. Click into another app (Notepad, File Explorer, or a browser).
    3. Press `Ctrl+Shift+F5`. Expected: the macro toggles on (verify by checking the macro's toggle state in the AutoMux window).
    4. Press again to toggle off.

    Test 6.2 — `bind_hotkey` IPC is no longer a no-op:
    1. Open the app, create a macro.
    2. In the macro card, click the `Set key…` chip.
    3. Press `Ctrl+F1`.
    4. Expected: the chip updates to show `Ctrl+F1` (or similar — the exact label depends on the modifier symbol map; the `bind_hotkey` IPC should NOT silently fail as it did in v2.0 before this phase).
    5. Click into another app, press `Ctrl+F1`. Expected: the macro toggles.

    Test 6.3 — First-run banner + in-card `↗ Global`:
    1. Same as macOS Test 5.2 and 5.5. The UI is platform-agnostic.

    Test 6.4 — Same-input overlap warning:
    1. Same as macOS Test 5.4. The UI is platform-agnostic.

    ## 7. Verification Status

    | Section | Status | Notes |
    |---------|--------|-------|
    | 1. Test Suite | ⬜ pending | |
    | 2. Clippy | ⬜ pending | |
    | 3. Windows Cross-Compile | ⬜ pending | |
    | 4. Profile Backwards-Compat | ⬜ pending | |
    | 5. Manual macOS Device Test | ⬜ pending | (requires human) |
    | 6. Manual Windows Device Test | ⬜ pending | (requires human) |

    The phase is complete when all six sections are marked `✅ done`. Sections 5 and 6 are inherently manual — the executor cannot perform them. Document the result (or the deferred-to-user status) and stop.
    ```

    Use the Edit tool to append the sections to the existing `08-VERIFICATION.md` from Tasks 1-3. Do NOT rewrite the file from scratch.

    The report is now complete as a planning artifact. The actual run of the manual tests happens outside the plan — the user (or a human verification step) executes the steps and updates the status checkboxes. The plan's deliverable is the document itself.
  </action>
  <verify>
    <automated>grep -c '## 7. Verification Status' /Users/alvaro/AutoClicker/.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `08-VERIFICATION.md` contains the section header `## 7. Verification Status`
    - The file contains all 7 sections (1 through 7)
    - Sections 5 and 6 contain the macOS and Windows manual test steps respectively
    - Section 5's Test 5.1 names the literal macro `Cmd+F5` (the macOS system-wide hotkey test)
    - Section 6's Test 6.1 names the literal macro `Ctrl+Shift+F5` (the Windows system-wide hotkey test)
    - Section 7's status table has 6 rows (Sections 1-6)
  </acceptance_criteria>
  <done>The verification report is complete: 7 sections covering the automated test suite, clippy, Windows cross-compile, profile backwards-compat, and the manual macOS + Windows device test plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / local runner → `cargo test -p automux-lib` | The test suite must pass in a trusted build environment. |
| CI runner → `cargo build --target x86_64-pc-windows-msvc` | The Windows cross-compile gate must pass on a CI runner that has the target installed. |
| Human verifier → device test execution | The macOS / Windows device tests require a physical host and a human operator to confirm global hotkey behavior. The agent cannot perform these. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-08-31 | Tampering | Verification report contents | mitigate | The report captures the literal output of `cargo test` and `cargo build`; a tampered report can be re-verified by re-running the commands. The manual test steps are pinned by exact macro names and key combos (no ambiguity). |
| T-08-32 | Denial of Service | Test suite timeout | accept | The full suite runs in < 10s per VALIDATION.md; if it times out, the regression is real and must be debugged. |
| T-08-33 | Repudiation | Manual test result claimed without evidence | mitigate | The status table requires the human verifier to mark sections `done` only after the literal step in the section passes. Screenshots are optional but encouraged. |
| T-08-34 | Information Disclosure | Profile backwards-compat test loads a real user profile | accept | The test uses a synthetic JSON string in the unit test path (Task 3 §4.1). The manual path (Task 3 §4.2) operates on the developer's own profile and is intentional. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | The verification commands do not install anything. slopcheck gate not relevant. |
</threat_model>

<verification>
After all four tasks complete:
- [ ] `08-VERIFICATION.md` exists and contains 7 sections
- [ ] Sections 1-4 contain the literal command output (or the deferred-to-CI note)
- [ ] Sections 5-6 contain the device test plan with named macros and key combos
- [ ] Section 7's status table has 6 rows
- [ ] `cargo test -p automux-lib` is green (Section 1 evidence)
- [ ] `cargo clippy -p automux-lib --all-targets -- -D warnings` is green (Section 2 evidence)
- [ ] `cargo build --target x86_64-pc-windows-msvc 2>&1 | grep warning | wc -l` returns 0, OR the deferred-to-CI note is present (Section 3 evidence)
- [ ] The profile backwards-compat test passes OR the manual smoke is recorded (Section 4 evidence)
</verification>

<success_criteria>
1. The full test suite is green and clippy reports no warnings (automated).
2. The Windows cross-compile is clean (or deferred to CI with a clear note).
3. A pre-Phase-8 profile loads without deserialization errors (backwards-compat proven).
4. The manual device test plan is documented for both macOS and Windows so the human verifier can execute it.
5. The verification report is in `.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md` and is the source of truth for the phase's completion status.
</success_criteria>

<output>
Create `.planning/phases/08-hotkey-reliability-conflict-safety/08-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan creates the following artifacts (one file — the verification report):

**New file:**
- `.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md` — the phase gate artifact. Contains 7 sections: Test Suite, Clippy, Windows Cross-Compile, Profile Backwards-Compat, Manual macOS Device Test, Manual Windows Device Test, and Verification Status.

**No source code changes in this plan** — verification is a read-only operation against the artifacts produced by Plans 08-01 through 08-05.

**No new Rust symbols, frontend symbols, CSS classes, or file paths in this plan** beyond the single `08-VERIFICATION.md` file.
