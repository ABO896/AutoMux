---
phase: 07-carry-work-platform-ci-safety
plan: 03
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - src/App.tsx
autonomous: true
requirements:
  - COMPAT-04
  - COMPAT-05
  - ERR-01

must_haves:
  truths:
    - "The 3-second polling loop in App.tsx calls BOTH `check_accessibility` AND `check_input_monitoring` in parallel via `Promise.all`, updating the new `inputMonitoring` signal alongside the existing `accessibility` signal"
    - "The solo Accessibility card in App.tsx is replaced by a combined Permissions card containing two rows: Accessibility and Input Monitoring, each with its own status dot, sublabel, and Grant Access button (button only shown when denied and not pending)"
    - "The Input Monitoring row's Grant Access button opens `x-apple.systempreferences:com.apple.preference.security?Privacy_ListenEvent` via `import { open } from \"@tauri-apps/plugin-opener\"` (NOT via a custom invoke command)"
    - "When `get_tcc_identity_status` returns true AND `accessibility()` is false, the Accessibility row's sublabel swaps to the identity-change copy: \"AutoMux was updated — Accessibility needs to be re-added in System Settings.\""
    - "A `listen('auto-save-error', ...)` subscription in App.tsx sets a new `saveError` signal to true; a persistent inline banner appears at the top of the macro list with a Dismiss button; clicking Dismiss clears the banner; the banner re-appears on the next auto-save-error event"
    - "The auto-save error banner copy is hard-coded user-friendly text — it NEVER reads or displays the event payload string from the Rust backend (D-13)"
  artifacts:
    - path: "src/App.tsx"
      provides: "Combined PermissionsCard, Input Monitoring polling, handleRequestInputMonitoringAccess, tccIdentityChanged copy swap, auto-save-error listener and banner"
      contains: "handleRequestInputMonitoringAccess"
  key_links:
    - from: "src/App.tsx"
      to: "src-tauri/src/ipc/mod.rs"
      via: "invoke<boolean>(\"check_input_monitoring\") and invoke<boolean>(\"get_tcc_identity_status\")"
      pattern: "check_input_monitoring|get_tcc_identity_status"
    - from: "src/App.tsx"
      to: "@tauri-apps/plugin-opener"
      via: "import { open } from \"@tauri-apps/plugin-opener\"; open(\"x-apple.systempreferences:...\")"
      pattern: "@tauri-apps/plugin-opener"
    - from: "src/App.tsx"
      to: "src-tauri/src/state/mod.rs:auto-save-error event"
      via: "listen<string>(\"auto-save-error\", ...)"
      pattern: "listen\\(.auto-save-error."
    - from: "src/App.tsx"
      to: "src-tauri/src/lib.rs"
      via: "Promise.all fetches check_accessibility, check_input_monitoring, get_tcc_identity_status at startup"
      pattern: "get_tcc_identity_status"
</interfaces>

---

<objective>
Wire the frontend surface for the macOS 26 follow-up requirements (COMPAT-04, COMPAT-05) and the auto-save error notification (ERR-01). Replace the solo Accessibility card on the Dashboard with a combined Permissions card that lists both Accessibility and Input Monitoring, swap the Accessibility sublabel to identity-change copy when TCC invalidates the grant, and add a persistent inline auto-save error banner at the top of the macro list. Wires the COMPAT-04 frontend surface (D-02, D-03, D-04) and the COMPAT-05 identity-change copy swap (D-09).

Purpose: This plan consumes the IPC commands and persistence helpers from plan 07-02. The backend already writes `tcc_granted.flag` on first Accessibility grant; this plan reads it via `get_tcc_identity_status` and surfaces the resulting "AutoMux was updated" copy in the UI. The auto-save banner is a thin frontend wrapper around the `auto-save-error` event the backend already emits.

Output:
- src/App.tsx — combined Permissions card, Input Monitoring polling/handler, TCC identity-change copy, auto-save error banner
</objective>

<execution_context>
@/Users/alvaro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alvaro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-carry-work-platform-ci-safety/07-CONTEXT.md
@.planning/phases/07-carry-work-platform-ci-safety/07-RESEARCH.md
@.planning/phases/07-carry-work-platform-ci-safety/07-PATTERNS.md
@.planning/phases/07-carry-work-platform-ci-safety/07-UI-SPEC.md

# Source files this plan reads but does not modify
@src/App.tsx (entire file, 1045 lines) — the file being modified
@src-tauri/src/ipc/mod.rs — check_input_monitoring and get_tcc_identity_status commands (added in plan 07-02)
@src-tauri/src/state/mod.rs lines 420–500 — confirms auto-save-error event is already emitted from the backend
@src/App.css (color tokens) — `--color-warning`, `--color-warning-glow` are referenced in UI-SPEC §Color
</context>

<interfaces>
<!-- Key interfaces and exact locations the executor needs. Do not explore — use these directly. -->

From src/App.tsx:
- Line 1–6: existing imports — add `import { open } from "@tauri-apps/plugin-opener";` to this block.
- Line 83–84: existing accessibility signals — add FOUR new signals after these:
  - `const [inputMonitoring, setInputMonitoring] = createSignal<boolean | null>(null);`
  - `const [inputMonitoringPending, setInputMonitoringPending] = createSignal(false);`
  - `const [saveError, setSaveError] = createSignal(false);`
  - `const [tccIdentityChanged, setTccIdentityChanged] = createSignal(false);`
  Plus a `let _imPendingTimeoutId: ReturnType<typeof setTimeout> | null = null;` near the existing `_pendingTimeoutId` declaration (line 80).
- Line 125: `const IS_MACOS = ...` — already present and usable for the Input Monitoring Grant button visibility.
- Line 129–155: initial-fetch effect — extend the `Promise.all` to include `invoke<boolean>("check_input_monitoring")` and `invoke<boolean>("get_tcc_identity_status")`. Add the corresponding setters.
- Line 168–182: 3-second polling effect — extend to call `check_input_monitoring` in parallel with `check_accessibility` via `Promise.all`.
- Line 208–226: `handleRequestAccess` — the template for the new `handleRequestInputMonitoringAccess` function. The new function uses `open()` from `@tauri-apps/plugin-opener` instead of `invoke("request_accessibility")`.
- Line 487–535: solo Accessibility card to REPLACE with the combined Permissions card (UI-SPEC §Component Inventory).
- Line 158–166: `state-changed` listener — the template for the new `auto-save-error` listener. Listener body must NOT read or surface the event payload string (D-13).
- Line 583 onward: Macros section header — the new `AutoSaveErrorBanner` JSX goes directly above this section header (UI-SPEC §New `AutoSaveErrorBanner`).

From package.json: `@tauri-apps/plugin-opener` is already a dependency (line 17 per STACK.md). No new install needed.

From src-tauri/src/lib.rs (per plan 07-02): the new commands `ipc::check_input_monitoring` and `ipc::get_tcc_identity_status` are registered in the invoke_handler! macro and callable via `invoke<boolean>(...)`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace solo Accessibility card with combined PermissionsCard — Input Monitoring row, TCC identity-change copy, polling extension (COMPAT-04, COMPAT-05)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (entire file, 1045 lines) — the file being modified
    - src/App.tsx lines 1–6 — current imports; add `import { open } from "@tauri-apps/plugin-opener";` to this block
    - src/App.tsx lines 80–84 — existing signal declarations (pattern for new signals)
    - src/App.tsx lines 83–84 — `accessibility` / `accessibilityPending` signals (pattern for `inputMonitoring` / `inputMonitoringPending`)
    - src/App.tsx lines 129–155 — initial-fetch effect (the Promise.all to extend)
    - src/App.tsx lines 158–166 — state-changed listener (teardown pattern)
    - src/App.tsx lines 168–182 — 3s polling effect (the setInterval to extend)
    - src/App.tsx lines 200–226 — `clearPending` and `handleRequestAccess` (the template for the new IM handler)
    - src/App.tsx lines 487–535 — solo Accessibility card to REPLACE
    - .planning/phases/07-carry-work-platform-ci-safety/07-UI-SPEC.md §Component Inventory and §Copywriting Contract — exact strings for the new Permissions card
    - .planning/phases/07-carry-work-platform-ci-safety/07-PATTERNS.md "src/App.tsx" section — Patterns 7a/7b/7c/7e and Shared Pattern 5
  </read_first>
  <behavior>
    - Two new signals `inputMonitoring` and `inputMonitoringPending` are declared at the same scope as `accessibility` / `accessibilityPending`
    - One new signal `tccIdentityChanged` is declared (initially false); set to true on the initial fetch when `get_tcc_identity_status` returns true
    - Initial-fetch Promise.all calls BOTH `check_input_monitoring` AND `get_tcc_identity_status` in addition to the existing commands
    - The 3-second polling effect calls BOTH `check_accessibility` AND `check_input_monitoring` via Promise.all
    - `handleRequestInputMonitoringAccess` calls `await open("x-apple.systempreferences:com.apple.preference.security?Privacy_ListenEvent")` from `@tauri-apps/plugin-opener` (not a custom invoke)
    - The combined Permissions card (D-02) has a "Permissions" header, an Accessibility row, and an Input Monitoring row, each with status dot+label+sublabel+conditional Grant Access button
    - The Accessibility row's sublabel swaps to the TCC identity-change copy (D-09) when `tccIdentityChanged() === true` AND `accessibility() === false`
    - The Input Monitoring row's sublabel is "Required for global hotkeys" by default and "Hotkeys will not fire. Mouse macros still work." when `accessibility() === false && inputMonitoring() === false`
    - The Input Monitoring row's status label reads "Warning" (not "Denied") when the probe fails, per UI-SPEC §Interaction Notes (D-06)
  </behavior>
  <action>
    Modify src/App.tsx in seven locations.

    Change 1 — add the opener import. On line 3 area, after the existing `import { listen } from "@tauri-apps/api/event";`, add a new line: `import { open } from "@tauri-apps/plugin-opener";`. Keep alphabetical order (the existing imports are loosely grouped; the opener import is fine after listen).

    Change 2 — add a new timeout ref. After line 80 (`let _pendingTimeoutId: ReturnType<typeof setTimeout> | null = null;`), add `let _imPendingTimeoutId: ReturnType<typeof setTimeout> | null = null;`.

    Change 3 — add four new signals. After line 84 (`const [accessibilityPending, setAccessibilityPending] = createSignal(false);`), add the four new signals in this order using `createSignal`:

    1. `inputMonitoring` signal — type `Accessor<boolean | null>`, initial value `null`. Pair with setter `setInputMonitoring`. Mirrors the existing `accessibility` signal.
    2. `inputMonitoringPending` signal — type `Accessor<boolean>`, initial value `false`. Pair with setter `setInputMonitoringPending`. Mirrors the existing `accessibilityPending` signal.
    3. `saveError` signal — type `Accessor<boolean>`, initial value `false`. Pair with setter `setSaveError`. Presence-only flag for the auto-save error banner.
    4. `tccIdentityChanged` signal — type `Accessor<boolean>`, initial value `false`. Pair with setter `setTccIdentityChanged`. Set to true on initial fetch when `get_tcc_identity_status` returns true.

    Change 4 — extend the initial-fetch effect. Modify the Promise.all on line 135 to also fetch `check_input_monitoring` and `get_tcc_identity_status`. The new destructure must include two new local variables `imOk: boolean` (from `check_input_monitoring`) and `tccChanged: boolean` (from `get_tcc_identity_status`). The Promise.all array gains two new entries: `invoke<boolean>("check_input_monitoring")` placed after the existing `invoke<boolean>("check_accessibility")` call, and `invoke<boolean>("get_tcc_identity_status")` placed after the `check_input_monitoring` call. Add the corresponding setters after the existing `setAccessibility(accessOk);` line: call `setInputMonitoring(imOk);` and `setTccIdentityChanged(tccChanged);`. The `get_tcc_identity_status` semantics: returns `true` when the flag file exists; on a fresh install (no flag yet) it returns `false`. The frontend treats `tccChanged === true` + `accessibility === false` as the identity-change signal — the copy swap in the Permissions card handles this case.

    Change 5 — extend the 3-second polling effect (D-04). Replace the body of the setInterval callback (lines 170–179) to call both commands in parallel via `Promise.all`. The new body awaits `Promise.all` with two entries: `invoke<boolean>("check_accessibility")` and `invoke<boolean>("check_input_monitoring")`. Destructures the result as `[a11y, im]: [boolean, boolean]`. Calls `setAccessibility(a11y)`, `setInputMonitoring(im)`, then `clearPending()`. The catch block `try { ... } catch (_) { /* ignore */ }` is preserved. The `setInterval` interval `3000` and the outer `createEffect` + `onCleanup(() => clearInterval(interval))` shape are unchanged. After the `clearPending()` call, also call `clearImPending()` to clear any pending IM-grant state. Note: `clearImPending` is defined in Change 6 below — the file edit ordering means Change 6 must be in place before the runtime call resolves, but TypeScript hoists function declarations so the order of edits in the file does not matter.

    Change 6 — add the IM helpers after `handleRequestAccess` (after line 226). Add two helpers:

    Add a `function clearImPending()` helper. The body: calls `setInputMonitoringPending(false)`. Then checks `if (_imPendingTimeoutId !== null) { clearTimeout(_imPendingTimeoutId); _imPendingTimeoutId = null; }`. Mirrors the existing `clearPending()` function (lines 200–206).

    Add an `async function handleRequestInputMonitoringAccess()` helper (D-03). The body: (1) calls `setInputMonitoringPending(true)`; (2) sets `_imPendingTimeoutId = setTimeout(() => { if (!requestAccessCancelled) setInputMonitoringPending(false); _imPendingTimeoutId = null; }, 30_000)`; (3) wraps `await open("x-apple.systempreferences:com.apple.preference.security?Privacy_ListenEvent")` in a `try { ... } catch (e) { ... }` block. The catch block checks `if (!requestAccessCancelled)`, then `console.error("Failed to open Input Monitoring settings:", e)`, then `setInputMonitoringPending(false)` and `_imPendingTimeoutId = null`. There is no `finally` block.

    Note: `requestAccessCancelled` is reused (already declared at line 186) — the IM flow piggybacks on the same cancellation flag because the two flows never run concurrently from the user's perspective. The polling loop's `clearImPending()` should be invoked from the 3s polling — confirm the `clearImPending();` call was added after the `clearPending();` call in Change 5.

    Change 7 — REPLACE the solo Accessibility card (lines 487–535) with the combined PermissionsCard. The new structure is a single `.glass-card` containing two stacked rows. The Accessibility row is essentially the existing card content with one change: its sublabel `<p class="text-xs text-text-dim">Accessibility Permissions</p>` becomes a conditional render that swaps between "Required for input injection" and the identity-change copy. The Input Monitoring row mirrors the Accessibility row with new copy and a different status label when denied.

    Replace the entire Accessibility card block (lines 487–535) with the combined PermissionsCard JSX. The exact JSX shape and copy are specified in 07-UI-SPEC.md §"Component Inventory > New: `PermissionsCard`" and 07-UI-SPEC.md §"Copywriting Contract" — follow those contracts exactly. The structure (described in prose; the executor reads 07-UI-SPEC.md and 07-PATTERNS.md §"src/App.tsx" §7c for the canonical SolidJS patterns):

    Outer wrapper: a single `<div class="glass-card flex-1 p-4">` element. Inside, a header row contains a `<span class="text-xs font-medium text-text-muted uppercase tracking-wider">Permissions</span>` label. Below the header, a `<div class="flex flex-col gap-3">` element wraps the two permission rows.

    Row 1 — Accessibility row (label = "Accessibility", sublabel swaps between "Required for input injection" and the TCC identity-change copy, status dot tri-state Granted/Denied/Pending…, button id="btn-request-access" calls handleRequestAccess, button class="mt-2 w-full py-1.5 rounded-lg bg-accent/10 border border-accent/30 text-accent text-xs font-medium hover:bg-accent/20 hover:border-accent/50 transition-all duration-200 cursor-pointer", text "Grant Access", button is rendered only when `accessibility() === false && !accessibilityPending()`). The sublabel swap uses `<Show when={tccIdentityChanged() === true && accessibility() === false} fallback="Required for input injection">` and the swap target text is the exact literal "AutoMux was updated — Accessibility needs to be re-added in System Settings." (UI-SPEC §"Copywriting Contract" row "Accessibility row sublabel (TCC identity change)").

    Row 2 — Input Monitoring row. Same structure as the Accessibility row, but the label is "Input Monitoring", the sublabel swap uses `<Show when={accessibility() === false && inputMonitoring() === false} fallback="Required for global hotkeys">` and the swap target text is the exact literal "Hotkeys will not fire. Mouse macros still work." (UI-SPEC §"Copywriting Contract" row "Input Monitoring row sublabel (denied)"). The status dot tri-state is Granted/Warning/Pending… — note that the denied-state label text is "Warning" (NOT "Denied", per UI-SPEC §"Interaction Notes > Input Monitoring — soft warning, not hard block (D-06)"). The button has id="btn-request-input-monitoring", calls `handleRequestInputMonitoringAccess`, and uses the same `bg-accent/10 border border-accent/30 text-accent` classes as the Accessibility button.

    Status dot CSS for each state (apply to both rows): `Granted` uses `<div class="w-2 h-2 rounded-full bg-success shadow-[0_0_6px_var(--color-success-glow)]" />` paired with `<span class="text-[11px] text-success font-medium">Granted</span>`. `Denied` (Accessibility row) and `Warning` (Input Monitoring row) use `<div class="w-2 h-2 rounded-full bg-danger status-pulse shadow-[0_0_6px_var(--color-danger-glow)]" />` with the row-specific label text. `Pending…` uses `<div class="w-2 h-2 rounded-full bg-warning status-pulse shadow-[0_0_6px_var(--color-warning-glow)]" />` with `<span class="text-[11px] text-warning font-medium">Pending…</span>`. These CSS class strings are copied verbatim from the existing solo Accessibility card (lines 493–522).

    The tri-state pattern for each row is the nested `<Show when={granted} fallback={<Show when={pending} fallback={<denied_or_warning>}>}>` shape (Granted → Pending → Denied/Warning fallback chain). This mirrors the existing solo Accessibility card's tri-state (lines 493–522).

    The Engine card (lines 537–558) is unchanged. The new Permissions card and the Engine card both have `class="flex-1"` and sit side-by-side in the same flex row (line 486 `<div class="flex gap-3">`).
  </action>
  <verify>
    <automated>grep -n "check_input_monitoring\|get_tcc_identity_status" /Users/alvaro/AutoClicker/src/App.tsx | head -10</automated>
    <automated>grep -n "handleRequestInputMonitoringAccess" /Users/alvaro/AutoClicker/src/App.tsx | head -5</automated>
    <automated>grep -n "tccIdentityChanged" /Users/alvaro/AutoClicker/src/App.tsx | head -5</automated>
    <automated>grep -n "@tauri-apps/plugin-opener" /Users/alvaro/AutoClicker/src/App.tsx | head -5</automated>
    <automated>grep -n "x-apple.systempreferences:com.apple.preference.security?Privacy_ListenEvent" /Users/alvaro/AutoClicker/src/App.tsx | head -5</automated>
    <automated>grep -n "AutoMux was updated" /Users/alvaro/AutoClicker/src/App.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep for "check_input_monitoring" in App.tsx returns ≥ 1 match (initial fetch or polling)
    - grep for "get_tcc_identity_status" in App.tsx returns ≥ 1 match (initial fetch)
    - grep for "handleRequestInputMonitoringAccess" in App.tsx returns ≥ 2 matches (one declaration, one onClick reference)
    - grep for "tccIdentityChanged" in App.tsx returns ≥ 2 matches (declaration and at least one read)
    - grep for "@tauri-apps/plugin-opener" in App.tsx returns ≥ 1 match (the new import)
    - grep for "x-apple.systempreferences:com.apple.preference.security?Privacy_ListenEvent" in App.tsx returns 1 match
    - grep for "AutoMux was updated" in App.tsx returns 1 match (the TCC identity-change sublabel)
    - The Engine card block (`<div class="glass-card flex-1 p-4">` with "Engine" header) is still present and unchanged
    - The solo Accessibility card is REPLACED — the file no longer contains the literal `<p class="text-xs text-text-dim">Accessibility Permissions</p>` (this was the old solo copy)
  </acceptance_criteria>
  <done>App.tsx is wired to detect and display both Accessibility and Input Monitoring status, with a combined Permissions card on the Dashboard. The TCC identity-change copy replaces the default sublabel when the flag exists and Accessibility is denied. The Input Monitoring Grant button opens the correct System Settings pane via @tauri-apps/plugin-opener. The 3s polling loop covers both permissions in a single Promise.all.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add auto-save-error listener + AutoSaveErrorBanner UI (ERR-01)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx lines 158–166 — the existing state-changed listener (the template for the new auto-save-error listener; copy the createEffect + listen + onCleanup shape verbatim)
    - src/App.tsx lines 583–603 — the "Macros" section header area (location for the new banner — directly above this header)
    - .planning/phases/07-carry-work-platform-ci-safety/07-UI-SPEC.md §"New: `AutoSaveErrorBanner`" and §"Copywriting Contract" — exact copy and layout
    - .planning/phases/07-carry-work-platform-ci-safety/07-PATTERNS.md "src/App.tsx" section §7d — listener lifecycle and banner JSX pattern
    - .planning/phases/07-carry-work-platform-ci-safety/07-CONTEXT.md "D-11", "D-12", "D-13", "D-14" — locked decisions
  </read_first>
  <behavior>
    - A new `createEffect` wraps `listen<string>("auto-save-error", ...)` and pairs it with `onCleanup` to remove the listener on unmount/HMR
    - The listener body calls `setSaveError(true)` and does NOT read or display the event payload string (D-13 — event payload is ignored)
    - The `AutoSaveErrorBanner` JSX is placed at the top of the macro list area, directly above the "Macros" section header
    - The banner is visible only when `saveError()` is true; clicking "Dismiss" sets `saveError(false)` to hide it
    - The banner copy is hard-coded: heading "Save failed", body "Your changes are not being saved. Check available disk space and file permissions." — these strings do NOT come from the event payload
    - A subsequent `auto-save-error` event re-shows the banner (D-14 — no permanent suppression)
  </behavior>
  <action>
    Modify src/App.tsx in two locations.

    Change 1 — add the listener. After the existing `state-changed` listener block (after line 166, before the polling effect on line 168), add a new `createEffect` that mirrors the shape of the `state-changed` listener (lines 158–166). The new listener's signature: `listen<string>("auto-save-error", (_event) => { setSaveError(true); })` where the `_event` parameter is prefixed with `_` to indicate it is intentionally ignored. The `createEffect` callback assigns the result to a `const unlisten` and pairs it with an `onCleanup(() => { unlisten.then((fn) => fn()); })` teardown — this is the canonical SolidJS `listen` teardown shape used in this codebase (PATTERNS.md §4). The listener body contains exactly one statement: `setSaveError(true);` — it does NOT read `_event.payload` and does NOT call `console.error` on the event payload. A short comment above the `createEffect` documents the D-11 rationale. A short comment inside the listener body documents the D-13 rationale (the event payload is consumed but never displayed).

    Change 2 — add the banner JSX. Find the "Macros" section header on line 585–603 (the `<div class="flex items-center justify-between mt-1">` containing the "Macros" uppercase label and the `+` button). Directly above this block, insert the banner. The exact JSX structure and copy are specified in 07-UI-SPEC.md §"Component Inventory > New: `AutoSaveErrorBanner`" and 07-UI-SPEC.md §"Copywriting Contract" — follow those contracts exactly. The structure (described in prose; the executor reads 07-UI-SPEC.md for exact copy):

    Outer wrapper: a `<Show when={saveError()}>` guard that renders the banner only when the signal is truthy. Inside the Show, a single `<div id="auto-save-error-banner" class="bg-warning/10 border border-warning/20 rounded-lg p-3 flex items-center gap-3">` element. Inside the div, three children arranged in a flex row with `gap-3`: (1) a `<span class="text-warning text-base">⚠</span>` warning glyph; (2) a `<div class="flex-1">` content block containing a `<p class="text-xs font-medium text-warning">Save failed</p>` heading and a `<p class="text-[11px] text-text-dim">Your changes are not being saved. Check available disk space and file permissions.</p>` body; (3) a `<button onClick={() => setSaveError(false)} class="text-[11px] text-text-muted hover:text-text-main transition-colors duration-200 cursor-pointer">Dismiss</button>` text-only button (no icon-only variant per UI-SPEC §"Copywriting Contract" row "Auto-save error banner dismiss button").

    The banner sits at the top of the macro list area (above the "Macros" header but below the active-profile badge and active-application card). It is only rendered when `saveError()` is truthy. The Dismiss button sets `saveError(false)` to clear the banner; a subsequent `auto-save-error` event sets it back to true and re-shows the banner (D-14).

    Do NOT change any other parts of the dashboard. Do NOT touch the profiles tab. Do NOT change the engine toggle, the new-macro form, or the macro card rendering.
  </action>
  <verify>
    <automated>grep -n "auto-save-error" /Users/alvaro/AutoClicker/src/App.tsx | head -5</automated>
    <automated>grep -n "setSaveError" /Users/alvaro/AutoClicker/src/App.tsx | head -10</automated>
    <automated>grep -n "Save failed\|Your changes are not being saved" /Users/alvaro/AutoClicker/src/App.tsx | head -5</automated>
    <automated>grep -n "auto-save-error-banner" /Users/alvaro/AutoClicker/src/App.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep for "auto-save-error" in App.tsx returns ≥ 2 matches (the `listen` string and the banner id)
    - grep for "setSaveError" in App.tsx returns ≥ 3 matches (declaration, listener call, dismiss button call)
    - grep for "Save failed" in App.tsx returns 1 match (the banner heading)
    - grep for "Your changes are not being saved" in App.tsx returns 1 match (the banner body)
    - grep for "auto-save-error-banner" in App.tsx returns 1 match (the banner id)
    - The listener body does NOT contain a reference to `_event.payload` or any payload-derived string (D-13) — the listener body must be `(_event) => { setSaveError(true); }` only
    - The banner JSX is placed ABOVE the "Macros" section header (search context: the banner id appears earlier in the file than the "Macros" header text)
  </acceptance_criteria>
  <done>A `listen('auto-save-error', ...)` subscription in App.tsx sets a `saveError` signal; a persistent inline banner at the top of the macro list shows fixed user-friendly copy ("Save failed" + "Your changes are not being saved. Check available disk space and file permissions.") with a Dismiss button. The event payload is never displayed (D-13). Dismissing hides the banner; a subsequent event re-shows it (D-14).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust backend → frontend `auto-save-error` event | The event carries a Rust error string in its payload; the frontend MUST NOT surface the raw string (Information Disclosure per D-13) |
| Frontend → macOS System Settings | The opener plugin invokes `x-apple.systempreferences:...` URLs; these are platform-controlled deep links |
| Frontend → Tauri IPC | `invoke<boolean>(...)` calls accept no arguments in this plan; the new commands are pure status-query functions with no input-validation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information Disclosure | `auto-save-error` event payload rendered to user | mitigate | D-13: listener body ignores the payload and sets a fixed-copy banner; the Rust error string is never passed to the JSX |
| T-07-09 | Tampering | TCC identity-change copy driven by file existence | accept | The flag is advisory; tampering (deleting the flag) can only suppress the "AutoMux was updated" copy, not bypass any security boundary |
| T-07-10 | Denial of Service | 3s polling causes CPU/network overhead | accept | Two boolean invocations every 3s is negligible; Promise.all parallelizes them |
| T-07-SC | Tampering | npm/cargo installs | accept | No new packages installed in this plan; `@tauri-apps/plugin-opener` was already a dependency |
</threat_model>

<verification>
After both tasks complete:

1. `grep "check_input_monitoring" src/App.tsx` returns ≥ 1 match
2. `grep "get_tcc_identity_status" src/App.tsx` returns ≥ 1 match
3. `grep "handleRequestInputMonitoringAccess" src/App.tsx` returns ≥ 2 matches
4. `grep "tccIdentityChanged" src/App.tsx` returns ≥ 2 matches
5. `grep "@tauri-apps/plugin-opener" src/App.tsx` returns ≥ 1 match
6. `grep "x-apple.systempreferences:com.apple.preference.security?Privacy_ListenEvent" src/App.tsx` returns 1 match
7. `grep "AutoMux was updated" src/App.tsx` returns 1 match
8. `grep "auto-save-error" src/App.tsx` returns ≥ 2 matches
9. `grep "Save failed" src/App.tsx` returns 1 match
10. `grep "auto-save-error-banner" src/App.tsx` returns 1 match
11. The listener body must NOT contain a reference to `_event.payload` (D-13) — visual check: the listener arrow body should be `setSaveError(true);` only
12. `npm run build` (or `tsc --noEmit` if no build script exists) from /Users/alvaro/AutoClicker exits 0 with no new TypeScript errors
</verification>

<success_criteria>
- App.tsx imports `open` from `@tauri-apps/plugin-opener`
- Four new signals (`inputMonitoring`, `inputMonitoringPending`, `saveError`, `tccIdentityChanged`) are declared
- Initial fetch invokes `check_input_monitoring` and `get_tcc_identity_status` alongside the existing commands
- 3-second polling invokes `check_accessibility` and `check_input_monitoring` via Promise.all
- The solo Accessibility card is replaced with a combined Permissions card containing Accessibility and Input Monitoring rows
- The Accessibility row's sublabel swaps to the TCC identity-change copy when `tccIdentityChanged() && !accessibility()`
- The Input Monitoring row's status label reads "Warning" (not "Denied") when its probe fails
- The Input Monitoring Grant button opens the correct System Settings URL via `open()` from `@tauri-apps/plugin-opener`
- A `listen('auto-save-error', ...)` subscription sets `saveError(true)` without surfacing the payload
- A persistent banner at the top of the macro list shows the fixed user-friendly copy with a Dismiss button
- The TypeScript project compiles clean
</success_criteria>

<output>
Create .planning/phases/07-carry-work-platform-ci-safety/07-03-SUMMARY.md when done
</output>

## Artifacts this phase produces

> The plan-review-convergence source-grounding pass reads this section to exclude newly-created symbols from drift verification. Listed per-plan so each PLAN.md is self-contained.

### Plan 07-03 — Frontend (COMPAT-04, COMPAT-05, ERR-01)

- **New symbols (TypeScript / SolidJS — all in src/App.tsx):**
  - `import { open } from "@tauri-apps/plugin-opener";` — new module-level import on line 4 area.
  - `_imPendingTimeoutId: ReturnType<typeof setTimeout> | null` — module-scoped ref, mirrors the existing `_pendingTimeoutId` for the Input Monitoring 30s pending timeout.
  - Signal: `inputMonitoring: Accessor<boolean | null>` paired with `setInputMonitoring` — mirrors `accessibility`.
  - Signal: `inputMonitoringPending: Accessor<boolean>` paired with `setInputMonitoringPending` — mirrors `accessibilityPending`.
  - Signal: `saveError: Accessor<boolean>` paired with `setSaveError` — presence-only flag for the auto-save error banner.
  - Signal: `tccIdentityChanged: Accessor<boolean>` paired with `setTccIdentityChanged` — set true on initial fetch when `get_tcc_identity_status` returns true.
  - Function: `clearImPending()` — clears `inputMonitoringPending` and the `_imPendingTimeoutId` timeout. Mirrors the existing `clearPending()`.
  - Function: `handleRequestInputMonitoringAccess()` — async function. Sets `inputMonitoringPending(true)`, starts the 30s timeout, then calls `await open("x-apple.systempreferences:com.apple.preference.security?Privacy_ListenEvent")` from `@tauri-apps/plugin-opener`. Catches and console.error's any error.
- **New JSX (in src/App.tsx):**
  - Combined `<PermissionsCard>` — replaces the solo Accessibility card. Contains a `Permissions` header and two stacked rows: Accessibility and Input Monitoring. Each row has a status dot+label, a sublabel, and a conditional `Grant Access` button.
  - `<AutoSaveErrorBanner>` — full-width inline banner placed above the "Macros" section header. Contains a `⚠` icon, heading `Save failed`, body `Your changes are not being saved. Check available disk space and file permissions.`, and a `Dismiss` text button that sets `setSaveError(false)`.
- **Modified JSX (in src/App.tsx):**
  - The solo Accessibility card block (lines 487–535) is REPLACED by the new PermissionsCard.
  - The initial-fetch `Promise.all` (line 135 area) gains two new entries: `invoke<boolean>("check_input_monitoring")` and `invoke<boolean>("get_tcc_identity_status")`.
  - The 3s `setInterval` polling effect (line 168–182) is extended to call `check_input_monitoring` in parallel with `check_accessibility` via `Promise.all`. The `clearPending()` call is followed by a `clearImPending()` call.
  - A new `createEffect` after the existing `state-changed` listener (line 158–166 area) subscribes to `listen<string>("auto-save-error", ...)` with an `onCleanup` that removes the listener. The listener body sets `setSaveError(true)` and never reads the event payload.
- **New files (none).**
