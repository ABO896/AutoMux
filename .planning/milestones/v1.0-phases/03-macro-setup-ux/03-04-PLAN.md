---
phase: 03-macro-setup-ux
plan: "04"
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - src/App.tsx
  - src/keymap.ts
autonomous: true
requirements:
  - UX-01
gap_closure: true

must_haves:
  truths:
    - "On macOS, clicking a card key badge, pressing a key, and blurring updates both the hotkey binding and MacroConfig.trigger_key — the badge shows the new key name after edit"
    - "Pressing an unmapped key (numpad, PrintScreen, international) during key capture does not commit any key — capture stays active or is silently cancelled with no side-effect"
    - "A macro card with trigger_key === null shows a clickable 'Set key…' placeholder that starts key capture, allowing a trigger key to be assigned without deleting the macro"
  artifacts:
    - path: "src/keymap.ts"
      provides: "domKeycodeToNative returns number | null; null on miss"
      contains: "number | null"
    - path: "src/App.tsx"
      provides: "startCapture guards against null/0 nativeCode; handleCardSetTriggerKey invokes set_macro_trigger_key on macOS after bind_hotkey; card Show fallback renders Set key… placeholder"
      contains: "if (nativeCode === null || nativeCode === 0) return"
  key_links:
    - from: "src/App.tsx startCapture"
      to: "domKeycodeToNative"
      via: "null guard before onCommit"
      pattern: "nativeCode === null"
    - from: "src/App.tsx handleCardSetTriggerKey IS_MACOS branch"
      to: "invoke set_macro_trigger_key"
      via: "third invoke call after bind_hotkey"
      pattern: "set_macro_trigger_key.*trigger_key.*nativeCode"
    - from: "src/App.tsx card Show fallback"
      to: "startCapture + handleCardSetTriggerKey"
      via: "onClick on Set key… span"
      pattern: "Set key"
---

<objective>
Fix three blockers found in Phase 3 verification that prevent UX-01 from being fully satisfied.

Purpose: The key capture widget silently assigns CGKeyCode 0 (letter A) for unmapped keys; macOS card-edit never persists the new trigger key to MacroConfig; and key-less macro cards have no UI path to assign a trigger key.

Output: Updated src/keymap.ts (null-returning miss) and src/App.tsx (null guard in startCapture, third invoke in IS_MACOS branch, Set-key fallback in card Show).
</objective>

<execution_context>
@/Users/alvaro/AutoClicker/.claude/worktrees/nifty-tu-645334/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alvaro/AutoClicker/.claude/worktrees/nifty-tu-645334/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alvaro/AutoClicker/.claude/worktrees/nifty-tu-645334/.planning/PROJECT.md
@/Users/alvaro/AutoClicker/.claude/worktrees/nifty-tu-645334/.planning/ROADMAP.md
@/Users/alvaro/AutoClicker/.claude/worktrees/nifty-tu-645334/.planning/STATE.md

@/Users/alvaro/AutoClicker/.claude/worktrees/nifty-tu-645334/.planning/phases/03-macro-setup-ux/03-04-PLAN.md
@/Users/alvaro/AutoClicker/.claude/worktrees/nifty-tu-645334/.planning/phases/03-macro-setup-ux/03-VERIFICATION.md

<interfaces>
<!-- Key signatures and patterns the executor needs. Extracted from current source. -->

From src/keymap.ts (lines 180-183):
  export function domKeycodeToNative(code: string): number {
    const map = IS_MACOS ? DOM_KEYCODE_TO_CGKEYCODE : DOM_KEYCODE_TO_VK;
    return map[code] ?? 0;
  }

From src/App.tsx handleCardSetTriggerKey (lines 280-293):
  async function handleCardSetTriggerKey(id: string, nativeCode: number) {
    try {
      if (IS_MACOS) {
        await invoke("unbind_hotkey", { macro_id: id });
        await invoke("bind_hotkey", { macro_id: id, keycode: nativeCode, modifiers: 0 });
      } else {
        await invoke("set_macro_trigger_key", { id, trigger_key: nativeCode });
      }
      setEditingCardId(null);
      setEditingField(null);
    } catch (e) {
      console.error("Card trigger key update failed:", e);
    }
  }

From src/App.tsx startCapture (lines 250-277):
  function startCapture(onCommit: (nativeCode: number) => void) { ... }
  // Inside onKeyDown: const nativeCode = domKeycodeToNative(e.code); onCommit(nativeCode);

From src/App.tsx card Show (lines 779-816):
  <Show when={macro.trigger_key !== null}>
    <div class="flex items-center gap-1">
      <Show when={editingCardId() === macro.id && editingField() === "key"} fallback={
        <span ... onClick={() => { setEditingCardId(macro.id); setEditingField("key");
          startCapture((nativeCode) => handleCardSetTriggerKey(macro.id, nativeCode)); }}>
          {resolveKeyName(macro.trigger_key!)}
        </span>
      }>
        <span ...>Press… <span onClick={cancel}>✕</span></span>
      </Show>
      <span class="text-[10px] text-text-muted">({macro.trigger_mode})</span>
    </div>
  </Show>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix domKeycodeToNative null return and add startCapture null guard (CR-03)</name>
  <files>src/keymap.ts, src/App.tsx</files>
  <read_first>
    - src/keymap.ts — read full file to understand return type, JSDoc, and call context before changing the signature
    - src/App.tsx — read lines 1-10 (import section) and lines 250-277 (startCapture) to understand how domKeycodeToNative is imported and used before adding the guard
  </read_first>
  <action>
    In src/keymap.ts:
    - Change the return type annotation of domKeycodeToNative from `number` to `number | null`
    - Update the JSDoc comment block above domKeycodeToNative: replace "Returns 0 if the code string has no entry" with "Returns null if the code string has no entry"
    - Change the return expression from `map[code] ?? 0` to `map[code] ?? null`
    - The function signature becomes: `export function domKeycodeToNative(code: string): number | null`

    In src/App.tsx:
    - The startCapture function signature takes `onCommit: (nativeCode: number) => void` — do not change the callback type; the guard prevents null from ever reaching it
    - Inside the onKeyDown handler in startCapture, after line `const nativeCode = domKeycodeToNative(e.code);` and before `onCommit(nativeCode)`, add an early-return guard:
      `if (nativeCode === null || nativeCode === 0) return;`
    - Do not call setTriggerKeyRecording(false) or remove the listener in this guard path — capture stays active so the user can press a different key
    - The TypeScript compiler will require this guard: after it, nativeCode is narrowed to `number` (not null), so onCommit(nativeCode) remains type-safe with no cast needed
  </action>
  <verify>
    <automated>cd /Users/alvaro/AutoClicker/.claude/worktrees/nifty-tu-645334 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/keymap.ts line for return expression reads: `return map[code] ?? null;`
    - src/keymap.ts function signature reads: `export function domKeycodeToNative(code: string): number | null`
    - src/App.tsx startCapture contains the line: `if (nativeCode === null || nativeCode === 0) return;` between the domKeycodeToNative call and the onCommit call
    - `npx tsc --noEmit` exits 0 with no type errors
  </acceptance_criteria>
  <done>domKeycodeToNative returns null on miss; startCapture silently ignores unmapped/zero keycodes; TypeScript strict-mode compilation passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Persist trigger_key on macOS card-edit and add Set-key fallback (CR-02 + WR-02)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx — read lines 279-293 (handleCardSetTriggerKey) to confirm the current IS_MACOS branch before adding the third invoke
    - src/App.tsx — read lines 779-817 (card Show block) to understand the full outer/inner Show structure before adding the fallback branch
    - src/App.tsx — read lines 784-793 (existing badge span) to copy the exact onClick closure pattern and Tailwind classes for use in the fallback span
  </read_first>
  <action>
    Fix 1 — CR-02: In handleCardSetTriggerKey, inside the IS_MACOS branch, add a third invoke call after `invoke("bind_hotkey", ...)` completes:
      `await invoke("set_macro_trigger_key", { id, trigger_key: nativeCode });`
    The IS_MACOS branch must now contain three sequential awaited invokes: unbind_hotkey, bind_hotkey, set_macro_trigger_key. The Windows branch is unchanged. The function parameter nativeCode is already `number`; after Task 1 the null guard in startCapture means nativeCode is always a valid non-zero integer here.

    Fix 2 — WR-02: The outer `<Show when={macro.trigger_key !== null}>` currently has no fallback prop. Convert it to use an explicit fallback that renders a clickable "Set key…" placeholder. The outer Show becomes:
      `<Show when={macro.trigger_key !== null} fallback={<fallback-content>}>`

    The fallback content must:
    - Be a `<div class="flex items-center gap-1">` wrapper (same as the when=true branch)
    - Contain a `<span>` with:
      - classes: `px-1.5 py-0.5 rounded border border-dashed border-border text-[10px] font-mono text-text-dim cursor-pointer hover:border-accent/40 hover:text-text-main`
      - onClick handler that: calls `setEditingCardId(macro.id)`, calls `setEditingField("key")`, then calls `startCapture((nativeCode) => handleCardSetTriggerKey(macro.id, nativeCode))`
      - text content: `Set key…`
    - Contain the trigger_mode span: `<span class="text-[10px] text-text-muted">({macro.trigger_mode})</span>`

    The inner Show (when editingCardId === macro.id && editingField === "key") that renders "Press…" does NOT move; it stays inside the when=true branch. The fallback only shows the "Set key…" placeholder without a capture-in-progress state — clicking it sets editing state and starts capture, so the outer Show will re-evaluate when trigger_key changes after commit.
  </action>
  <verify>
    <automated>cd /Users/alvaro/AutoClicker/.claude/worktrees/nifty-tu-645334 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx handleCardSetTriggerKey IS_MACOS branch contains three awaited invoke calls in order: `unbind_hotkey`, `bind_hotkey`, `set_macro_trigger_key`
    - `invoke("set_macro_trigger_key", { id, trigger_key: nativeCode })` appears inside `if (IS_MACOS)` in handleCardSetTriggerKey
    - src/App.tsx outer Show on line ~779 has a `fallback=` prop
    - The fallback content contains a span with text `Set key…`
    - The fallback span onClick calls startCapture with a callback that calls handleCardSetTriggerKey
    - `npx tsc --noEmit` exits 0 with no type errors
  </acceptance_criteria>
  <done>macOS card-edit persists trigger_key to StateActor via set_macro_trigger_key; macros with trigger_key === null show a clickable Set-key placeholder in their card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keydown event → nativeCode | User keyboard input crosses from DOM event into platform keycode lookup |
| nativeCode → invoke set_macro_trigger_key | Integer keycode crosses from frontend into Rust StateActor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | domKeycodeToNative | mitigate | Null guard in startCapture prevents committing null or 0; valid keycodes only reach onCommit |
| T-03-04-02 | Denial of Service | startCapture null-path | accept | Capture stays active on unrecognized key — no resource leak; listener already attached, no additional cost |
| T-03-04-03 | Spoofing | handleCardSetTriggerKey | accept | IPC call authenticated by Tauri's same-process model; no external caller can forge invoke() |
</threat_model>

<verification>
Run TypeScript type-check after both tasks:
```
cd /Users/alvaro/AutoClicker/.claude/worktrees/nifty-tu-645334
npx tsc --noEmit
```
Expected: exits 0 with no errors.

Source assertions:
- `grep -n "number | null" src/keymap.ts` — must return at least one match on the domKeycodeToNative signature line
- `grep -n "map\[code\] ?? null" src/keymap.ts` — must return one match
- `grep -n "nativeCode === null || nativeCode === 0" src/App.tsx` — must return one match inside startCapture
- `grep -n "set_macro_trigger_key" src/App.tsx` — must return two matches: one in the IS_MACOS branch of handleCardSetTriggerKey, one in the Windows branch
- `grep -n "Set key" src/App.tsx` — must return one match in the outer Show fallback
</verification>

<success_criteria>
- CR-02 fixed: handleCardSetTriggerKey IS_MACOS branch calls invoke("set_macro_trigger_key") after invoke("bind_hotkey"), so MacroConfig.trigger_key is updated in StateActor and the state broadcast carries the new keycode
- CR-03 fixed: domKeycodeToNative returns null on miss; startCapture skips onCommit when nativeCode is null or 0; no unmapped key can silently set the trigger to CGKeyCode 0
- WR-02 fixed: macro cards with trigger_key === null render a clickable "Set key…" placeholder that starts key capture via the existing handleCardSetTriggerKey flow
- TypeScript compilation passes with no errors after both changes
</success_criteria>

<output>
After completion, create `.planning/phases/03-macro-setup-ux/03-04-SUMMARY.md`
</output>
