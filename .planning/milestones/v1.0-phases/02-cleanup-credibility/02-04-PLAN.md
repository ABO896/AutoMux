---
phase: 02-cleanup-credibility
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
autonomous: true
requirements: [README-03]
tags: [frontend, solidjs, version-display, getversion, comment-fix]
must_haves:
  truths:
    - "The footer/version display reads the runtime version from @tauri-apps/api/app via getVersion() instead of a hardcoded 'v1.0.0'"
    - "The displayed version stays in sync with tauri.conf.json / package.json automatically"
    - "The async fetch follows the established createEffect + Promise.all + cancelled-flag pattern already used in App.tsx"
    - "The polling-interval comment in App.tsx matches the actual setInterval value"
    - "tsc --noEmit passes clean"
  artifacts:
    - path: "src/App.tsx"
      provides: "getVersion() import, appVersion signal, Promise.all extension, JSX binding, corrected polling comment"
      contains: "getVersion"
  key_links:
    - from: "App.tsx footer JSX (around line 300)"
      to: "appVersion() signal"
      via: "{appVersion()} interpolation"
      pattern: "v\\{appVersion\\(\\)\\}"
    - from: "App.tsx initial createEffect (around line 95)"
      to: "@tauri-apps/api/app getVersion"
      via: "added entry in existing Promise.all"
      pattern: "getVersion\\(\\)"
---

<objective>
Replace the hardcoded `v1.0.0` version string in the AutoMux footer with a runtime call to Tauri's `getVersion()` so the displayed version stays in sync with `tauri.conf.json` and `package.json` automatically. This is the credibility slice for the frontend: users see the real shipped version, not a stale literal. Bundled with the same App.tsx pass: fix the trivial polling-interval comment mismatch flagged in RESEARCH.md Open Question #3.

Purpose: D-10 in CONTEXT.md (the version string fix is recorded under README-03 scope as a credibility/clarity item). Removes a visible source of incorrectness that contradicts the actual app version.
Output: `src/App.tsx` updated to import `getVersion`, add an `appVersion` signal, extend the existing `Promise.all` initial fetch, bind the signal in the footer JSX, and correct the polling comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cleanup-credibility/02-CONTEXT.md
@.planning/phases/02-cleanup-credibility/02-RESEARCH.md
@.planning/phases/02-cleanup-credibility/02-PATTERNS.md
@src/App.tsx

<interfaces>
<!-- Extracted from src/App.tsx and Tauri docs — established patterns to copy. -->

Tauri API:
- `import { getVersion } from "@tauri-apps/api/app";`
- `getVersion(): Promise<string>` — returns the version declared in tauri.conf.json
- Already in project dependencies (@tauri-apps/api ^2); no install needed

Existing App.tsx import block (lines 1–4):
- `import { createSignal, createEffect, onCleanup, Show, For } from "solid-js";`
- `import { invoke } from "@tauri-apps/api/core";`
- `import { listen } from "@tauri-apps/api/event";`
- `import "./App.css";`

Existing signal declaration style (lines 68–91):
- `const [state, setState] = createSignal<AppState | null>(null);`
- `const [accessibility, setAccessibility] = createSignal<boolean | null>(null);`
- Pattern to follow: `const [appVersion, setAppVersion] = createSignal<string>("…");`

Existing initial createEffect Promise.all (lines 95–119):
- Inside `createEffect(() => { let cancelled = false; onCleanup(...); (async () => { ... })(); });`
- Promise.all currently returns `[stateData, accessOk, app, profileList]`
- All set-calls are gated by `if (cancelled) return;` before any setSignal()

Current hardcoded footer (line 300 area):
- `<span class="text-[10px] text-text-dim font-mono">v1.0.0</span>`

Polling comment site (line 132 area per PATTERNS.md):
- Comment claims "Poll accessibility every 3s" but setInterval value is 10000ms (10s)
- Fix the comment to match the actual setInterval value — DO NOT change the timer value
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire getVersion() into App.tsx footer and fix polling comment</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (full file — read end-to-end; identify exact line numbers for import block, signal declarations, initial Promise.all createEffect, polling-interval comment, and the hardcoded `v1.0.0` footer span)
    - .planning/phases/02-cleanup-credibility/02-PATTERNS.md (src/App.tsx section — exact import line to add, exact Promise.all extension, exact JSX replacement)
    - .planning/phases/02-cleanup-credibility/02-RESEARCH.md (Pattern 2: SolidJS Async Init; Pitfall 5: getVersion() outside async context; Open Question #3 on comment fix)
    - .planning/phases/02-cleanup-credibility/02-CONTEXT.md (D-10)
    - tsconfig.json (confirm strict + noUnusedLocals + noUnusedParameters are enabled — guards against leaving the new signal unused)
  </read_first>
  <action>
    Implements D-10 (version string) plus the comment-fix recommendation from RESEARCH.md Open Question #3. Single-file edit to `src/App.tsx`.

    1. Add the import `import { getVersion } from "@tauri-apps/api/app";` to the existing import block at the top of the file (after the existing `@tauri-apps/api/event` line is fine).

    2. Add a new signal alongside the existing top-level signal declarations: `const [appVersion, setAppVersion] = createSignal<string>("…");` (ellipsis as loading placeholder, matching the visual style of other loading states). Place it near the other primary state signals (around lines 68–91 per PATTERNS.md).

    3. Extend the existing initial-fetch `createEffect`'s `Promise.all` (around lines 95–119 per PATTERNS.md) to include `getVersion()` as an additional awaitable. The destructuring becomes `[stateData, accessOk, app, profileList, version]`. After the existing `if (cancelled) return;` guard, add `setAppVersion(version);` alongside the other setSignal calls. Do NOT introduce a second `createEffect` for this — extending the existing Promise.all is the cleaner pattern per RESEARCH.md.

    4. Replace the hardcoded footer span (line ~300):
       - Current: `<span class="text-[10px] text-text-dim font-mono">v1.0.0</span>`
       - Replacement: `<span class="text-[10px] text-text-dim font-mono">v{appVersion()}</span>`

    5. Fix the polling-interval comment mismatch (around line 132 per PATTERNS.md). Read the actual `setInterval(..., N)` value on the nearby line and ALIGN THE COMMENT TO THE CODE. If the timer is `3000`, the comment is correct as `Poll accessibility every 3s`. If the timer is `10000`, change the comment to `Poll accessibility every 10s`. DO NOT change the timer value itself — the polling cadence is a separate concern out of this phase's scope per CONTEXT.md.

    Do NOT modify any other line in App.tsx. Do NOT remove the intentional Phase 3 scaffolding path (`{ Key: parseInt(inputVal) || 0 }` in `handleCreateMacro`) — per RESEARCH.md it stays for Phase 3 UX-01. Do NOT add a new Rust IPC command for version (per RESEARCH.md anti-pattern — `getVersion()` is built into Tauri core).
  </action>
  <verify>
    <automated>grep -q 'import { getVersion } from "@tauri-apps/api/app"' src/App.tsx &amp;&amp; grep -q "createSignal<string>" src/App.tsx &amp;&amp; grep -q "setAppVersion(version)" src/App.tsx &amp;&amp; grep -q "v{appVersion()}" src/App.tsx &amp;&amp; ! grep -q ">v1.0.0<" src/App.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { getVersion } from "@tauri-apps/api/app"' src/App.tsx` equals 1
    - `grep -c "const \\[appVersion, setAppVersion\\] = createSignal" src/App.tsx` equals 1
    - `grep -c "getVersion()" src/App.tsx` is at least 1 (inside the Promise.all)
    - `grep -c "setAppVersion(version)" src/App.tsx` equals 1
    - `grep -c "v{appVersion()}" src/App.tsx` equals 1
    - `grep -c ">v1.0.0<" src/App.tsx` equals 0 (hardcoded literal removed)
    - The new `setAppVersion(version)` call appears AFTER the existing `if (cancelled) return;` guard inside the initial createEffect (verified by reading)
    - No new top-level `createEffect(` block was added — `grep -c "createEffect(" src/App.tsx` equals the prior count (extension is in-place)
    - The polling-interval comment near line 132 numerically matches the adjacent `setInterval(..., N)` value (verified by reading both)
    - `npx tsc --noEmit` exits 0 with no output (no unused locals introduced; noUnusedLocals strict mode would catch a stray `appVersion`)
    - `npm run build` exits 0 (frontend compiles)
  </acceptance_criteria>
  <done>getVersion imported; appVersion signal added; Promise.all extended with getVersion() and setAppVersion(version) call gated by the cancelled flag; footer JSX bound to v{appVersion()}; hardcoded v1.0.0 removed; polling comment aligned to actual interval; tsc --noEmit clean; npm run build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri Rust core → JS frontend | `getVersion()` reads the version declared in `tauri.conf.json` and returns it across the IPC bridge. The returned string is application metadata, not user input. |
| Frontend signal → DOM | `appVersion()` is interpolated into JSX; SolidJS escapes text by default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering (XSS via version string) | `{appVersion()}` interpolation in footer | accept | The version string originates from `tauri.conf.json` (developer-controlled at build time), not user input. SolidJS auto-escapes text interpolations. No HTML injection vector. |
| T-02-13 | Information disclosure (Promise leak rendered as `[object Promise]`) | Missing await of getVersion | mitigate | Task 1 places `getVersion()` inside an existing `Promise.all` that is already awaited, with the result destructured into a variable before being passed to `setAppVersion`. Acceptance criteria pin the `setAppVersion(version)` call site after the await. |
| T-02-14 | Denial of service (unhandled rejection if getVersion fails) | createEffect catch | mitigate | The existing initial-fetch createEffect already has a try/catch with `console.error` and `cancelled` guards; extending its Promise.all reuses that error handling. No new error path introduced. |
| T-02-15 | Repudiation (footer shows wrong version after release) | Hardcoded `v1.0.0` literal | mitigate | Task 1 removes the literal entirely and binds to runtime `getVersion()`; acceptance criteria assert `>v1.0.0<` no longer appears anywhere in App.tsx. |
</threat_model>

<verification>
- `grep -c ">v1.0.0<" src/App.tsx` returns 0
- `grep -c "v{appVersion()}" src/App.tsx` returns 1
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- Manual: launch dev build (`npm run tauri dev` if available), open footer — version displayed matches the value in `tauri.conf.json` (currently 1.1.0 per RESEARCH.md)
- Polling-interval comment and adjacent setInterval value agree
</verification>

<success_criteria>
- App.tsx imports getVersion from @tauri-apps/api/app
- appVersion signal declared with typed createSignal<string>
- getVersion() awaited inside the existing initial Promise.all (no new createEffect)
- Footer JSX shows `v{appVersion()}` — no hardcoded version literal anywhere in App.tsx
- Polling comment numerically matches adjacent setInterval value
- tsc --noEmit and npm run build both pass clean
- No new IPC command added; no other App.tsx logic touched
</success_criteria>

<output>
After completion, create `.planning/phases/02-cleanup-credibility/02-04-SUMMARY.md` with the exact diff applied, the resolved polling-interval value and matching comment text, and the tsc + build exit codes.
</output>
