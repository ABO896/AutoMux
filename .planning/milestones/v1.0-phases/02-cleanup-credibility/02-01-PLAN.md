---
phase: 02-cleanup-credibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - LICENSE
  - package.json
  - src-tauri/Cargo.toml
autonomous: true
requirements: [README-01, README-02, README-03]
tags: [license, readme, gpl, repository-metadata]
must_haves:
  truths:
    - "README contains no 'MIT License' string anywhere in the document"
    - "README displays a GPL-3.0-only shields.io badge that does not depend on GitHub's license cache"
    - "package.json, Cargo.toml, and the LICENSE file all agree on GPL-3.0-only"
    - "README opening paragraph leads with what AutoMux does for the user, not jargon-heavy performance claims"
    - "README feature list contains no Phase 3 features (no key capture, no process picker UX claims beyond what is implemented)"
  artifacts:
    - path: "README.md"
      provides: "Corrected license prose + GPL-3.0-only badge + polished opening + audited feature list"
      contains: "GPL-3.0-only"
    - path: "LICENSE"
      provides: "Full GPL-3.0 license text at repository root"
      contains: "GNU GENERAL PUBLIC LICENSE"
    - path: "package.json"
      provides: "license field set to GPL-3.0-only"
      contains: "\"license\": \"GPL-3.0-only\""
    - path: "src-tauri/Cargo.toml"
      provides: "license field set to GPL-3.0-only in [package]"
      contains: "license = \"GPL-3.0-only\""
  key_links:
    - from: "README.md badge line"
      to: "shields.io static SPDX badge URL"
      via: "img.shields.io/badge/License-GPL--3.0--only-blue"
      pattern: "img\\.shields\\.io/badge/License-GPL"
    - from: "README.md prose"
      to: "LICENSE file"
      via: "'See `LICENSE` for more information.'"
      pattern: "See `LICENSE`"
---

<objective>
Restore legal correctness and improve README appeal in a single shippable slice. This phase delivers the "License is GPL-3.0-only everywhere it is declared, and the README accurately reflects what AutoMux is" outcome end-to-end: prose, badge, package manifests, license file, opening paragraph, and feature list.

Purpose: README-01, README-02, README-03 in CONTEXT.md and ROADMAP success criterion #1. Per RESEARCH.md the LICENSE file is missing and `package.json` still declares MIT — both must be corrected alongside the README prose to avoid a partial/contradictory state being shipped.
Output: README.md (license prose + badge + opening + feature list polished), LICENSE (new, GPL-3.0 full text), package.json (license field), src-tauri/Cargo.toml (license field).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cleanup-credibility/02-CONTEXT.md
@.planning/phases/02-cleanup-credibility/02-RESEARCH.md
@.planning/phases/02-cleanup-credibility/02-PATTERNS.md
@README.md
@package.json
@src-tauri/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LICENSE file and align package manifests to GPL-3.0-only</name>
  <files>LICENSE, package.json, src-tauri/Cargo.toml</files>
  <read_first>
    - package.json (current license field is "MIT" — confirm before editing)
    - src-tauri/Cargo.toml (currently has no license field in [package]; confirm)
    - .planning/phases/02-cleanup-credibility/02-RESEARCH.md (Critical Finding: Missing LICENSE File section + Open Question #1)
    - .planning/phases/02-cleanup-credibility/02-PATTERNS.md (LICENSE, package.json, Cargo.toml sections)
  </read_first>
  <action>
    Implements README-01 license-metadata consistency (D-01).

    1. Create `LICENSE` at repository root containing the full verbatim text of the GNU General Public License, Version 3, 29 June 2007, as published by the Free Software Foundation. Source the canonical text from https://www.gnu.org/licenses/gpl-3.0.txt. The file MUST start with the line `                    GNU GENERAL PUBLIC LICENSE` and contain the standard preamble, terms and conditions, and "How to Apply These Terms" appendix. Do NOT abridge.
    2. In `package.json`, change the value of the `"license"` field from `"MIT"` to `"GPL-3.0-only"`. Keep all other fields, ordering, and formatting (2-space indent) unchanged.
    3. In `src-tauri/Cargo.toml`, add `license = "GPL-3.0-only"` inside the existing `[package]` section (per RESEARCH.md Open Question #1 recommendation — keeps all three manifests consistent). Place it adjacent to other package-level metadata fields (e.g., after `version` or `description`). Do NOT modify dependencies or other sections.
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q "GNU GENERAL PUBLIC LICENSE" LICENSE && grep -q "Version 3" LICENSE && grep -q '"license": "GPL-3.0-only"' package.json && grep -q 'license = "GPL-3.0-only"' src-tauri/Cargo.toml &amp;&amp; ! grep -q '"license": "MIT"' package.json &amp;&amp; cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f LICENSE` exits 0
    - `grep -c "GNU GENERAL PUBLIC LICENSE" LICENSE` is at least 1
    - `grep -c "Version 3" LICENSE` is at least 1
    - LICENSE byte length is at least 30000 (full GPL-3.0 text, not a stub)
    - `grep -c '"license": "GPL-3.0-only"' package.json` equals 1
    - `grep -c '"license": "MIT"' package.json` equals 0
    - `grep -c 'license = "GPL-3.0-only"' src-tauri/Cargo.toml` equals 1
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0 (no manifest parse error)
  </acceptance_criteria>
  <done>LICENSE exists with full GPL-3.0 text; package.json and Cargo.toml both declare GPL-3.0-only; cargo check passes; no MIT string remains in either manifest.</done>
</task>

<task type="auto">
  <name>Task 2: Update README prose, badge, opening paragraph, and feature list</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — read fully; targeted edits at lines 8, 11, 79 and feature list)
    - .planning/phases/02-cleanup-credibility/02-CONTEXT.md (D-01 through D-04, Claude's Discretion section)
    - .planning/phases/02-cleanup-credibility/02-PATTERNS.md (README.md section — exact strings to replace at lines 8 and 79; opening paragraph guidance)
    - .planning/phases/02-cleanup-credibility/02-RESEARCH.md (Pattern 3: shields.io SPDX Badge; Pitfall 3 on GitHub cache)
    - .planning/PROJECT.md (Core Value statement — use as anchor for opening paragraph)
  </read_first>
  <action>
    Implements README-01 prose (D-01), README-02 badge (D-02), README-03 polish pass (D-03). Single-file edit to README.md.

    1. License prose (line 79 area): replace the sentence `Distributed under the MIT License. See \`LICENSE\` for more information.` with `Distributed under the GNU General Public License v3. See \`LICENSE\` for more information.` Search and replace every occurrence of the literal string `MIT License` in the document — there must be zero remaining.

    2. License badge (line 8 area): replace the existing GitHub auto-license badge anchor (`<a href="https://github.com/ABO896/AutoMux/blob/master/LICENSE"><img src="https://img.shields.io/github/license/ABO896/AutoMux?style=flat-square" alt="License"></a>`) with an explicit shields.io SPDX badge that does not depend on GitHub's license-detection cache:
       `<a href="https://spdx.org/licenses/GPL-3.0-only.html"><img src="https://img.shields.io/badge/License-GPL--3.0--only-blue?style=flat-square" alt="License: GPL-3.0-only"></a>`

    3. Opening paragraph polish (per D-03 area "opening paragraph"): rewrite the current jargon-heavy lead ("A native, cross-platform macro engine architected for $O(1)$ speed and 0% idle CPU overhead...") into a 2–3 sentence description that leads with what AutoMux does for the user and then names the technical qualities that back it. Target audience: gamers, productivity power users, anyone automating repetitive mouse/keyboard tasks. Mention macOS and Windows support explicitly (per D-03 area "platform info"). Wording is at Claude's discretion per D-03; structure rule: user value first, technical claims second.

    4. Feature list audit (per D-03 area "feature list accuracy"): walk every bullet in the current feature list. Each bullet MUST describe a feature that is actually implemented in the current codebase. Do NOT add bullets for Phase 3 features (key capture widget, running-process picker UX). Per PATTERNS.md, the existing bullets (Zero Polling Architecture, O(1) Hotkey Routing, Latched Triggering, Process Detection Parity, Ultra-Lightweight, Emergency Failsafes) correctly reflect implemented behavior — verify each is still accurate, fix wording if any is stale; remove any bullet that overstates current capability.

    5. Badges audit (per D-03 area "badges audit"): verify the Release badge (`img.shields.io/github/v/release/...`) and Build status badge (`img.shields.io/github/actions/workflow/status/...`) still point to valid URLs and render. Keep both. The license badge is replaced per step 2. Remove any badge that links to a broken URL.

    Do NOT add new top-level sections (per D-03). Do NOT add screenshots or demo GIFs (per D-04). Do NOT add Phase 3 feature descriptions.
  </action>
  <verify>
    <automated>grep -c "MIT License" README.md | grep -qx 0 &amp;&amp; grep -q "img.shields.io/badge/License-GPL--3.0--only-blue" README.md &amp;&amp; grep -q "GNU General Public License v3" README.md &amp;&amp; grep -qi "macOS" README.md &amp;&amp; grep -qi "Windows" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MIT License" README.md` equals 0
    - `grep -c "MIT" README.md` equals 0 (no stray MIT mentions in any casing context)
    - `grep -c "img.shields.io/badge/License-GPL--3.0--only-blue" README.md` is at least 1
    - `grep -c "GNU General Public License v3" README.md` is at least 1
    - `grep -c "img.shields.io/github/license/ABO896" README.md` equals 0 (old GitHub auto-badge removed)
    - README contains both substrings `macOS` and `Windows` (platform compatibility called out per D-03)
    - README does NOT contain the strings `key capture widget`, `process picker`, or any Phase 3 feature claim (per D-03 scope discipline)
    - README opening line is not the previous jargon lead — `grep -c "architected for \\\$O(1)\\\$ speed" README.md` equals 0
    - Existing Release and Build-status badge anchors are still present (`grep -c "img.shields.io/github/v/release" README.md` is at least 1 and `grep -c "img.shields.io/github/actions/workflow/status" README.md` is at least 1)
  </acceptance_criteria>
  <done>README contains zero "MIT" mentions, displays the explicit shields.io GPL-3.0-only badge, has a user-focused opening paragraph, calls out macOS and Windows support, and the feature list does not advertise Phase 3 features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → public consumers | README, LICENSE, package.json are visible to all downstream consumers (GitHub, npm tooling, crates.io if published). License declarations carry legal weight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure (legal/integrity) | License declaration | mitigate | Align README prose, LICENSE file, package.json, and Cargo.toml on GPL-3.0-only in a single plan so no intermediate commit ships a contradiction (per Task 1 + Task 2 atomicity). |
| T-02-02 | Tampering (badge spoofing) | shields.io badge URL | accept | Badge URL points to shields.io static endpoint with no user-controllable data. shields.io is a trusted third-party service in standard use across the OSS ecosystem. No PII or credentials flow through it. |
| T-02-03 | Repudiation (license ambiguity) | Missing LICENSE file | mitigate | Task 1 creates the LICENSE file with full GPL-3.0 verbatim text, eliminating ambiguity that previously existed between README prose and missing license file. |
</threat_model>

<verification>
- `grep -c "MIT" README.md package.json` returns 0 for both files
- `grep -c "GPL-3.0" LICENSE README.md package.json src-tauri/Cargo.toml` returns at least 1 for each file
- `cargo check --manifest-path src-tauri/Cargo.toml` exits 0
- Manually open README.md in a Markdown preview — license badge renders the GPL-3.0-only chip
</verification>

<success_criteria>
- LICENSE file exists at repo root with full GPL-3.0 text (≥ 30 KB)
- README displays explicit shields.io GPL-3.0-only badge; old GitHub auto-badge removed
- README prose says "GNU General Public License v3"; zero "MIT License" or "MIT" strings remain
- package.json `"license"` field is `"GPL-3.0-only"`
- src-tauri/Cargo.toml `[package]` has `license = "GPL-3.0-only"`
- README opening paragraph leads with user value; macOS + Windows are explicitly named
- Feature list contains no Phase 3 feature claims
- All four files (README.md, LICENSE, package.json, src-tauri/Cargo.toml) committed together
</success_criteria>

<output>
After completion, create `.planning/phases/02-cleanup-credibility/02-01-SUMMARY.md`
</output>
