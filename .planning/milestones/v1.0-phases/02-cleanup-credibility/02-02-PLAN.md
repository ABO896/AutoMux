---
phase: 02-cleanup-credibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - implementation_plan.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03]
tags: [cleanup, gitignore, repository-hygiene, dead-code]
must_haves:
  truths:
    - "implementation_plan.md is absent from the repository root"
    - ".gitignore covers Thumbs.db and *.log in addition to existing entries"
    - "Rust source tree has zero dead-code warnings under cargo check + clippy"
    - "TypeScript source tree has zero unused-locals / unused-parameters warnings under tsc --noEmit"
    - "AUDIT-02 audit result is recorded in the plan SUMMARY for traceability"
  artifacts:
    - path: ".gitignore"
      provides: "Coverage for Thumbs.db (Windows OS artifact) and *.log (debug temp files)"
      contains: "Thumbs.db"
    - path: "implementation_plan.md"
      provides: "ABSENT — file deleted from repo root"
      contains: ""
  key_links:
    - from: ".gitignore"
      to: "OS-artifact and debug-log patterns"
      via: "newly appended entries"
      pattern: "Thumbs\\.db|\\*\\.log"
---

<objective>
Deliver the "repository contains no stale artifacts and no untracked debug junk" slice end-to-end in a single shippable plan: remove `implementation_plan.md`, extend `.gitignore` to cover the two confirmed gaps (`Thumbs.db`, `*.log`), and verify (and record) that both the Rust and TypeScript layers have zero dead code today.

Purpose: AUDIT-01, AUDIT-02, AUDIT-03 from CONTEXT.md (D-07, D-08, D-09); ROADMAP success criterion #2 ("implementation_plan.md absent; src-tauri/gen/ and debug temp files covered by .gitignore") and #3 ("no unused source files remain").
Output: `implementation_plan.md` deleted, `.gitignore` extended, AUDIT-02 verification recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cleanup-credibility/02-CONTEXT.md
@.planning/phases/02-cleanup-credibility/02-RESEARCH.md
@.planning/phases/02-cleanup-credibility/02-PATTERNS.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete implementation_plan.md and extend .gitignore for Thumbs.db + *.log</name>
  <files>implementation_plan.md, .gitignore</files>
  <read_first>
    - .gitignore (current 22 lines — confirm `.DS_Store` already present, `Thumbs.db` and `*.log` not present)
    - src-tauri/.gitignore (confirm `/gen/schemas` already covered there — do NOT duplicate in root)
    - .planning/phases/02-cleanup-credibility/02-RESEARCH.md (.gitignore Gap Analysis section — exact gaps are Thumbs.db and *.log only)
    - .planning/phases/02-cleanup-credibility/02-PATTERNS.md (.gitignore section — append target patterns)
  </read_first>
  <action>
    Implements AUDIT-01 (D-07) and AUDIT-03 (D-09).

    1. Delete `implementation_plan.md` from the repository root outright (per RESEARCH.md Open Question #2 recommendation — `.planning/` directory now serves this purpose; do NOT move to `archive/`). Use `git rm implementation_plan.md` so the deletion is staged.

    2. Append the following two entries to the root `.gitignore`, grouped with comments. Place them after the existing `# OS / Environment` block or as a new trailing block — formatting is Claude's discretion per CONTEXT.md, but the literal strings `Thumbs.db` and `*.log` MUST each appear on their own line:
       - `Thumbs.db` (Windows OS artifact; counterpart to existing `.DS_Store`)
       - `*.log` (debug / temp log files)

       Do NOT add `src-tauri/gen/` to the root `.gitignore` — it is already covered by `src-tauri/.gitignore` (`/gen/schemas`); duplicating would be noise. Do NOT remove or reorder existing entries.

    3. Confirm via `git check-ignore` that both new patterns are honoured (the executor will run this in verify).
  </action>
  <verify>
    <automated>test ! -f implementation_plan.md &amp;&amp; grep -q "^Thumbs.db$" .gitignore &amp;&amp; grep -q "^\*\.log$" .gitignore &amp;&amp; git check-ignore -q Thumbs.db &amp;&amp; git check-ignore -q debug.log</automated>
  </verify>
  <acceptance_criteria>
    - `test -f implementation_plan.md` exits non-zero (file absent)
    - `git ls-files implementation_plan.md` produces empty output (not tracked)
    - `grep -c "^Thumbs.db$" .gitignore` equals 1
    - `grep -c "^\*\.log$" .gitignore` equals 1
    - `grep -c "^\.DS_Store$" .gitignore` equals 1 (existing entry preserved)
    - `grep -c "^node_modules/$" .gitignore` equals 1 (existing entry preserved)
    - `git check-ignore -q Thumbs.db` exits 0
    - `git check-ignore -q somefile.log` exits 0
    - `git check-ignore -q README.md` exits non-zero (not over-broadly ignoring tracked files)
  </acceptance_criteria>
  <done>implementation_plan.md deleted (staged via git rm); .gitignore contains Thumbs.db and *.log entries; git check-ignore confirms both patterns active; pre-existing entries preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and record AUDIT-02 dead-code clean state (Rust + TypeScript)</name>
  <files>.planning/phases/02-cleanup-credibility/02-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/02-cleanup-credibility/02-RESEARCH.md (Dead Code Audit Results section — Rust and TS both already pass clean per RESEARCH; this task re-confirms at execution time and records evidence)
    - .planning/phases/02-cleanup-credibility/02-CONTEXT.md (D-08 scope: source files only; do NOT audit Cargo dependencies)
    - tsconfig.json (confirm noUnusedLocals: true and noUnusedParameters: true still enabled — guards the TS audit)
    - src-tauri/Cargo.toml (so executor knows the manifest path for cargo commands)
  </read_first>
  <action>
    Implements AUDIT-02 (D-08). Per RESEARCH.md, both compilers pass clean today. This task RE-VERIFIES at execution time (so the audit reflects the post-Phase-1 codebase as actually shipped) and records the evidence in the plan SUMMARY for traceability.

    1. Run `RUSTFLAGS="-Wdead-code" cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tee /tmp/02-02-rust-audit.log` and capture the full output. Expected: zero warnings, zero errors.
    2. Run `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings 2>&1 | tee /tmp/02-02-clippy.log`. Expected: zero warnings (clippy treated as errors via `-D warnings`).
    3. Run `npx tsc --noEmit 2>&1 | tee /tmp/02-02-ts-audit.log`. Expected: exit 0, zero output (tsconfig already has `noUnusedLocals: true` and `noUnusedParameters: true`).
    4. If any of the three commands surfaces a warning or error, STOP and report it — that is genuine dead code that must be removed before AUDIT-02 can be marked complete. Per CONTEXT.md D-08, do NOT remove the `{ Key: parseInt(inputVal) || 0 }` path in `handleCreateMacro` — RESEARCH.md flags it as intentional Phase 3 scaffolding.
    5. In the plan SUMMARY (`.planning/phases/02-cleanup-credibility/02-02-SUMMARY.md`) record:
       - The three commands run and their exit codes
       - The number of warnings/errors from each (must be 0)
       - The statement: "AUDIT-02 verified clean — no dead Rust or TypeScript source code as of {commit SHA}"
       - The intentional retention of the Phase 3 scaffolding path in `handleCreateMacro` (cite CONCERNS.md + RESEARCH.md justification)

    Do NOT modify any Rust or TypeScript source file in this task — the audit is verification only. Per D-08, do NOT touch Cargo.toml dependencies.
  </action>
  <verify>
    <automated>RUSTFLAGS="-Wdead-code" cargo check --manifest-path src-tauri/Cargo.toml 2>&amp;1 | grep -c "^warning:" | grep -qx 0 &amp;&amp; npx tsc --noEmit &amp;&amp; cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `RUSTFLAGS="-Wdead-code" cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | grep -c "^warning:"` equals 0
    - `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` exits 0
    - `npx tsc --noEmit` exits 0 with zero stdout/stderr output
    - `.planning/phases/02-cleanup-credibility/02-02-SUMMARY.md` exists and contains the substring `AUDIT-02 verified clean`
    - SUMMARY records the three exact commands run and their numeric warning counts
    - No source file under `src-tauri/src/` or `src/` was modified in this task (`git diff --name-only -- src-tauri/src/ src/` produces empty output for this task's changes)
  </acceptance_criteria>
  <done>All three audit commands pass clean; SUMMARY records the evidence with command names, exit codes, and warning counts; no source files modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → version control | `.gitignore` controls what enters the public repo; overly narrow patterns leak OS artifacts and debug logs (potential info disclosure). |
| Working tree → public repo | Stale planning documents in the repo root mislead consumers about project state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information disclosure | Untracked `*.log` debug files committed accidentally | mitigate | Task 1 appends `*.log` to root `.gitignore` so future runtime/debug logs cannot be staged accidentally; `git check-ignore` test in acceptance proves the pattern is active. |
| T-02-05 | Information disclosure | Windows `Thumbs.db` shell-cache files leaking on Windows-contributor commits | mitigate | Task 1 appends `Thumbs.db`; counterpart to existing `.DS_Store` rule. |
| T-02-06 | Tampering (over-broad ignore) | New `*.log` pattern accidentally ignoring tracked source files | accept | No source file under `src/` or `src-tauri/src/` ends in `.log`; `git check-ignore README.md` acceptance check guards against accidental over-broad pattern. |
| T-02-07 | Repudiation (stale claim of completeness) | AUDIT-02 declared clean without re-running compilers | mitigate | Task 2 re-runs cargo check, cargo clippy, and tsc at execution time; SUMMARY records command + exit code, not a pre-recorded assertion. |
</threat_model>

<verification>
- `test ! -f implementation_plan.md && echo PASS`
- `grep -E "^(Thumbs\.db|\*\.log)$" .gitignore | wc -l` returns 2
- `git check-ignore -v Thumbs.db debug.log` confirms both matched
- `RUSTFLAGS="-Wdead-code" cargo check --manifest-path src-tauri/Cargo.toml` zero warnings
- `npx tsc --noEmit` exits 0 with no output
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` exits 0
</verification>

<success_criteria>
- `implementation_plan.md` absent from repository (deleted via `git rm`)
- `.gitignore` has new entries for `Thumbs.db` and `*.log`; existing entries unchanged
- Both new patterns confirmed via `git check-ignore`
- AUDIT-02 verification commands all exit clean; SUMMARY records evidence
- No Rust or TypeScript source file modified by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/02-cleanup-credibility/02-02-SUMMARY.md` containing the AUDIT-02 evidence block (commands, exit codes, warning counts, intentional-retention note for the Phase 3 scaffolding path).
</output>
