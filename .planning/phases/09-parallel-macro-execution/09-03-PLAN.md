---
phase: 09-parallel-macro-execution
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - .planning/phases/09-parallel-macro-execution/09-VERIFICATION.md
autonomous: true
requirements: [EXEC-01, EXEC-02]
must_haves:
  truths:
    - "09-VERIFICATION.md exists mirroring 08-VERIFICATION.md with sections: §1 Test Suite, §2 Clippy, §5 macOS manual device test, §6 Windows manual device test, §7 Verification Status. §3 (Windows cross-compile) and §4 (profile backwards-compat) are intentionally omitted per CONTEXT — Phase 9 adds no schema changes and no platform-specific code (D-05)."
    - "§1 shows `cargo test` passing with both new parallel tests present (parallel_two_macros_concurrent, parallel_stop_one_keeps_other) and the total macOS-host test count increased by exactly 2 over the Phase 8 baseline of 9 (i.e. 11 on macOS host)."
    - "§2 shows `cargo clippy --all-targets -- -D warnings` output and confirms Phase 9 introduced NO new warnings (any pre-existing needless_return at ipc/mod.rs is documented as pre-existing, not newly introduced)."
    - "§5 documents 3 macOS manual device tests and §6 documents 3 Windows manual device tests, each mapped 1:1 to a ROADMAP success criterion: T9.1 = enable B while A fires, both fire concurrently; T9.2 = stop A while B runs, B continues; T9.3 = two macros with the same input, both fire and the Phase 8 conflict warning shows (D-08)."
    - "§7 marks the automated gates (§1, §2) with their status and marks §5/§6 as pending human execution on real macOS + Windows hosts (mirrors Phase 8's disposition)."
    - statement: "EXEC-01: on-device confirmation that multiple macros run simultaneously on macOS (§5 T9.1–T9.3) — recorded here for a human verification step to complete."
      verification: backstop
    - statement: "EXEC-02: on-device confirmation that multiple macros run simultaneously on Windows (§6) — recorded here for a human verification step to complete."
      verification: backstop
  artifacts:
    - .planning/phases/09-parallel-macro-execution/09-VERIFICATION.md
  key_links:
    - "§1 test-count gate must reflect the 2 new parallel tests from plan 09-01 (parallel_two_macros_concurrent, parallel_stop_one_keeps_other)."
    - "§5/§6 manual test steps reference the per-card running-state dots from plan 09-02 (pulsing green = firing) as the visual confirmation that both macros fire."
---

<objective>
Produce the Phase 9 verification artifact — the single source of truth for the phase's gate status, mirroring 08-VERIFICATION.md. Run the automated gates (full test suite showing the two new parallel tests pass; clippy clean of new warnings) and document them, then document the 3-per-platform manual device tests that a human executes on real macOS + Windows hosts to close EXEC-01/EXEC-02 on-device.

Purpose: Give EXEC-01/EXEC-02 an auditable completion record (automated proof + documented manual device tests) matching the Phase 8 gate format.
Output: `.planning/phases/09-parallel-macro-execution/09-VERIFICATION.md`.

**Structure decision (D-05):** Mirror ONLY sections §1, §2, §5, §6, §7 of 08-VERIFICATION.md. Omit §3 (Windows cross-compile) and §4 (profile backwards-compat): Phase 9 makes no schema changes (no serde defaults to prove) and adds no target-os-gated platform-specific code (the drop counter is std::sync::atomic and the IPC command is platform-agnostic), so the standard test suite + clippy already cover the shared code. Note this omission rationale explicitly in the artifact so a reviewer sees it is deliberate, not an oversight.

**Flagged assumptions (spec-less probe fallback):** EXEC-01/EXEC-02 were unclassified/unresolved from the edge probe; this artifact records the on-device confirmation half (backstop truths above). The unit-level proof lives in plan 09-01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-parallel-macro-execution/09-CONTEXT.md
@.planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated gates and write 09-VERIFICATION.md sections 1 (Test Suite) + 2 (Clippy)</name>
  <files>.planning/phases/09-parallel-macro-execution/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md (sections 1, 2, and 7 — the exact format, the "Notes on command" convention explaining the automux vs automux-lib package-name nuance, and the per-test verification table)
    - .planning/phases/09-parallel-macro-execution/09-01-SUMMARY.md (the actual test names, total count, and any clippy notes from plan 09-01)
  </read_first>
  <action>
    Create `.planning/phases/09-parallel-macro-execution/09-VERIFICATION.md` with frontmatter matching 08-VERIFICATION.md's shape (phase: 9, slug: parallel-macro-execution, status, nyquist_compliant, created, verified) and a title plus a one-line "single source of truth" intro.

    Run `cd src-tauri && cargo test 2>&1 | tail -40` and capture the verbatim output into a fenced text block in section 1 (Test Suite). Include a "Notes on command" line explaining the package name is automux (with the lib name automux_lib), so `cargo test` from src-tauri/ is the equivalent of the hyphenated form — same convention as 08-VERIFICATION.md. Add a per-test table listing at least the 2 new tests (parallel_two_macros_concurrent, parallel_stop_one_keeps_other) with their plan (09-01), requirement (EXEC-01/EXEC-02), and result. State the test-count math: Phase 8 baseline 9 -> Phase 9 adds 2 -> 11 on the macOS host (note the Windows-cfg-gated windows_mod_constants adds 1 more on a Windows host, so 12 there). Mark section 1 status done only if the suite exits 0 and both parallel tests are present and passing.

    Run `cd src-tauri && cargo clippy --all-targets -- -D warnings 2>&1 | tail -20` and capture output into section 2 (Clippy). Determine whether any warning is NEW to Phase 9 versus pre-existing (compare against the pre-existing needless_return at ipc/mod.rs documented in 08-VERIFICATION.md section 2). Mark section 2 as "done (no new warnings)" if Phase 9 added zero new warnings; document any pre-existing warning exactly as Phase 8 did (origin + disposition). If Phase 9 DID introduce a new warning, that is a plan-09-01 defect — do not mark done; report it so it is fixed upstream.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test 2>&1 | tail -6 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/09-parallel-macro-execution/09-VERIFICATION.md` exists with valid frontmatter (phase: 9, slug: parallel-macro-execution).
    - `grep -c 'parallel_two_macros_concurrent' .planning/phases/09-parallel-macro-execution/09-VERIFICATION.md` returns >= 1 and the same for parallel_stop_one_keeps_other.
    - The artifact contains a "## 1. Test Suite" heading and a "## 2. Clippy" heading.
    - `cd src-tauri && cargo test 2>&1 | grep -E 'test result: ok'` exits 0 (suite green) and the captured output in section 1 shows both parallel tests as ok.
    - Section 1 states the macOS-host total test count as 11 (9 baseline + 2 new).
    - Section 2 explicitly states whether Phase 9 introduced any NEW clippy warning (expected: none).
  </acceptance_criteria>
  <done>09-VERIFICATION.md exists with sections 1 and 2 populated from real cargo test + clippy output; both new parallel tests are shown passing; the test-count math and any pre-existing clippy warning are documented per the Phase 8 convention.</done>
</task>

<task type="auto">
  <name>Task 2: Write sections 5 (macOS manual), 6 (Windows manual), 7 (Verification Status)</name>
  <files>.planning/phases/09-parallel-macro-execution/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md (sections 5, 6, 7 — the manual-test step format, the pre-flight checklists, and the status table + post-completion checklist)
    - .planning/phases/09-parallel-macro-execution/09-CONTEXT.md (D-08 decisions + the "D-08 manual test example (macOS T9.1)" concrete steps in the Specifics section)
    - .planning/ROADMAP.md (Phase 9 "Success Criteria" — the 3 "what must be TRUE" bullets each manual test maps to)
  </read_first>
  <action>
    Append section 5 (Manual macOS Device Test — EXEC-01) with a pre-flight checklist (Accessibility + Input Monitoring granted; app running via `npm run tauri dev` or release build) and 3 numbered device tests, each stating which ROADMAP success criterion it maps to and referencing the plan-09-02 running-state dots as the visual signal:
    - T9.1 (maps to SC1): Create macro A = Left Click at 200ms and macro B = Right Click at 300ms. Enable A (verify A shows a pulsing green firing dot). After ~1s, enable B (verify B shows a pulsing dot AND A keeps its pulsing dot and keeps firing at its own rate — A is not paused, delayed, or doubled). Expected: both fire concurrently.
    - T9.2 (maps to SC3): Same A + B both enabled and firing. Disable A (verify A's dot goes dim/disabled). Expected: B keeps its pulsing dot and continues firing at 300ms — stopping A does not affect B.
    - T9.3 (maps to SC1 + Phase 8 conflict): Create macro A and macro B BOTH as Left Click. Enable both. Expected: both fire (both pulsing) AND the Phase 8 same-input conflict warning region appears. (This scenario's warning is already unit-covered by Phase 8's conflict_detection_overlap; here it is the device-level confirmation that parallel firing and the warning coexist.)

    Append section 6 (Manual Windows Device Test — EXEC-02) mirroring the 3 tests with Win32-appropriate setup (SendInput injection; the app running on a Windows host; note if UAC/SmartScreen requires elevation). Label them 6.1/6.2/6.3 mapping to the same three criteria (6.1 -> SC2 concurrent on Windows, 6.2 -> SC3 stop-one, 6.3 -> same-input concurrent + conflict warning).

    Append section 7 (Verification Status) with a status table (one row per section 1/2/5/6) and a note that §3 and §4 are intentionally omitted with the D-05 rationale. Mark section 1 and section 2 with their real automated status from Task 1; mark sections 5 and 6 as pending (requires human — must be run on real macOS and Windows hosts). Add a "Post-completion checklist for the user" listing: run T9.1–T9.3 on macOS and mark §5 done; run 6.1–6.3 on Windows and mark §6 done; the phase is fully complete when §1/§2 are done and §5/§6 are done.
  </action>
  <verify>
    <automated>grep -cE '^## (5|6|7)\.' .planning/phases/09-parallel-macro-execution/09-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^## (1|2|5|6|7)\.' .planning/phases/09-parallel-macro-execution/09-VERIFICATION.md` returns 5 (sections 1,2,5,6,7 all present; 3 and 4 absent by design).
    - `grep -c 'T9.1\|T9.2\|T9.3' .planning/phases/09-parallel-macro-execution/09-VERIFICATION.md` returns >= 3 (the three macOS manual tests are present).
    - Section 6 contains 3 Windows manual tests (grep for "6.1", "6.2", "6.3").
    - Section 7 contains a status table and explicitly states §3 and §4 are intentionally omitted per D-05.
    - Each of the 3 macOS tests names the ROADMAP success criterion it maps to (grep for "SC1"/"SC2"/"SC3" or the criterion wording).
    - Section 7 marks §5 and §6 as pending/requires-human (not done), matching Phase 8's disposition.
  </acceptance_criteria>
  <done>09-VERIFICATION.md is complete with 3 macOS + 3 Windows manual device tests mapped to the ROADMAP success criteria, a status table, the D-05 omission rationale for §3/§4, and a post-completion checklist for the human verifier.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files newly created across Phase 9 (read by the plan-review-convergence source-grounding pass to exclude these from drift verification):
- `09-VERIFICATION.md` — verification artifact (this plan)
- `parallel_two_macros_concurrent`, `parallel_stop_one_keeps_other` — #[tokio::test] in src-tauri/src/scheduler/mod.rs (plan 09-01)
- `ACTION_DROP_COUNT`, `get_action_drop_count` — src-tauri/src/scheduler/mod.rs (plan 09-01)
- `get_debug_action_drop_count` — src-tauri/src/ipc/mod.rs (plan 09-01)
- `computeRunningState` + `RunningState` — src/App.tsx (plan 09-02)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only writes an internal planning/verification document; it introduces no runtime code and no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Information Disclosure | 09-VERIFICATION.md contents | low | accept | Internal planning artifact; contains test names, gate output, and manual test steps only — no secrets or credentials |
| T-09-SC | Tampering | cargo/npm dependency additions | low | accept | No installs in this plan; it runs existing gates and writes documentation only |
</threat_model>

<verification>
- 09-VERIFICATION.md exists and mirrors 08-VERIFICATION.md's §1/§2/§5/§6/§7 structure.
- `cd src-tauri && cargo test` — green, with both new parallel tests passing (documented in §1).
- `cd src-tauri && cargo clippy --all-targets -- -D warnings` — no NEW warnings (documented in §2).
- 3 macOS + 3 Windows manual device tests documented, each mapped to a ROADMAP success criterion.
</verification>

<success_criteria>
- The phase has an auditable verification record matching the Phase 8 gate format.
- Automated gates (test suite + clippy) are green and documented with real output.
- Manual device tests are documented for human execution to close EXEC-01/EXEC-02 on-device.
</success_criteria>

<output>
Create `.planning/phases/09-parallel-macro-execution/09-03-SUMMARY.md` when done.
</output>
