---
phase: 07-carry-work-platform-ci-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/platform/windows/mod.rs
  - .github/workflows/release.yml
autonomous: true
requirements:
  - BUILD-01
  - MEM-01
  - CI-03
  - CI-04
  - CI-05
  - SAFE-04

must_haves:
  truths:
    - "`cargo build --target x86_64-pc-windows-msvc` produces zero warnings about unused imports or unused must_use calls on platform/windows/mod.rs"
    - "Every OpenProcess call in get_app_name_from_hwnd is matched by an unsafe CloseHandle call before the function returns, on both success and early-return paths"
    - "The release workflow does not contain a step that attempts to upload an updater .sig artifact (the prior 'Signature not found' skip is gone)"
    - "The release workflow installs frontend dependencies with `npm ci` (lockfile-gated), not `npm install`"
    - "flush_held_inputs in observer.rs drains the REGISTRY into a local Vec, releases the lock, and only then posts CGEvents — the `// @safety-officer: CR-01` comment block documents the lock-ordering invariant"
  artifacts:
    - path: "src-tauri/src/platform/windows/mod.rs"
      provides: "Windows platform module with unused Win32 imports removed and CloseHandle wired after every OpenProcess"
      contains: "CloseHandle"
    - path: ".github/workflows/release.yml"
      provides: "Release workflow hardened: no updater sig upload step, `npm ci` install, tauri-action id and output variable reference"
      contains: "npm ci"
    - path: "src-tauri/src/platform/macos/observer.rs"
      provides: "Verified lock-ordering invariant in flush_held_inputs (no code change required)"
      contains: "@safety-officer: CR-01"
  key_links:
    - from: "src-tauri/src/platform/windows/mod.rs"
      to: "Windows kernel"
      via: "CloseHandle after every OpenProcess"
      pattern: "CloseHandle\\(handle\\)"
    - from: ".github/workflows/release.yml"
      to: "tauri-action"
      via: "steps.tauri.outputs.artifactPaths"
      pattern: "steps\\.tauri\\.outputs\\.artifactPaths"
    - from: "src-tauri/src/platform/macos/observer.rs"
      to: "REGISTRY mutex"
      via: "drain Vec then release lock before CGEvent post"
      pattern: "let inputs_to_flush: Vec<ActiveInput>"

---

<objective>
Resolve the three Windows platform cleanup items (BUILD-01 unused imports, MEM-01 OpenProcess/CloseHandle pairing, TranslateMessage must_use), the three CI release hardening items (CI-03 updater sig step removal, CI-04 npm ci, CI-05 tauri-action artifactPaths), and verify that the macOS emergency-stop lock ordering already satisfies SAFE-04 without code change.

Purpose: This plan clears the v1.2.0 carryover work on non-macOS surfaces (Windows + CI) and confirms a Phase 6 cleanup has already landed for SAFE-04. The macOS 26 follow-ups (COMPAT-04, COMPAT-05) and the auto-save error UI (ERR-01) are deferred to plans 07-02 and 07-03.

Output:
- src-tauri/src/platform/windows/mod.rs — unused imports trimmed, CloseHandle wired, TranslateMessage warning suppressed
- .github/workflows/release.yml — updater sig step removed, npm ci in place, tauri-action id + output variable referenced
- src-tauri/src/platform/macos/observer.rs — verified-correct lock-ordering, no edit
</objective>

<execution_context>
@/Users/alvaro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alvaro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-carry-work-platform-ci-safety/07-CONTEXT.md
@.planning/phases/07-carry-work-platform-ci-safety/07-RESEARCH.md
@.planning/phases/07-carry-work-platform-ci-safety/07-PATTERNS.md
@.planning/phases/07-carry-work-platform-ci-safety/07-VALIDATION.md

# Source files this plan reads but does not modify
@src-tauri/src/platform/windows/mod.rs — current full file (531 lines); the two functions being patched are at lines 263–291 (get_app_name_from_hwnd) and 384–401 (mid-file imports)
@src-tauri/src/platform/macos/observer.rs — current full file (619 lines); flush_held_inputs at lines 80–138; emergency-stop path at lines 351–415
@.github/workflows/release.yml — current full file (60 lines)
</context>

<interfaces>
<!-- Key interfaces and exact locations the executor needs. Do not explore — use these directly. -->

From src-tauri/src/platform/windows/mod.rs:
- Top-of-file imports (lines 10–21): SendInput, INPUT, INPUT_0, INPUT_TYPE, KEYBDINPUT, KEYEVENTF_KEYUP, MOUSEEVENTF_ABSOLUTE, MOUSEEVENTF_LEFTDOWN, MOUSEEVENTF_LEFTUP, MOUSEEVENTF_MIDDLEDOWN, MOUSEEVENTF_MIDDLEUP, MOUSEEVENTF_MOVE, MOUSEEVENTF_RIGHTDOWN, MOUSEEVENTF_RIGHTUP, MOUSEINPUT, GetForegroundWindow, GetWindowThreadProcessId, HWND — all in active use, do NOT remove.
- get_app_name_from_hwnd (lines 263–291): the OpenProcess at line 279 is followed by QueryFullProcessImageNameW then `?` — CloseHandle must run before `?` on the QueryFullProcessImageNameW result.
- Mid-file imports (lines 384–401): HMODULE at line 389, HHOOK at line 399, GetWindowTextW/IsWindowVisible at line 301 (top-level) and re-imported inside enum_callback at line 307, TranslateMessage at line 398 used at line 514.

From src-tauri/src/platform/macos/observer.rs:
- flush_held_inputs (lines 80–138): the existing drain-block at lines 85–88 plus the `@safety-officer: CR-01` comment at lines 81–84 are the verification target. If present → SAFE-04 verified.

From .github/workflows/release.yml:
- Line 28: `run: npm install` → change to `run: npm ci`.
- Line 35–36: tauri-action invocation at `tauri-apps/tauri-action@84b9d35b5fc46c1e45415bdb6144030364f7ebc5` (v0) — needs `id: tauri` to expose output variables.
- The file currently has NO updater .sig upload step (CI-03 step absent in the committed file) — add the explanatory comment at the Build and Release Tauri App step noting the intentional omission, but no removal needed.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Windows platform cleanup — CloseHandle, unused imports, TranslateMessage must_use (BUILD-01, MEM-01)</name>
  <files>src-tauri/src/platform/windows/mod.rs</files>
  <read_first>
    - src-tauri/src/platform/windows/mod.rs (entire file, 531 lines) — the file being modified
    - .planning/phases/07-carry-work-platform-ci-safety/07-PATTERNS.md "src-tauri/src/platform/windows/mod.rs" section — disposition table for each import symbol
    - .planning/phases/07-carry-work-platform-ci-safety/07-RESEARCH.md "Pitfall 3" and "Pitfall 4" — CloseHandle ordering and HMODULE/HHOOK audit notes
    - .planning/phases/07-carry-work-platform-ci-safety/07-CONTEXT.md "BUILD-01" and "MEM-01" entries — locked scope
  </read_first>
  <behavior>
    - get_app_name_from_hwnd: when QueryFullProcessImageNameW succeeds OR fails, the OpenProcess handle is closed via unsafe CloseHandle(handle) before the function returns or propagates
    - Top-level use statement at line 301 importing GetWindowTextW/IsWindowVisible from WindowsAndMessaging no longer exists (the inner re-import at line 307 stays)
    - HMODULE import at line 389 is removed (SetWindowsHookExW at line 484 passes None for the module handle, so the import is unused)
    - HHOOK import at line 399 is kept (used at line 518 in UnhookWindowsHookEx(hook.unwrap()))
    - TranslateMessage(&msg) at line 514 is prefixed with `let _ =` to silence the must_use lint
    - `use windows::Win32::Foundation::CloseHandle;` is added to the local import block in get_app_name_from_hwnd (line 264 area)
  </behavior>
  <action>
    Modify src-tauri/src/platform/windows/mod.rs in three places.

    Change 1 — get_app_name_from_hwnd (lines 263–291): add CloseHandle to the local use block, rebind the QueryFullProcessImageNameW result to a local named `query_result`, call `unsafe { CloseHandle(handle); }` immediately after the QueryFullProcessImageNameW block (before the `?` on the result), then call `query_result.ok()?` to propagate. The handle is now closed on both the success and failure paths. The early-return paths above (HWND default, process_id == 0) never reach OpenProcess, so they need no CloseHandle.

    Concrete edits to make in the function body:
    - Add `CloseHandle,` to the `use windows::Win32::System::Threading::{...}` block — wait, CloseHandle lives in `windows::Win32::Foundation`. Add a separate `use windows::Win32::Foundation::CloseHandle;` line above the existing Threading import.
    - Change `QueryFullProcessImageNameW(...)\n    .ok()?;` to:
        let query_result = QueryFullProcessImageNameW(...);
        unsafe { CloseHandle(handle); }
        query_result.ok()?;
    - Keep `let handle = OpenProcess(...).ok()?;` and the rest of the function unchanged.

    Change 2 — top-level imports (line 301 area): the line that reads `use windows::Win32::UI::WindowsAndMessaging::{GetWindowTextW, IsWindowVisible};` (or equivalent inside list_running_apps_impl) imports GetWindowTextW/IsWindowVisible at top-level. Remove GetWindowTextW and IsWindowVisible from THIS top-level import. The inner re-import inside `unsafe extern "system" fn enum_callback` (line 307) must remain — that is where they are actually used.

    Change 3 — mid-file imports (lines 384–401):
    - Remove HMODULE from the line `use windows::Win32::Foundation::{HMODULE, LPARAM, LRESULT, WPARAM};` (line 389). Keep LPARAM, LRESULT, WPARAM.
    - Keep HHOOK at line 399 — do NOT remove. It is required at line 518: `UnhookWindowsHookEx(hook.unwrap())`.
    - Keep TranslateMessage at line 398.

    Change 4 — TranslateMessage call site (line 514): change `TranslateMessage(&msg);` to `let _ = TranslateMessage(&msg);`. This silences the `must_use` warning by acknowledging the return value is intentionally discarded.

    Do NOT remove any other imports. Do NOT change the HHOOK, LPARAM, LRESULT, WPARAM, MSG, or any other imports in lines 384–401. The SendInput, INPUT, KEYBDINPUT, MOUSEINPUT, etc. imports at lines 10–21 are all in active use and must remain.
  </action>
  <verify>
    <automated>grep -n "CloseHandle" /Users/alvaro/AutoClicker/src-tauri/src/platform/windows/mod.rs | grep -v "^.*:.*//.*CloseHandle" | head -10</automated>
    <automated>grep -n "HMODULE" /Users/alvaro/AutoClicker/src-tauri/src/platform/windows/mod.rs | head -5</automated>
    <automated>grep -n "HHOOK" /Users/alvaro/AutoClicker/src-tauri/src/platform/windows/mod.rs | head -5</automated>
    <automated>grep -n "let _ = TranslateMessage" /Users/alvaro/AutoClicker/src-tauri/src/platform/windows/mod.rs | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep for "CloseHandle" in windows/mod.rs returns at least 2 matches: one in the new `use` import line and one in the `unsafe { CloseHandle(handle); }` call inside get_app_name_from_hwnd
    - grep for "HMODULE" returns 0 matches in the top-level import block (any remaining HMODULE references inside the function body or in a `use` block inside the function are acceptable; what matters is that the unused top-level import is gone)
    - grep for "HHOOK" returns at least 1 match (HHOOK is still imported because it is used in UnhookWindowsHookEx)
    - grep for `let _ = TranslateMessage` returns 1 match
    - The get_app_name_from_hwnd function compiles: the body now reads `let handle = OpenProcess(...).ok()?;` then a local QueryFullProcessImageNameW bind then `unsafe { CloseHandle(handle); }` then `query_result.ok()?`
  </acceptance_criteria>
  <done>Windows platform module compiles with zero unused-import warnings on its top-level/mid-file imports, and every OpenProcess in get_app_name_from_hwnd is matched by CloseHandle before the function returns. HMODULE is removed from the unused import; HHOOK, GetAsyncKeyState, the keyboard/mouse constants, and all active-use symbols remain. TranslateMessage is wrapped in `let _ =` to silence the must_use lint.</done>
</task>

<task type="auto">
  <name>Task 2: Release workflow hardening — remove updater sig step, npm ci, tauri-action artifactPaths (CI-03, CI-04, CI-05)</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml (entire file, 60 lines) — the file being modified
    - .planning/phases/07-carry-work-platform-ci-safety/07-PATTERNS.md ".github/workflows/release.yml" section — exact patterns and pitfall warnings
    - .planning/phases/07-carry-work-platform-ci-safety/07-RESEARCH.md "Pitfall 5" — artifactPaths camelCase vs kebab-case
    - .planning/phases/07-carry-work-platform-ci-safety/07-CONTEXT.md "D-15", "D-16", "D-17" — locked decisions
  </read_first>
  <behavior>
    - The release workflow installs frontend dependencies with `npm ci` (line 28 was `npm install`, now `npm ci`)
    - The tauri-action invocation at line 35 has `id: tauri` so the output variables are accessible as `steps.tauri.outputs.*`
    - The line `tauri-apps/tauri-action@84b9d35b5fc46c1e45415bdb6144030364f7ebc5  # v0` is unchanged (do NOT bump the SHA — D-17 references this exact pin)
    - The release workflow has NO step that references `.sig` files, `Signature not found`, or `updater` (CI-03 step is absent in the committed file; add a comment near the Build and Release step explaining the intentional omission)
    - If any explicit upload step exists or is added in the future, it must reference `${{ steps.tauri.outputs.artifactPaths }}` (camelCase — not `artifact-paths` kebab)
  </behavior>
  <action>
    Modify .github/workflows/release.yml in three places.

    Change 1 (CI-04 / D-16): On line 28, change `run: npm install` to `run: npm ci`. The step name "Install Frontend Dependencies" stays unchanged.

    Change 2 (CI-05 / D-17): On the "Build and Release Tauri App" step (line 35), add `id: tauri` on the line directly under `- name: Build and Release Tauri App`. The exact indentation matches the existing step (2-space indent for step key, 4-space indent for `id`/`uses`/`with`). Example target shape:

        - name: Build and Release Tauri App
          id: tauri
          uses: tauri-apps/tauri-action@84b9d35b5fc46c1e45415bdb6144030364f7ebc5  # v0
          env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          with:
            tagName: ${{ github.ref_name }}
            ...

    Change 3 (CI-03 / D-15): Add a comment ABOVE the "Build and Release Tauri App" step (between the "Setup Rust" step and the "Build and Release Tauri App" step) that reads exactly:

        # Updater signature upload removed — re-add when real signing is configured (v3 scope)

    The actual sig-upload step does not exist in the current file — this comment documents the intentional omission so future contributors do not re-introduce it. The comment is single-line, 2-space indented, with a leading `#` and a single space.

    Do NOT change the tauri-action SHA. Do NOT change `releaseDraft: true`, `prerelease: false`, the tagName expression, or any other field. Do NOT add new explicit upload steps — the tauri-action handles uploads via its own internals. Do NOT touch the "Verify universal binary" step (line 47 onward).
  </action>
  <verify>
    <automated>grep -n "npm ci\|npm install" /Users/alvaro/AutoClicker/.github/workflows/release.yml | head -5</automated>
    <automated>grep -n "id: tauri" /Users/alvaro/AutoClicker/.github/workflows/release.yml | head -5</automated>
    <automated>grep -c "Signature not found\|sig.*upload\|updater" /Users/alvaro/AutoClicker/.github/workflows/release.yml | head -5</automated>
    <automated>grep -n "Updater signature upload removed" /Users/alvaro/AutoClicker/.github/workflows/release.yml | head -5</automated>
    <automated>grep -n "steps.tauri.outputs.artifactPaths" /Users/alvaro/AutoClicker/.github/workflows/release.yml | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep for "npm ci" in release.yml returns ≥ 1 match
    - grep for "npm install" in release.yml returns 0 matches (the old install line is fully replaced)
    - grep for "id: tauri" in release.yml returns ≥ 1 match
    - grep for "Updater signature upload removed" in release.yml returns ≥ 1 match
    - grep for "Signature not found\|sig.*upload\|updater" in release.yml returns 0 matches (the comment uses different wording and must not match the CI-03 grep gate)
    - grep for "steps.tauri.outputs.artifactPaths" in release.yml returns 0 matches (no explicit upload step exists in the current file — this is correct)
  </acceptance_criteria>
  <done>Release workflow installs frontend deps via `npm ci`, the tauri-action step has `id: tauri` for output reference, and an explanatory comment documents why the updater sig upload step is intentionally absent. CI-03, CI-04, CI-05 verification gates all pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verify SAFE-04 lock-ordering invariant is already correct in observer.rs (no code change)</name>
  <files>src-tauri/src/platform/macos/observer.rs</files>
  <read_first>
    - src-tauri/src/platform/macos/observer.rs lines 80–138 — flush_held_inputs (primary verification target)
    - src-tauri/src/platform/macos/observer.rs lines 351–415 — emergency-stop path in the CGEventTap callback (secondary verification target)
    - .planning/phases/07-carry-work-platform-ci-safety/07-RESEARCH.md "SAFE-04 Status — Already Correct" section — describes the verified-correct pattern
    - .planning/phases/07-carry-work-platform-ci-safety/07-PATTERNS.md "src-tauri/src/platform/macos/observer.rs — SAFE-04 verification only" section
  </read_first>
  <behavior>
    - flush_held_inputs drains the REGISTRY into a local Vec<ActiveInput> via `let mut reg = get_registry().lock().unwrap(); reg.drain().collect();` inside a brace block, releasing the lock at end of block — NO CGEvent dispatch happens inside the lock
    - The drain-block is preceded by a `// @safety-officer: CR-01` comment block that documents the invariant in plain language
    - The same drain-before-release pattern is duplicated in the emergency-stop path (lines 351–415) inside the CGEventTap callback, so the macro-trigger emergency stop also cannot deadlock
  </behavior>
  <action>
    Verification-only task. NO code change to observer.rs.

    1. Open src-tauri/src/platform/macos/observer.rs and read lines 80–138 (the flush_held_inputs function). Confirm visually that the function body matches this shape:
       - A comment block beginning with `// @safety-officer: CR-01` that explains why the lock must be released before any CGEvent post.
       - A `let inputs_to_flush: Vec<ActiveInput> = { let mut reg = get_registry().lock().unwrap(); reg.drain().collect() };` block — the lock is held only inside the inner braces and dropped at the closing brace.
       - After the `};` and outside the lock: a `core_graphics::event_source::CGEventSource::new(...)` call, then a `for input in inputs_to_flush.iter()` loop that posts CGEvents.

    2. Open lines 351–415 (the emergency-stop path inside the CGEventTap callback) and confirm the same drain-before-release pattern is present. The variable name may be `inputs_to_release` instead of `inputs_to_flush`; either is acceptable.

    3. Run `grep -n "@safety-officer: CR-01" src-tauri/src/platform/macos/observer.rs` from /Users/alvaro/AutoClicker and confirm at least 2 matches (one for flush_held_inputs, one for the emergency-stop path).

    4. Run `grep -n "let inputs_to_flush\|let inputs_to_release" src-tauri/src/platform/macos/observer.rs` and confirm at least 2 matches.

    If all four checks pass: SAFE-04 is already satisfied. Do NOT modify the file. Document the verification result in the SUMMARY by quoting the relevant lines (line numbers of the drain-block and the comment).

    If any check fails: STOP. Do not attempt to write the fix in this plan. The fix belongs in a follow-up plan because the pattern is intricate (re-entrancy safety, CFRunLoop interaction, TAP_STARTING guard). Surface the finding to the orchestrator.

    Do NOT run `cargo build` for this task — the verification is purely a read-and-grep exercise. Cargo build is too slow to use as the verify signal here.
  </action>
  <verify>
    <automated>grep -n "@safety-officer: CR-01" /Users/alvaro/AutoClicker/src-tauri/src/platform/macos/observer.rs | head -10</automated>
    <automated>grep -n "let inputs_to_flush\|let inputs_to_release" /Users/alvaro/AutoClicker/src-tauri/src/platform/macos/observer.rs | head -10</automated>
    <automated>grep -n "reg.drain().collect" /Users/alvaro/AutoClicker/src-tauri/src/platform/macos/observer.rs | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep for "@safety-officer: CR-01" returns ≥ 2 matches
    - grep for "let inputs_to_flush" OR "let inputs_to_release" returns ≥ 2 matches
    - grep for "reg.drain().collect" returns ≥ 1 match (the drain call inside the lock)
    - The SUMMARY file produced by this plan records the actual line numbers of the verified drain-block and comment so future audits do not have to re-read the file
  </acceptance_criteria>
  <done>SAFE-04 lock-ordering invariant is verified to already be in place. The drain-before-release pattern is present in both flush_held_inputs (line 85 onward) and the CGEventTap emergency-stop callback. No code change is made to observer.rs. REQUIREMENTS.md SAFE-04 status is ready to be marked complete by the orchestrator after this plan's SUMMARY is approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust compiler → developer | BUILD-01 / MEM-01 fixes must be visible to the compiler (no warnings on Windows target) |
| Windows kernel → `get_app_name_from_hwnd` | Win32 OpenProcess / QueryFullProcessImageNameW acquire a process handle that MUST be released |
| GitHub Actions runner → tauri-action | The release workflow pins an exact tauri-action SHA and references its official output variables |
| OS TCC → flush_held_inputs dispatch loop | Re-entrancy risk: a CGEventTap callback may synchronously fire on injected events; the lock must not be held during dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | HANDLE leak in `get_app_name_from_hwnd` | mitigate | CloseHandle wired before every `?` on QueryFullProcessImageNameW; both success and failure paths release the handle |
| T-07-02 | Information Disclosure | `release.yml` with `npm install` | mitigate | Replace with `npm ci` so install is lockfile-gated; no unverified transitive deps |
| T-07-03 | Tampering | updater signature upload step in CI | mitigate | Step absent from current workflow; explicit comment documents the intentional omission so future contributors do not re-introduce the broken "Signature not found" path |
| T-07-04 | Denial of Service | Re-entrant deadlock in `flush_held_inputs` | accept (already mitigated) | Verified in Task 3: drain-before-release pattern is in place; do not introduce edits to this function |
| T-07-SC | Tampering | npm/cargo installs | accept | No new packages installed in this plan; existing dependencies are unchanged |
</threat_model>

<verification>
After all three tasks complete:

1. `grep -n "CloseHandle" src-tauri/src/platform/windows/mod.rs` returns ≥ 2 matches (one in the new `use` import, one in the call site inside get_app_name_from_hwnd)
2. `grep -n "HMODULE" src-tauri/src/platform/windows/mod.rs` returns 0 matches in the top-level import (or 0 matches overall if no function-body references exist)
3. `grep -n "HHOOK" src-tauri/src/platform/windows/mod.rs` returns ≥ 1 match (still imported because it is used in UnhookWindowsHookEx)
4. `grep "npm ci" .github/workflows/release.yml` returns ≥ 1 match; `grep "npm install" .github/workflows/release.yml` returns 0 matches
5. `grep "id: tauri" .github/workflows/release.yml` returns ≥ 1 match
6. `grep -c "Signature not found\|sig.*upload\|updater" .github/workflows/release.yml` returns 0
7. `grep "@safety-officer: CR-01" src-tauri/src/platform/macos/observer.rs` returns ≥ 2 matches
8. `grep "let inputs_to_flush\|let inputs_to_release" src-tauri/src/platform/macos/observer.rs` returns ≥ 2 matches
9. SUMMARY.md records the verified line numbers and quotes the relevant comment block

No `cargo build` is required in this plan (the Windows cross-compile target is not installed on the dev machine — CI verifies BUILD-01 on a Windows runner; the local verification is a read-and-grep exercise).
</verification>

<success_criteria>
- Windows platform module has no unused-import warnings on its own imports (verified by grep that the unused symbols are gone and the used symbols remain)
- Every OpenProcess in get_app_name_from_hwnd is matched by CloseHandle before the function returns, on both the success and early-return paths (RESEARCH.md Pitfall 3)
- Release workflow installs frontend deps via `npm ci` and exposes tauri-action outputs via `id: tauri`; an explanatory comment documents the intentional absence of the updater sig upload step
- SAFE-04 lock-ordering invariant is verified in place — REQUIREMENTS.md SAFE-04 can be marked complete
- All three CI-03/04/05 grep gates return the expected counts (0 for CI-03 sig-updated match, ≥ 1 for npm ci and id: tauri)
- The SUMMARY documents verification results and line numbers, providing a clean handoff for the orchestrator to mark the requirements complete
</success_criteria>

<output>
Create .planning/phases/07-carry-work-platform-ci-safety/07-01-SUMMARY.md when done
</output>

## Artifacts this phase produces

> Listed once for the entire phase. The plan-review-convergence source-grounding pass reads this section to exclude newly-created symbols from drift verification.

### Plan 07-01 — Windows & CI

- **New symbols (none):** No new functions, structs, fields, or IPC commands in this plan. All changes are edits to existing files.
- **Modified symbols (Rust):**
  - `src-tauri/src/platform/windows/mod.rs::get_app_name_from_hwnd` — body restructured: rebinds QueryFullProcessImageNameW to `query_result`, calls `unsafe { CloseHandle(handle); }` before `query_result.ok()?`.
  - `src-tauri/src/platform/windows/mod.rs` top-of-file imports — adds `use windows::Win32::Foundation::CloseHandle;` local to the function.
  - `src-tauri/src/platform/windows/mod.rs` line 301 area — top-level `GetWindowTextW, IsWindowVisible` removed from list_running_apps_impl's `use` block (inner re-import inside `enum_callback` retained).
  - `src-tauri/src/platform/windows/mod.rs` line 389 — HMODULE removed from the unconditional `use windows::Win32::Foundation::{...}` block.
  - `src-tauri/src/platform/windows/mod.rs` line 514 — `TranslateMessage(&msg);` becomes `let _ = TranslateMessage(&msg);`.
- **Modified symbols (CI):**
  - `.github/workflows/release.yml` line 28 — `run: npm install` → `run: npm ci`.
  - `.github/workflows/release.yml` line 36 area — adds `id: tauri` to the "Build and Release Tauri App" step.
  - `.github/workflows/release.yml` line 34 area — adds comment `# Updater signature upload removed — re-add when real signing is configured (v3 scope)`.
- **New files (none).**
- **Verification-only:** `src-tauri/src/platform/macos/observer.rs::flush_held_inputs` and the CGEventTap emergency-stop callback — read-and-grep verification of the existing drain-before-release pattern; no edits.