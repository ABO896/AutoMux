---
phase: 10-ui-redesign-macro-management
plan: 06
type: execute
wave: 6
depends_on: [10-05]
files_modified:
  - .planning/phases/10-ui-redesign-macro-management/10-VERIFICATION.md
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UX-08, UX-09, UX-10]
must_haves:
  truths:
    - "cargo build and cargo test pass, and npx tsc --noEmit is clean, after all Phase 10 changes are integrated"
    - "All 22 UI-SPEC Verification Checklist items pass under human visual QA in BOTH light and dark themes"
    - "No new npm dependencies were added this phase (package.json diff is empty except any version bump); no new Cargo dependencies"
    - "Typography holds to exactly 4 size roles (20/15/13/11px) and 2 weights (400 Body, 600 else); spacing holds to the 4/8/16/24/32/48/64 scale plus the declared 84px sidebar and 24px chip-touch-target exceptions"
    - statement: "Idle CPU/GPU with the full redesign active is not measurably higher than the v1.2.0 baseline (final confirmation of the early 10-03 perf gate; UI-04)"
      verification: backstop
    - statement: "UI-01 liquid-glass visual identity — unclassified edge-probe row surfaced as a flagged planner assumption; verified by manual visual QA against the UI-SPEC checklist (no algorithmic edge applies to a pure-visual requirement)"
      verification: backstop
    - statement: "UI-02 Raycast-inspired layout + keyboard navigation — unclassified edge-probe row surfaced as a flagged planner assumption; verified by the sidebar native-tab-order check and manual visual QA"
      verification: backstop
    - statement: "UI-03 Windows modern-equivalent UI — unclassified edge-probe row surfaced as a flagged planner assumption; the same CSS-only mechanism renders identically on WebView2; verified by manual visual QA (on-device Windows check where available)"
      verification: backstop
    - statement: "UI-04 no idle-overhead — unclassified edge-probe row surfaced as a flagged planner assumption; verified by the on-device idle CPU/GPU measurement here and in 10-03"
      verification: backstop
  artifacts:
    - ".planning/phases/10-ui-redesign-macro-management/10-VERIFICATION.md — the phase verification record (checklist results + perf numbers)"
  key_links:
    - "Verification record ties each UI-SPEC checklist item and each phase requirement (UI-01..04, UX-08..10) to a pass/fail with evidence"
---

<objective>
Close the phase with the automated gate (cargo build/test, tsc) plus a human walk of the full UI-SPEC Verification Checklist across both themes, recording results and the final UI-04 perf re-confirmation in a `10-VERIFICATION.md`. This is where the four `unclassified` edge-probe rows for UI-01/UI-02/UI-03/UI-04 — which must stay unresolved rather than be forced into a wired check — are dispositioned as flagged assumptions, verified by manual visual QA against the design contract.

Purpose: prove the redesign and macro-management surfaces meet the contract before the phase is marked complete, and capture evidence for the four visual/perf requirements that have no automated test path in this project (no frontend test framework exists — manual QA against the UI-SPEC checklist is the established sampling mechanism).

Output: passing automated gates + an approved human verification record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md
@.planning/phases/10-ui-redesign-macro-management/10-RESEARCH.md
</context>

## Artifacts this phase produces (introduced by this plan)

- `.planning/phases/10-ui-redesign-macro-management/10-VERIFICATION.md` — records each UI-SPEC Verification Checklist item as pass/fail with evidence, the requirement→result map for UI-01..04 / UX-08..10, and the final idle CPU/GPU numbers vs the v1.2.0 baseline.

## Flagged Assumptions (spec-less probe fallback — unclassified rows, deliberately not wired-checked)

Per the spec-less probe fallback, the four `unclassified` edge-probe rows (UI-01, UI-02, UI-03, UI-04) MUST remain unresolved rather than be forced into a backstop check. They are surfaced here as explicit planner assumptions and confirmed via the manual UI-SPEC checklist walk (this project has no frontend test framework; visual/perf requirements are verified by human QA against the design contract, which is the pre-existing sampling mechanism per CONVENTIONS.md and RESEARCH Validation Architecture):
- UI-01 (liquid-glass visual identity) — no algorithmic edge applies; verified by the checklist's blur/translucency and both-theme rows.
- UI-02 (Raycast layout, keyboard-navigable) — verified by the sidebar native-tab-order + icon+label rows.
- UI-03 (Windows modern equivalent) — same CSS-only mechanism on WebView2; verified by visual QA (on-device Windows where available; otherwise the identical-mechanism argument from RESEARCH stands).
- UI-04 (no idle overhead) — verified by on-device idle CPU/GPU measurement (10-03 early gate + this final re-confirmation).

<tasks>

<task type="auto">
  <name>Task 1: Run automated gates and record results</name>
  <files>.planning/phases/10-ui-redesign-macro-management/10-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md (Verification Checklist — the 22 executor-facing items to record; the automated gate lines: cargo build, cargo test, tsc)
    - .planning/phases/08-hotkey-reliability-conflict-safety/08-VERIFICATION.md (the existing verification-record format to mirror, if present)
  </read_first>
  <action>
Run the automated gates and record their output in a new `10-VERIFICATION.md`: `cd src-tauri && cargo build` (expect 0 errors), `cd src-tauri && cargo test` (expect all pass, including the 3 update_macro tests from 10-01), and `npx tsc --noEmit` (expect clean). Also run `git diff --stat -- package.json` (or inspect the lockfile) to confirm no new npm dependency was added, and confirm `Cargo.toml` gained no new dependency. Record each result with the exact command and its pass/fail. Create the automated-results section of the verification record; leave the human-QA section for Task 2.

Use the `automux` package name (NOT `automux-lib`) for cargo commands, per the Phase 8 decision — `cargo test` runs the whole suite; scoped runs use `cargo test <name>`.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | tail -3 && cargo test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0; `cargo test` reports 0 failed (incl. update_macro_applies_all_fields, update_macro_conflict_no_partial_mutation).
    - `npx tsc --noEmit` exits clean.
    - `git diff --stat -- package.json` shows no new dependency lines (version bumps only, if any).
    - `10-VERIFICATION.md` exists with an automated-results section recording each command + result.
  </acceptance_criteria>
  <done>All automated gates pass and are recorded; no new npm/Cargo dependency was introduced this phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full UI-SPEC Verification Checklist walk (both themes) + final perf re-confirm</name>
  <what-built>The full Phase 10 redesign + macro management: sidebar nav, light/dark theme system, real backdrop-filter translucency, resized window, the two-selector action-type UI with selectable Key Press, inline macro edit persisting all fields, and inline delete confirmation. This checkpoint walks the UI-SPEC's 22-item Verification Checklist across both themes and records the final UI-04 perf re-confirmation — the mechanism for the four visual/perf requirements (UI-01..04) that have no automated test path in this project.</what-built>
  <how-to-verify>
Run the app on your macOS Tahoe device (and a Windows device if available for the UI-03 check) and walk every item in the UI-SPEC "Verification Checklist" section. Confirm at minimum:
1. Sidebar replaces the tab bar; Macros/Profiles are keyboard-reachable (Tab key) and show icon + text label.
2. Theme toggle cycles System → Light → Dark → System; data-theme updates instantly; preference persists across restart; OS appearance change is followed live when preference is System.
3. Both themes render with no dark-only element in light mode (spot-check every card, banner, form field).
4. Cards + sidebar show real blur (content behind visibly blurs). Idle CPU/GPU is not measurably higher than the v1.2.0 baseline (final confirmation of the 10-03 gate; note the numbers).
5. Window opens at 720×680, respects 560×520 minimum, stays resizable.
6. Input selector = exactly Left Click / Right Click / Middle Click / Key Press; selecting Key Press reveals the reused capture widget in both create and edit forms. Mode selector = exactly Pulse (Repeat) / Hold (Sustained).
7. ✎ opens inline edit on the clicked card only; a second card's edit discards the first's unsaved changes; Save Changes persists name, action type, key/button, and timing across an app restart (edit each field independently); Cancel discards with no IPC.
8. ✕ opens the inline C-D1 confirmation (Delete "{name}"? / This can't be undone. / Cancel / Delete), NOT a native dialog; Delete removes the macro, Cancel leaves it; deleting the last macro shows the empty-state card; a failed delete shows the inline banner and keeps the confirm state open.
9. Long macro names / long target-app identifiers truncate with title hover-reveal in both the card header and the delete-confirmation heading.
10. No flash of a blank/unstyled macro-list region on cold launch.
11. Typography (4 sizes / 2 weights) and spacing scale hold with no out-of-scale values.
Record any failures; the phase is not complete until every checklist item passes or is dispositioned.
  </how-to-verify>
  <resume-signal>Type "approved" if all 22 checklist items pass in both themes and idle overhead is not measurably higher than baseline; otherwise list the failing items.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (verification-only) | This plan runs gates and human QA; it introduces no new runtime code or IPC surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-11 | Tampering | a regression slipping through because visual requirements have no automated test | low | mitigate | The human-verify checklist walk across both themes + on-device perf measurement is the established sampling mechanism (no frontend test framework exists); results are recorded in 10-VERIFICATION.md |
| T-10-SC | Tampering | npm/cargo package installs | low | accept | N/A — zero new dependencies; Task 1 explicitly asserts the package.json/Cargo.toml diff is dependency-free |
</threat_model>

<verification>
- Automated: cargo build 0 errors, cargo test 0 failed, tsc clean, no new dependency.
- Human: all 22 UI-SPEC checklist items pass in both themes; final idle-overhead measurement recorded and not measurably above baseline.
</verification>

<success_criteria>
Phase 10 is provably complete against the UI design contract and all seven requirements (UI-01..04, UX-08..10), with automated gates green and a recorded human verification.
</success_criteria>

<output>
Create `.planning/phases/10-ui-redesign-macro-management/10-06-SUMMARY.md` when done.
</output>
