---
phase: 10-ui-redesign-macro-management
plan: 05
type: execute
wave: 5
depends_on: [10-04]
files_modified:
  - src/App.tsx
  - src/components/MacroCard.tsx
autonomous: true
requirements: [UX-08, UX-09]
must_haves:
  truths:
    - "Clicking ✎ expands the card in place into a pre-filled edit form (the shared MacroForm from 10-04); Save Changes persists name, action type (Input + Mode), key/button assignment, and timing via update_macro, and the changes survive an app restart (D-13, UX-09)"
    - "Only one card can be in edit OR delete-confirm state at a time; opening edit on a second card discards the first card's unsaved edit (no confirmation — nothing was persisted), via the App()-owned editingCardId signal"
    - "Clicking ✕ shows an inline in-card confirmation (Delete \"{name}\"? / This can't be undone. / Cancel / Delete) in the card header, NOT a native browser dialog (D-14, UX-08)"
    - "Deleting a macro removes exactly the macro whose id was clicked — even when another macro shares the same display name — because remove_macro targets by id, not name"
    - "Deleting the last remaining macro leaves the empty-state card visible (No macros configured yet. / + Create Your First Macro), not a blank region"
    - "Remaining macro cards keep their prior relative order after a delete (removal does not reorder the list)"
    - "The edit form's Save Changes button is disabled for an empty/whitespace-only name (client guard mirrors the create form); an empty name cannot be persisted via the edit path"
    - "Macro names persist verbatim as UTF-8 (serde String, no data-layer truncation); on-screen truncation is CSS-only (truncate + title), so the full value is preserved on save and across restart"
    - "On a backend conflict from Save Changes (e.g. hotkey already bound), the Phase 8 ConflictErrorToast fires and the edit form stays open so the user can correct and retry"
    - statement: "Once a macro is deleted and its card removed via the state-changed broadcast, no double-delete path remains in the UI (the Delete control no longer exists for that macro)"
      verification: backstop
    - statement: "Delete is a single synchronous remove_macro IPC serialized through the StateActor Intent channel — there is no concurrent-delete UI path to guard"
      verification: backstop
    - statement: "On cold boot the macro-list region renders the empty-state card until the first state-changed payload arrives — no flash of a blank or unstyled list region"
      verification: backstop
    - statement: "A failed remove_macro shows an inline 'Delete failed' / 'Could not delete this macro. Try again.' banner and leaves the card's confirmation state open (the delete intent is not silently discarded)"
      verification: backstop
    - statement: "Long macro names and long target-app identifiers truncate with a title-attribute hover-reveal in BOTH the card header and the delete-confirmation heading; the literal surrounding text of the heading never truncates"
      verification: backstop
  artifacts:
    - "src/components/MacroCard.tsx — normal / inline-edit / inline-delete-confirm display states for one macro"
  key_links:
    - "MacroCard Save Changes → invoke('update_macro') full field set → state-changed → card collapses to normal display"
    - "confirmingDeleteId + editingCardId owned by App(), passed as () => boolean accessor props (mutual exclusion enforced centrally)"
    - "Delete success removes the card via the existing remove_macro IPC + state-changed broadcast (no local list mutation)"
---

<objective>
Extract `MacroCard.tsx` with its three display states — normal, inline edit (C-E1, the primary UX-09 surface), and inline delete confirmation (C-D1, D-14) — completing macro management. The edit state mounts the shared `MacroForm` (10-04) pre-filled with the macro's current values and saves through the `update_macro` path proven by the tracer (10-01), now generalizing the tracer's name-only edit to the full field set. Delete's native browser confirm is replaced by an in-card confirmation matching the glass aesthetic.

Purpose: deliver UX-09 (edit name/action-type/key/timing, persisted) and UX-08's restyled confirmation, with the mutual-exclusion, encoding, ordering, and failure-path edges pinned as observable truths.

Output: `MacroCard.tsx` wired into the list with centralized `editingCardId`/`confirmingDeleteId` state in `App()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-redesign-macro-management/10-CONTEXT.md
@.planning/phases/10-ui-redesign-macro-management/10-RESEARCH.md
@.planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md
@.planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md
</context>

## Artifacts this phase produces (introduced by this plan)

- `src/components/MacroCard.tsx` — new `Component<MacroCardProps>` rendering one macro in normal / edit / confirm-delete states. Props are accessor-style (`isEditing: () => boolean`, `isConfirmingDelete: () => boolean`, and callbacks `onEditStart`/`onEditCancel`/`onEditSave`/`onDeleteStart`/`onDeleteConfirm`/`onDeleteCancel`).
- App.tsx: new `confirmingDeleteId` signal (alongside `editingCardId` at 279-280); the macro-list `<For>` body replaced by `<MacroCard>`; `handleRemoveMacro` reworked to drive the inline confirmation instead of the native dialog; an inline delete-failure banner (reusing the `profileMessage`-style pattern); the full-field `update_macro` save handler (generalizing the 10-01 tracer's name-only version).

<tasks>

<task type="auto">
  <name>Task 1: Extract MacroCard.tsx with normal + inline-edit states (full UX-09)</name>
  <files>src/components/MacroCard.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (READ the macro-card `<For>` block: header row with running-state dot + toggle + delete button; the `editingCardId`/`editingField` signals at 279-280 and the existing target/key inline editors 1320-1401; the tracer's name-only inline edit added in 10-01; `handleRemoveMacro` 519-526; `showConflictError` 241-248; the `MacroConfig`/`RunningState`/`AppState` types; `computeRunningState` from Phase 9)
    - src/components/MacroForm.tsx (the shared form from 10-04 — mounted pre-filled in the edit state)
    - .planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md (Component Inventory C-E1 with the ✎ button classes + placement left of ✕; Interaction Contract UX-09 rows 1-4alt; Accessibility → color+glyph+title pairing; the overflow / long-text UI Considerations rows)
    - .planning/phases/10-ui-redesign-macro-management/10-RESEARCH.md (Pattern 1 signals-down; Code Example → the update_macro argument shape; buildActionSequence)
    - .planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md (MacroCard.tsx section: mutual-exclusion pattern, accessor props, error-handling reuse)
  </read_first>
  <action>
Create `src/components/MacroCard.tsx` rendering ONE macro with a normal display state (running-state dot via the unchanged Phase 9 `computeRunningState`, name, target/trigger meta row, step chips — restyled to the new tokens/typography) and an edit state. Read all props via `props.x` / `props.x()` inline — NEVER destructure (RESEARCH Pitfall 5). Add a `✎` edit icon button to the header action cluster, placed to the LEFT of the existing `✕` delete button, using the exact C-E1 neutral-tinted classes and `title="Edit macro"`. Clicking ✎ calls `props.onEditStart(macro.id)`.

When `props.isEditing()` is true, replace the normal display rows with `<MacroForm>` pre-filled from the macro's current values (name, Input+Mode derived from `macro.sequence.steps` and `macro.trigger_mode`, target_app, trigger key), with the CTA labeled `Save Changes` (accent) and a `Cancel` (neutral) button. `Save Changes` calls `props.onEditSave(...)` which (in App) invokes `update_macro` with the FULL field set — generalizing the 10-01 tracer's name-only call. On success the card collapses to normal (driven by the `state-changed` broadcast + clearing `editingCardId`). On a backend conflict reject, App calls `showConflictError(...)` and keeps `editingCardId` set so the form stays open (UX-09 row 3alt). `Cancel` calls `props.onEditCancel()` — discards local edits, NO IPC.

Long-text handling: the card-header macro-name `<span>` and target-app `<span>` use `truncate` (`overflow-hidden text-ellipsis whitespace-nowrap`) plus a native `title="{full value}"` for hover-reveal (no tooltip component).

In `App.tsx`: keep `editingCardId` centralized in `App()` (mutual exclusion — opening edit on card B while card A edits discards A's edit; do NOT introduce per-card local edit signals). Replace the macro-list `<For>` body with `<MacroCard macro={m} isEditing={() => editingCardId() === m.id} ... />`. The full-field `update_macro` save handler lives in `App()`; it builds the argument object from MacroForm's assembled values and the macro id.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10 && grep -c 'const { ' src/components/MacroCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/MacroCard.tsx` exists and default-exports a `Component`; `grep -c 'const { ' src/components/MacroCard.tsx` returns 0.
    - The ✎ edit button carries `title="Edit macro"` (`grep -c 'Edit macro' src/components/MacroCard.tsx` returns 1) and sits before the ✕ delete button in the header.
    - The edit state mounts MacroForm (`grep -c 'MacroForm' src/components/MacroCard.tsx` returns >= 1).
    - `editingCardId` stays App-owned (no `createSignal` for edit state inside MacroCard: `grep -c 'createSignal' src/components/MacroCard.tsx` returns 0 for edit/confirm state).
    - Card header name and target spans use `truncate` + `title` (`grep -c 'truncate' src/components/MacroCard.tsx` returns >= 1).
    - `npx tsc --noEmit` clean.
    - Manual: edit each field (name, Input, Mode, key/button, interval, target app) independently via ✎ → Save Changes; each change survives an app restart; opening edit on a second card discards the first card's unsaved edit; empty name disables Save Changes.
  </acceptance_criteria>
  <done>MacroCard's inline edit persists the full field set through update_macro across restart, enforces single-card edit mutual-exclusion, and truncates long text with title hover-reveal.</done>
</task>

<task type="auto">
  <name>Task 2: Inline delete confirmation (C-D1) replacing the native dialog + failure banner</name>
  <files>src/components/MacroCard.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (READ `handleRemoveMacro` 519-526 — currently guarded by the native browser confirmation call on line 520; the `profileMessage` signal 222-225 and its render at 1466-1479 for the inline-banner pattern; the empty-state card block for the macro list)
    - src/components/MacroCard.tsx (the component from Task 1 — the header row that swaps to the confirm state)
    - .planning/phases/10-ui-redesign-macro-management/10-UI-SPEC.md (Component Inventory C-D1 with EXACT Cancel/Delete button classes and heading/body typography; Copywriting → destructive confirmation copy `Delete "{macro name}"?` / `This can't be undone.` / `Cancel` / `Delete`; UI Considerations → the delete-fail backstop copy `Delete failed` / `Could not delete this macro. Try again.` and the cold-boot no-flash row; Interaction Contract UX-08 rows 1/2/2alt)
    - .planning/phases/10-ui-redesign-macro-management/10-PATTERNS.md (delete-confirm: no code analog, message/toast pattern for failure)
  </read_first>
  <action>
Replace delete's native browser confirmation with the C-D1 inline in-card confirmation. In `App.tsx`, add a `confirmingDeleteId` signal alongside `editingCardId`. Rework `handleRemoveMacro` (App.tsx:519-526): remove the native browser confirmation-dialog guard on line 520 and instead have the ✕ button set `confirmingDeleteId(macro.id)`. Pass `isConfirmingDelete={() => confirmingDeleteId() === m.id}` plus `onDeleteStart`/`onDeleteConfirm`/`onDeleteCancel` callbacks to `MacroCard`.

In `MacroCard.tsx`, when `props.isConfirmingDelete()` is true, swap the card's header-row content in place to the C-D1 confirmation: heading `Delete "{macro name}"?` (Heading role 15px/600, the quoted name truncates with `title`, the literal `Delete "` / `"?` text never truncates), body `This can't be undone.` (Body role 13px/400), and `Cancel` (neutral) + `Delete` (destructive) buttons using the exact C-D1 classes. `Cancel` calls `props.onDeleteCancel()` (clears `confirmingDeleteId`, no IPC). `Delete` calls `props.onDeleteConfirm(macro.id, macro.name)` which invokes the existing `remove_macro` IPC. On success the macro leaves via the `state-changed` broadcast (no local list mutation). On failure, App shows an inline `Delete failed` / `Could not delete this macro. Try again.` banner (reuse the `profileMessage`-style pattern) and KEEPS the card's confirm state open (do not silently revert) so the user can retry.

Confirm the macro list renders the empty-state card (No macros configured yet. / + Create Your First Macro) whenever the list is empty — including cold boot before the first `state-changed` payload — so there is no flash of a blank/unstyled region and deleting the last macro lands on the empty-state card.

Mutual exclusion: `confirmingDeleteId` and `editingCardId` are both App-owned; entering delete-confirm on one card (or edit on another) clears the other in-progress state so only one card is ever in a non-normal state.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -8 && grep -cE 'window\.confirm' src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The native browser confirmation call is gone: `grep -cE 'window\.confirm' src/App.tsx` returns 0.
    - `confirmingDeleteId` exists and is App-owned (`grep -c 'confirmingDeleteId' src/App.tsx` returns >= 2).
    - MacroCard's confirm state renders the exact copy: `grep -c "This can't be undone." src/components/MacroCard.tsx` returns 1; the heading uses `Delete "` (`grep -c 'Delete \\"' src/components/MacroCard.tsx` returns >= 1).
    - The delete-fail banner copy exists: `grep -c 'Delete failed' src/App.tsx` returns 1.
    - `npx tsc --noEmit` clean.
    - Manual: clicking ✕ shows the inline confirmation (not a native dialog); Delete removes the macro; Cancel leaves it untouched; deleting the last macro shows the empty-state card; a simulated remove_macro failure shows the inline banner and leaves the confirm state open.
  </acceptance_criteria>
  <done>Delete uses the inline C-D1 confirmation with exact copy, no native dialog; failures surface an inline banner without discarding the delete intent; the empty-state card covers the zero-macro and cold-boot cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView (SolidJS) → Tauri IPC | Edit uses `update_macro`; delete uses `remove_macro` |
| IPC → StateActor | Both are serialized Intents processed by the single-owner StateActor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08 | Tampering | edit Save Changes could bypass the trigger-key conflict check | medium | mitigate | Save routes through `update_macro` → `resolve_trigger_key_update` (10-01); a conflict rejects atomically and fires ConflictErrorToast with the form kept open |
| T-10-09 | Repudiation / accidental data loss | deleting the wrong macro when two share a display name | low | mitigate | `remove_macro` and the confirm callback target by macro `id`, not name; the confirmation heading is cosmetic only |
| T-10-10 | Denial of Service (minor) | a failed remove_macro silently dropping the user's delete intent | low | mitigate | On failure the card's confirm state stays open and an inline banner reports it — the user can retry rather than lose the action |
| T-10-SC | Tampering | npm/cargo package installs | low | accept | N/A — zero new dependencies (no install tasks) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- No native browser confirmation call remains in App.tsx; exact C-D1 + delete-fail copy present.
- No prop destructuring, no per-card local edit/confirm signals in MacroCard.
- Manual: full-field edit persists across restart; single-card mutual exclusion; inline delete confirm + failure banner + empty-state on last delete.
</verification>

<success_criteria>
Macro management is complete: users edit every field inline (persisted across restart) and delete via a styled in-app confirmation, with the mutual-exclusion, targeting, encoding, ordering, and failure-path edges observably pinned.
</success_criteria>

<output>
Create `.planning/phases/10-ui-redesign-macro-management/10-05-SUMMARY.md` when done.
</output>
