---
phase: 01-reliability-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src-tauri/src/scheduler/mod.rs
autonomous: true
requirements: [RELY-01, SAFE-03]
tags: [reliability, scheduler, frontend-poll, interval-floor]

must_haves:
  truths:
    - "Granting macOS Accessibility permission while AutoMux is running clears the permissions prompt within ~3 seconds without restarting the app"
    - "Setting any macro step interval below 5ms results in actual scheduled interval of 5ms (clamped silently); 1ms-4ms inputs cannot saturate the OS event queue"
    - "Existing scheduler behavior for intervals >= 5ms is unchanged"
  artifacts:
    - path: "src/App.tsx"
      provides: "Accessibility poll at 3000ms"
      contains: "}, 3000);"
    - path: "src-tauri/src/scheduler/mod.rs"
      provides: "5ms minimum interval floor"
      contains: "interval_ms.max(5)"
  key_links:
    - from: "src/App.tsx (createEffect at line ~124)"
      to: "invoke('check_accessibility')"
      via: "setInterval at 3000ms"
      pattern: "setInterval.*3000"
    - from: "src-tauri/src/scheduler/mod.rs IntervalTask::new"
      to: "Duration::from_millis"
      via: ".max(5) clamp"
      pattern: "interval_ms\\.max\\(5\\)|new_ms\\.max\\(5\\)"
---

<objective>
Deliver two user-visible reliability fixes as a single vertical slice: (1) macOS Accessibility grant is detected within ~3 seconds of returning to AutoMux (RELY-01 / D-01), and (2) the scheduler's documented 5ms minimum interval is actually enforced (SAFE-03 / D-11).

Purpose: Both are trivial single-token edits whose absence violates documented behavior (a 10s poll instead of 3s; a 1ms floor instead of 5ms). Together they restore correct observable behavior at the frontend poll path and the scheduler timer path with zero architectural changes.

Output: One frontend timer constant fix and two scheduler clamp updates. No new files, no new dependencies, no API changes.
</objective>

## Phase Goal

**As a** AutoMux user, **I want to** see the permissions prompt clear within seconds of granting Accessibility and have my macros never run faster than the documented 5ms floor, **so that** I can start using macros immediately after granting permission and the app cannot saturate the OS input queue with a misconfigured 1ms interval.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reliability-safety/01-CONTEXT.md
@.planning/phases/01-reliability-safety/01-RESEARCH.md
@.planning/phases/01-reliability-safety/01-PATTERNS.md

<interfaces>
Relevant exports (no new contracts created in this plan — only constant changes):

From src-tauri/src/scheduler/mod.rs:
- `IntervalTask::new(step_id: StepId, input: InputEvent, interval_ms: u64) -> Self` — currently uses `interval_ms.max(1)` at line 66
- `SchedulerIntent::UpdateInterval { step_id, new_ms }` handler — currently uses `new_ms.max(1)` at line 184

From src/App.tsx:
- `createEffect` accessibility poll at lines 124–134; `setInterval(..., 10000)` at line 133 must become `3000`
- Existing `try/catch (_) {}` silent failure pattern at lines 128–131 is preserved (D-03 says no toast on transition)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix accessibility poll interval (RELY-01, D-01)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (lines 110–140 — to see the createEffect/setInterval/onCleanup polling block in full context, including the surrounding comment "Poll accessibility every 3s")
    - .planning/phases/01-reliability-safety/01-PATTERNS.md (section "RELY-01" — exact target pattern)
    - .planning/phases/01-reliability-safety/01-CONTEXT.md (D-01 through D-04 — confirms no other UI change in this fix)
  </read_first>
  <action>
    Change the literal `10000` to `3000` in the `setInterval` call inside the `createEffect` that polls `check_accessibility` (currently around src/App.tsx:133). Do NOT add a toast, modal, Re-check button, or System Settings redirect — D-02/D-03/D-04 explicitly forbid all four. The pre-existing silent `catch (_) { /* ignore */ }` and `onCleanup(() => clearInterval(interval))` lines must remain unchanged. Do not touch the surrounding `createSignal(accessibility)` declaration, the prompt-rendering JSX, or the "Open Settings" button binding.
  </action>
  <verify>
    <automated>grep -n "setInterval" src/App.tsx | grep -E "3000\\)?\\s*;?\\s*$" && ! grep -nE "setInterval\\([^)]*10000" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains a `setInterval` call ending in `}, 3000);` inside the accessibility-poll `createEffect`
    - src/App.tsx contains NO `setInterval(...)` call with the literal `10000` argument
    - The `onCleanup(() => clearInterval(interval));` line at the end of the createEffect is preserved verbatim
    - `npm run build` (or `npx tsc --noEmit`) exits 0 — no TypeScript errors introduced
    - No new imports, no new signals, no new JSX added — the diff for this file is exactly one numeric literal change
  </acceptance_criteria>
  <done>Frontend poll fires every 3 seconds; permission grant detected within ~3s.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce 5ms scheduler interval floor (SAFE-03, D-11, D-12)</name>
  <files>src-tauri/src/scheduler/mod.rs</files>
  <read_first>
    - src-tauri/src/scheduler/mod.rs (lines 1–80 to see imports, IntervalTask struct, and `IntervalTask::new` at line ~66; and lines 170–200 to see the `SchedulerIntent::UpdateInterval` arm at line ~184)
    - .planning/phases/01-reliability-safety/01-PATTERNS.md (section "SAFE-03" — both sites shown verbatim)
    - .planning/phases/01-reliability-safety/01-RESEARCH.md (D-11 and D-12 — scheduler-layer only, silent clamp)
  </read_first>
  <action>
    Make exactly two substitutions in src-tauri/src/scheduler/mod.rs: change `interval_ms.max(1)` to `interval_ms.max(5)` inside `IntervalTask::new` (line ~66, used to compute `Duration::from_millis(...)` for the initial `interval` field), and change `new_ms.max(1)` to `new_ms.max(5)` inside the `SchedulerIntent::UpdateInterval` handler (line ~184, used to compute `task.interval = Duration::from_millis(...)`). Do NOT return an error, log a warning, or emit a Tauri event on clamp — D-11 specifies silent clamping. Do NOT add validation at the IPC layer or frontend (D-12). Do NOT touch any other `.max(...)` call in the file. Verify after substitution that no `.max(1)` remains in the file.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build --lib 2>&1 | tail -20 && grep -vE '^\\s*//' src/scheduler/mod.rs | grep -cE '\\.max\\(5\\)' | grep -qE '^2$' && ! grep -vE '^\\s*//' src/scheduler/mod.rs | grep -E '\\.max\\(1\\)'</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/src/scheduler/mod.rs` contains exactly two non-comment occurrences of `.max(5)` (one in `IntervalTask::new`, one in `SchedulerIntent::UpdateInterval`)
    - `src-tauri/src/scheduler/mod.rs` contains zero non-comment occurrences of `interval_ms.max(1)` or `new_ms.max(1)`
    - `cargo build --manifest-path src-tauri/Cargo.toml` exits 0
    - Existing scheduler unit tests (`cargo test --manifest-path src-tauri/Cargo.toml -p automux_lib scheduler`) still pass — no tests were modified to accommodate the new floor
    - No new functions, no new error returns, no new event emissions added
  </acceptance_criteria>
  <done>Any input interval of 1–4ms produces an actual scheduled interval of 5ms; intervals >=5ms unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → backend | Frontend `invoke("check_accessibility")` poll; no new payload introduced |
| user → scheduler (via IPC) | `update_step_interval` IPC eventually reaches `SchedulerIntent::UpdateInterval`; the scheduler clamps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | scheduler (interval timer hot path) | mitigate | SAFE-03: enforce `interval_ms.max(5)` at both `IntervalTask::new` and `UpdateInterval` handler so a frontend or persisted profile cannot configure a 1–4ms interval that saturates the OS input queue |
| T-01-02 | Information Disclosure | accessibility poll | accept | Poll only calls existing `check_accessibility` command (already validated); shortening the interval from 10s to 3s does not change the data crossing the boundary, only the frequency |
</threat_model>

<verification>
- `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
- `cargo test --manifest-path src-tauri/Cargo.toml` succeeds (no scheduler tests regress on the new floor; if a test asserts the old 1ms floor, the assumption itself was wrong and the test must be updated — but per PATTERNS no such test exists)
- `npx tsc --noEmit` (or the project's typecheck script) succeeds with zero errors
- Manual smoke: launch app on macOS without Accessibility granted, grant it in System Settings, return to AutoMux — prompt clears within ~3 seconds
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion #1 (RELY-01) satisfied: grant detected without restart
- ROADMAP Phase 1 success criterion #5 (SAFE-03) satisfied: sub-5ms intervals clamped
- No regressions in scheduler behavior for valid (>=5ms) intervals
- Diff is minimal: one frontend literal change, two backend literal changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-reliability-safety/01-01-SUMMARY.md` capturing exact lines changed, before/after snippets, and confirmation that `cargo build` + `cargo test` + frontend typecheck all pass.
</output>
